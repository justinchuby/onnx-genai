# Orchestration Log — Luba

**Timestamp:** 2026-08-12T01:00:00Z
**Wave:** Apple/macOS MLAS f16 cast (PR #31993)
**Model:** Claude Opus

## Spawns

### Spawn 1 — Audit
**Task:** Verify upstream excludes NEON f16↔f32 cast kernel on Apple targets; assess feasibility of re-enabling it.

**Outcome:**
- Confirmed exclusion: `mlas.h:98-105` (`#if !defined(__APPLE__)`), `cmake/onnxruntime_mlas.cmake:608` (`if (NOT APPLE)`), declarations at `mlasi.h:1400`, dispatch at `platform.cpp:810`.
- Corrected premise: kernel uses `vcvt_f32_f16`/`vcvt_f16_f32` and `float16x4_t`, requiring `-march=armv8.2-a+fp16` (NOT baseline ARM64). All Apple Silicon has FEAT_FP16.
- Confirmed: upstream has no ARM64 LayerNormF32 kernel (only RISC-V).
- Output: `.squad/decisions/inbox/luba-apple-mlas-audit.md`

### Spawn 2 — Implementation
**Task:** Implement cast kernel enable for Apple ARM64; open draft PR.

**Outcome:**
- Introduced `MLAS_CAST_F16_NEON_SUPPORTED` macro separate from `MLAS_F16VEC_INTRINSICS_SUPPORTED`.
- `-march=armv8.2-a+fp16` scoped to single file.
- Draft PR #31993 opened: `nxrt/mlas-apple-f16-cast` on `justinchuby/onnxruntime`.
- Output: `.squad/decisions/inbox/luba-apple-f16-cast-impl.md`
