# Orchestration Log — pris

**Timestamp:** 2026-08-14T21:43:12Z
**Agent:** Pris (🧪 Tester — numerics/oracle gates)
**Task:** Build the f64 dequant→GEMM numerics oracle gate for int4 `MatMulNBits` (Marlin sign-off criterion)
**Requested by:** Coordinator (Justin @justinchuby)
**Mode:** background (parallel with Deckard's kernel)

## Summary

Landed a reusable f64 oracle test that any int4 GEMM (today's tiled/GEMV path AND Deckard's Marlin) must
pass before shipping — the apples-to-apples, op-level red/green target for Deckard and the sign-off
criterion for Chew. **Outcome: numerics gate merged (#961, 401af46f); tiled baseline sits ~1 fp16 ULP
inside the envelope, giving Marlin a defensible reference.**

## Deliverables / Findings

- **Oracle:** self-contained integration test
  `crates/onnx-runtime-ep-cuda/tests/matmul_nbits_marlin_numerics.rs` (no edits to `matmul_nbits.rs`, so no
  conflict with `squad/marlin-kernel`). Dequant `(code-zp)·scale` accumulated in f64, sharing the
  candidate's fp16-rounded activations + rounded scale ⇒ residual isolates only accumulation + fp16 output
  rounding. Drives the op purely through ONNX semantics, so Marlin validates with zero test changes.
- **Coverage:** groups {16,32,64,128}, sym + asym-zp, M∈{1,2,4,8,16,32}, real glm-4-9b + Qwen2.5 projection
  shapes, fp16/fp32 scales.
- **Justified envelope** (`Envelope::for_output(max_out)`): `abs=max(max_out·4e-3,4e-3)`, `rel=5e-2`
  (denominator floored `max(1e-1,3e-2·max_out)`) — ~8 fp16-ULP headroom.
- **Baseline (H200):** tiled/GEMV path measures ~8–10× inside abs, ~6× inside rel (≈1 fp16 ULP). Marlin must
  land in the same envelope.
- **Harness lesson:** pre-zero the output + `synchronize()` after execute — an un-synced run reading stale
  device-pool memory faked an abs-81.8 divergence; not a kernel bug.

## Outcome

Merged PR #961 (401af46f). Chew concurred the tolerance is engineering-justified, not a rubber stamp
(verified in his #960 numerics review). Drop `pris-marlin-numerics.md` merged into decisions.md and deleted.
