# Orchestration Log — Deckard

**Timestamp:** 2026-08-12T03:00:00Z
**Wave:** Rejection-response wave
**PR:** microsoft/onnxruntime #32003 (draft)

## Task

Split strict-aliasing/`-Werror` fixes from #31988 into standalone PR #32003.

## Changes

Opened draft PR #32003 with only: `memcpy` replacements for `reinterpret_cast<half2*>` member punning, `(void)` parameter casts, `#include <cstring>`. No cols-per-CTA work, no test changes.

## Finding (incomplete fix)

Fixed `vec_permuted` overload and bf16 overload. Missed 4 identical `vec_a` punning sites at lines 117–120 in the `__CUDA_ARCH__ < 530` fallback. Coordinator found the gap; Isidore completed the fix under lockout.

## Lockout note

Deckard is locked out of reviewing #32003 (author). Isidore assigned.
