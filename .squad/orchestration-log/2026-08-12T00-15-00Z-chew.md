# Orchestration Log — Chew (lockout revision of #31988)

**Timestamp:** 2026-08-12T00:15:00Z
**Agent:** Chew (Opus)
**Wave:** CUDA MatMulNBits upstream workstream

## Task

Lockout revision of PR #31988 after Sebastian's incorrect "SAFE" on the `n % 8 != 0` path.

## Finding

Without a guard, `SelectColsPerBlock` returning 4 would allow `n=12` (12%4==0) to enter the M=1 GEMV path. Upstream declines `n=12` (12%8≠0). This changes shape routing, not just grid geometry — a behavioral change beyond occupancy.

## Fix

Added `n % kColsPerThreadBlock != 0 → return false` before the M=1 call.
Accepted-shape set is now provably identical to upstream (n%8==0 required).

## Tests Added

- `SelectColsPerBlock_OnlyMod8Accepted`: for n∈[1..256] × 5 SM counts, asserts acceptance iff n%8==0. Exhaustive and non-vacuous.
- `SelectColsPerBlock_RoutingInvariance_NMod8Required`: documents why the guard is necessary.

## Template Cost Documented

24 → 72 instantiations (~38 KB binary, bounded by independent .cu TUs). All three variants (8, 4, 2) reachable in production — verified programmatically.

## Commit

`a4aa076657`, pushed to `nxrt/cuda-matmulnbits-sm-cols`. PR stays draft.
