# Nabil (ORT Plugin EP Eng) — Round 1

**Timestamp:** 2026-08-10T21:09:11Z  
**Branch:** squad/ep-plugin-export  
**Routed by:** Coordinator  
**Why:** Fix non-compiling adapter crate + 4 security findings from Holden's initial audit

## Outcome

Crate compiles. Security findings resolved:
- C1: verified `catch_unwind` present on all 9 `extern "C"` callbacks
- H1: confirmed `AtomicPtr` already in use for `HOST_ORT_API` (no `static mut`)
- H2: improved `graphs.is_null()` null check → `ORT_INVALID_ARGUMENT` via new `invalid_arg_status`
- H3: removed unsound `unsafe impl Send + Sync` for `OutboundGraphReader`

Fixed 2 `unused_unsafe` warnings (graph_reader.rs:40,:501). Fixed M1: `check()` extracts real ORT error message. 23 unit tests pass. `clippy -D warnings` clean.
