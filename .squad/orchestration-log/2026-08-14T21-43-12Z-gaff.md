# Orchestration Log — gaff

**Timestamp:** 2026-08-14T21:43:12Z
**Agent:** Gaff (🛡️ Quality / Portability / Safety reviewer)
**Task:** Quality / portability (RULES.md §11) / capture-safety / build-hygiene review of PR #960 (Marlin int4 M>1 GEMM)
**Requested by:** Coordinator (Justin @justinchuby)
**Mode:** review (read-only)

## Summary

Quality review of Deckard's Marlin M>1 GEMM (numerics = Chew; the B\*=2.16 plateau is an accepted honest
plateau, not a defect). Frozen head `a11facbc` (code `3735d57e`). **Verdict: 🟢 APPROVE — zero blocking
defects; one trivial cosmetic test-only note.**

## Findings

- **Rule 11 portability PASS:** genuinely opt-in (`ONNX_GENAI_MARLIN_M_GT_1` default OFF, checked first at
  all three M>1 dispatch seams); SM80 guard correct (`device_supports_marlin` ANDed at every call site — a
  force-set env var on <SM80 still falls through to the portable tiled GEMM); Marlin-OFF is provably
  byte-identical to the prior tiled path; runtime ineligibility / launch error also falls through.
- **Env-var honesty PASS:** both knobs read & wired; `verify_documented_env_vars.py` EXIT 0.
- **Capture-safety PASS:** four caches share one sound contract (cache-hit → warm; cold miss during
  `is_capturing()` → Err so caller falls back, never alloc in capture; pre-warm populates). GQA M>1 Fused
  flash path (on-device totals, no host read-back) and SkipLN latch relaxation (shape-keyed cache rejects
  mid-capture shape change) both verified.
- **Build/lint PASS:** `cargo fmt --all --check` clean; exact CUDA and engine `-D warnings` clippy gates
  clean; all `unsafe` blocks carry SAFETY justifications.

## Note (cosmetic, non-blocking)

- `matmul_nbits.rs:7545` test-only `0xcafef00d_1234_5678u64` trips `clippy::unusual_byte_groupings`
  (warn-level), but it sits in a `#[cfg(test)]` module the CUDA clippy gate does not compile (no
  `--all-targets`), so it does not break CI; trivially fixable.

## Outcome

🟢 APPROVE recorded; drop `gaff-marlin-quality-review.md` merged into decisions.md and deleted.
