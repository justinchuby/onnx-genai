# Orchestration Log — Sebastian — 2026-08-12T06:00:00Z

**Issue:** #762 (EP plugin parity CUDA)
**Branch:** squad/ep-plugin-parity-cuda
**Commit:** a5448fa36
**Mode:** Lockout revision (resolving Challenger S1/S2/S3)

## Summary

Resolved all three substantive findings from Challenger's v4 review:

**S1 — Canary tests now mirror production allocation:**
Extracted `production_scratch_alloc(numel, dtype)` helper matching `compute.rs` exactly.
Added `scratch_buffer_wider_write_absorbed_by_padding` (proves 4-byte writes into f16
slots are absorbed by design) and `scratch_buffer_detects_oversized_write` (proves
16-byte writes overflow production allocation — canary catches it).

**S2 — `mark_absent()` + `validate_write_dtype()`:**
Added `TensorMut::validate_write_dtype(write_dtype)` — for present outputs: exact dtype
match required; for absent outputs: rejects writes exceeding `max(declared_byte_size, 8)`
bytes/elem. Full auto-enforcement would require kernel API restructure; invariant loudly
documented and callable from kernel harnesses and test assertions.

**S3 — `NodeOutputSink::Absent` variant added:**
`build_subgraph_routing` now assigns `Absent` instead of phantom `Buffer` slots.
`num_intermediate_buffers` no longer inflated. Compute path handles `Absent` defensively.

**Nits:** Removed 4 no-op identity `transmute::<&[T], &[T]>` calls from both compute paths.

## Outcome

280 passed / 0 failed (+2 new canary tests). Clippy clean. fmt clean. Miri: 4/4 canary tests clean.
