# Orchestration Log — roy-20

**Timestamp:** 2026-07-14T14:50:00Z
**Agent:** roy-20 (opus)
**Role:** ORT2 optimizer engineer / reviewer (NON-AUTHOR)
**Task:** Review AttentionFusion matcher/guards (optimizer/fusion.rs half)

## Summary

Reviewed Batty's AttentionFusion matcher in `fusion.rs`. Verified matcher soundness with four adversarial decline tests, confirmed all decline guards, scale forms, k_transposed agreement, and bert_toy conformance. Folded Roy's FusedGemm advisory from the previous cycle (permanent expanding-bias decline test) into the commit.

## Adversarial tests run (throwaway, worktree reverted clean)

1. `roy_adv_classifier_head_bias_softmax_declines` — MatMul+bias+Softmax+MatMul with no scalar scale → DECLINES ✅
2. `roy_adv_scaled_but_softmax_not_fed_by_matmul_declines` — full SDPA silhouette but Relu (not MatMul) feeds scale → DECLINES ✅
3. `roy_adv_ambiguous_mask_both_operands_scaled_declines` — both Add operands parse as scale → DECLINES ✅
4. `roy_adv_interior_value_escapes_declines` — valid SDPA but probs is graph output → DECLINES ✅

## Verification performed

- `cargo test -p onnx-runtime-optimizer -p onnx-runtime-session` — PASSED (optimizer 40, session 18)
- `cargo build` (8 crates) — PASSED
- `cargo clippy -p onnx-runtime-optimizer --all-targets -- -D warnings` — PASSED
- `cargo test -p onnx-runtime-ep-cpu fused_attention` — PASSED (5)

## Outcome

- Verdict: 🟢 APPROVE — matcher is sound, conservative, no misfires
- Non-blocking follow-up: document/test rank-2 FusedAttention equivalence (semantics-preserving)
