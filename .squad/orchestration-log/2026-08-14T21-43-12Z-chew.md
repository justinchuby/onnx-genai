# Orchestration Log — chew

**Timestamp:** 2026-08-14T21:43:12Z
**Agent:** Chew (🔬 Numerics & Precision reviewer)
**Task:** Numerics correctness review of PR #960 (Marlin int4 M>1 GEMM)
**Requested by:** Coordinator (Justin @justinchuby)
**Mode:** review (read-only)

## Summary

Numerics-only review of Deckard's Marlin fp16×int4 tensor-core GEMM (Gaff owns quality/portability).
**Verdict: 🟡 APPROVE-WITH-NOTES — numerically sound, ship; no correctness bug; three non-blocking notes.**

## Findings

- **int4 dequant + relayout/repack CORRECT:** host `repack_int4_weights` == device repack (byte-identical,
  all group sizes); per-lane byte layout matches the `mma.sync.m16n8k16` B-fragment distribution exactly;
  asymmetric zp indexing matches existing int4 tiled kernels; **scale-after-accumulate** is exact factoring
  (scale/zp constant across K within a group); padding columns cannot leak into valid outputs.
- **split-K fp32 partials SOUND:** disjoint contiguous group slices fully cover the partials buffer; fixed
  reduction order ⇒ deterministic/capture-stable; only re-associates cross-group fp32 sums (benign).
- **lm_head cached dense-GEMM plan CLAIM HOLDS:** cuBLASLt heuristic is deterministic for a fixed
  shape/dtype/device/workspace ⇒ cached algo == dynamic algo; caching changes only *when* selection happens.
- **Empirical:** ran the in-crate GPU parity suite on H200 → **11/11 pass** (incl. f64-oracle parity,
  split-K parity + determinism, repack roundtrip all group sizes).
- **#961 tolerance:** appropriately tight (~8× fp16-ULP headroom) — engineering-justified, concur.

## Notes (non-blocking)

- **N1:** "byte-identical greedy tokens" is a soft argmax-stability guarantee (empirical, 2 models × 24
  tokens), NOT a numeric invariant — keep the flag opt-in; add prompt/model coverage before any default flip.
- **N2:** hard Marlin launch errors are silently swallowed into the tiled fallback — log/count so a real
  kernel fault can't hide behind the slow path.
- **N3:** Marlin assumes nibble-packed int zero-points (matches every existing int4 tiled kernel — not a
  regression; guard dispatch if float-zp int4 is ever added).

## Outcome

🟡 APPROVE-WITH-NOTES recorded; drop `chew-marlin-numerics-review.md` merged into decisions.md and deleted.
