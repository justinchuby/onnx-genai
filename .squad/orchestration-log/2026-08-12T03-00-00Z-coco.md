# Orchestration Log — Coco

**Timestamp:** 2026-08-12T03:00:00Z
**Wave:** Rejection-response wave
**PR:** microsoft/onnxruntime #31993
**Model:** Opus
**Head after task:** `02a9f34`

## Task

Fix NaN blocker: hardware NEON `FCVTL`/`FCVTN` quiet signalling NaNs while MLAS software reference does not, so bit-exact `SpecialValues` test would fail on Apple Silicon.

## Changes

1. **NaN assertion strategy:** `isnan` + sign match + payload equality modulo quiet bit (bit 22 f32 / bit 9 f16). Non-NaN inputs: raw-bit equality preserved.
2. **RNE tie input:** Corrected from 1 + 2⁻¹⁰ (exactly representable in fp16) to 1 + 2⁻¹¹ (genuine RNE tie between 0x3C00 and 0x3C01).
3. **Removed `-march=armv8.2-a+fp16`:** `vcvt_f32_f16`/`vcvt_f16_f32` guarded by `#if (__ARM_FP & 2)` (AArch64 baseline), not `__ARM_FEATURE_FP16_VECTOR_ARITHMETIC`. +fp16 is for fp16 arithmetic only.
4. **macOS gate:** `TARGET_OS_OSX` in C++; `CMAKE_SYSTEM_NAME STREQUAL "Darwin"` in CMake.

## Lockout

Luba/Holden/Freysa/Mariette locked out of this review. Coco did this fix.
