# Bryant — QKV-bias MatMulNBits fusion

- **Agent:** Bryant (opus)
- **Why:** Add always-on `CpuMatMulNBitsBiasFusion` to fold `Add(MatMulNBits, [N]-bias)` into the GEMV bias epilogue, eliminating the standalone QKV-bias Add (6.5% to 0 of 7B node execution) while retaining a byte-identical opener and adding five fusion guards.
- **Mode:** background
- **Files:** `crates/onnx-runtime-ep-cpu/src/optimizer.rs`, `crates/onnx-runtime-ep-cpu/src/lib.rs`
- **Review:** Gaff (opus) — APPROVE
- **Merged:** `28adcd9`
