# Deckard (Systems) — Round 2

**Timestamp:** 2026-08-10T21:09:11Z  
**Branch:** squad/ep-plugin-export  
**Routed by:** Coordinator  
**Why:** Shape inference too thin; unsafe `SameAsInput(0)` default caused wrong output shapes

## Outcome

22 inference rules implemented in `ShapeInference` enum. Fail-closed `Declined` replaces silent `SameAsInput(0)`. `for_node` extracts attributes from ORT IR during Compile. `SubgraphRouting` added for multi-node fused subgraphs. 66 tests pass. Required Nabil changes documented.
