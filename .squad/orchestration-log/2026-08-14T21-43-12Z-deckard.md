# Orchestration Log — deckard

**Timestamp:** 2026-08-14T21:43:12Z
**Agent:** Deckard (⚙️ CUDA / decode performance engineer)
**Task:** Build Marlin fp16×int4 tensor-core GEMM (Lever A) — the funded PRIMARY decode lever + the single condition of the #957 spec-capture CONDITIONAL-GO
**Requested by:** Coordinator (Justin @justinchuby)
**Mode:** background (multi-stage, updates 1→10)

## Summary

Built + shipped a from-scratch SM80 `mma.sync.m16n8k16` fused fp16×int4 tensor-core GEMM and wired it
through the `MatMulNBits` M>1 path, closing the M=8 speculative-verify capture cliff. **Outcome: Lever A
LANDED & MERGED (#960, 7774ec5b); the #957 spec-capture CONDITIONAL-GO is now GO in practice for glm-4-9b.**

## Deliverables / Findings

- **Kernel:** new module `crates/onnx-runtime-ep-cuda/src/kernels/marlin_gemm.rs` (adapt-not-vendor:
  ONNX N-major nibble packing, asymmetric nibble zero-points, groups 16/32/64/128; per-group scale applied
  AFTER the tensor-core accumulate; raw inline `mma.sync` PTX; no upstream source ⇒ no LICENSE vendoring).
- **Wiring:** `MatMulNBits` M>1 (plain + rmsnorm-prologue + gate_up SwiGLU fused) + split-K + GQA/SkipLN
  M>1 capture-safety valves + lm_head cached dense-GEMM capture plan.
- **Portability (Rule 11):** opt-in `ONNX_GENAI_MARLIN_M_GT_1` (default OFF, SM80 arch guard, byte-identical
  tiled fallback on <SM80/CPU/ineligible/launch-error); split-K default-ON (`ONNX_GENAI_MARLIN_SPLITK=0`).
- **Measured (glm-4-9b canonical gate, H200 GPU7):** M=8 verify graph **41 fragmented segments / B\*=8.76×
  (hard NO-GO) → SINGLE whole-graph capture, ZERO unsupported nodes, B\*=2.16×** (arc 8.76→4.99→2.71→2.63→
  2.16×), byte-identical greedy tokens throughout, prefill ~2×.
- **Attribution correction (update-10 fixes update-8):** fused gate_up split-K requires `block_size==32`;
  glm is block-128 so its fused gate_up node never forms — glm's 2.63→2.16× was ENTIRELY the `4abe4e57`
  general small-M split-K retune, NOT gate_up fusion. qwen (block-32) fires the fused node; capture fully
  solved but B\*≈4.7× is an honest denominator effect, a drafting-depth follow-up not a kernel bug.

## Outcome

Merged PR #960 (7774ec5b). Reviews: Chew 🟡 APPROVE-WITH-NOTES (numerics), Gaff 🟢 APPROVE (quality). Open
follow-ups (non-blocking): keep flag opt-in until broader parity coverage; add fallback logging/counter
(Chew N2) before any default flip; qwen B\* is a drafting-depth story. Drop `deckard-marlin-kernel.md`
merged into decisions.md and deleted.
