# Orchestration Log — batty-18

**Timestamp:** 2026-07-14T14:50:00Z
**Agent:** batty-18 (opus/high)
**Role:** ORT2 optimizer/kernels engineer
**Task:** AttentionFusion — fuse SDPA core into com.microsoft::FusedAttention

## Summary

Implemented the final Phase-2 optimizer sub-item: fused the scaled-dot-product-attention core (`MatMul(Q,Kᵀ)·scale[+mask]→Softmax(axis=-1)→MatMul(·,V)`) into a new `com.microsoft::FusedAttention` node with a CPU kernel and shape rule. Also folded in Roy's advisory from the FusedGemm cycle (permanent expanding-bias FusedGemm decline test).

## Work product

- **NEW** `crates/onnx-runtime-ep-cpu/src/kernels/fused_attention.rs` — batched SDPA kernel + Factory + 5 unit tests
- **NEW** `crates/onnx-runtime-session/tests/fused_attention_parity.rs` — synthetic e2e parity (masked + unmasked)
- `crates/onnx-runtime-ep-cpu/src/kernels/softmax.rs` — `softmax_slices` made `pub(crate)`
- `crates/onnx-runtime-ep-cpu/src/kernels/mod.rs` — registered `("FusedAttention","com.microsoft",1)`
- `crates/onnx-runtime-ep-cpu/src/provider.rs` — FusedAttention added to contrib-domain support test
- `crates/onnx-runtime-shape-inference/src/handlers/linalg.rs` — FusedAttention shape rule
- `crates/onnx-runtime-shape-inference/tests/op_rules.rs` — 3 shape tests
- `crates/onnx-runtime-optimizer/src/fusion.rs` — `RewriteKind::Attention`, full matcher, + Roy's folded FusedGemm decline test
- `crates/onnx-runtime-session/tests/bert_toy_optimized_parity.rs` — asserts 5 FusedAttention + 0 surviving Softmax

## Outcome

- Commit: `39a23c8` → cherry-picked to main as `64edd75`
- Reviewers: Roy (roy-20) 🟢 + Chew (chew-26) 🟢
- bert_toy: 5 FusedAttention, 0 Softmax, LayerNorm=12, FusedMatMulBias=32 unaffected
- Parity: vs reference 1.043e-7 (bound 2e-3); vs opt-off 1.416e-7 (DRIFT_ATOL 1e-5)
- **Phase-2 optimizer OpFusion + AttentionFusion sub-items COMPLETE**
- Fusion set: ConstantFolding, DeadNodeElimination, OpFusion [LayerNorm + FusedMatMulBias + FusedGemm + FusedAttention], AttentionFusion
