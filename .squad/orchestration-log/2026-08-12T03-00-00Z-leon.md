# Orchestration Log — Leon

**Timestamp:** 2026-08-12T03:00:00Z
**Wave:** Rejection-response wave
**PR:** microsoft/onnxruntime #32001 (`nxrt/mlas-apple-framework-option`)
**Model:** Opus
**Head after task:** `0d924a421b`

## Task

Fix all rejected items on PR #32001 (Apple Accelerate infrastructure):
- B1: arm64 detection keyed off `onnxruntime_target_platform` (empty when `CMAKE_OSX_ARCHITECTURES` defined-but-empty on Apple Silicon)
- B2: PR body inaccurate (FATAL_ERROR, iOS, universal2, x86_64 — all contradicted by code)
- N1: Darwin-only gate using `APPLE` instead of `CMAKE_SYSTEM_NAME STREQUAL "Darwin"`
- N2: `MLAS_USE_APPLE_ACCELERATE=1` observable contract removed by earlier reviewer
- N3: Split loud failure (CLI `BuildError`) from tolerant CMake (warn-and-disable)
- N4: `--use_apple_accelerate` in wrong argument group

## Outcome

All items fixed in commit `0d924a421b`.

- **B1:** Now reads `CMAKE_OSX_ARCHITECTURES` (single value) for `arm64`/`arm64e` first; falls back to `CMAKE_SYSTEM_PROCESSOR`. Handles defined-but-empty. Coordinator proved with 5-case harness.
- **B2:** PR body rewritten to match code reality.
- **N1:** `CMAKE_SYSTEM_NAME STREQUAL "Darwin"` — excludes iOS/tvOS/visionOS.
- **N2:** `MLAS_USE_APPLE_ACCELERATE=1` reinstated as `target_compile_definitions` on `onnxruntime_mlas`.
- **N3:** `build.py` raises `BuildError` for non-macOS explicit opt-in; CMake warns-and-disables.
- **N4:** Flag in CPU Execution Provider argument group alongside `--no_sve`, `--enable_arm_neon_nchwc`.

## Challenger false positive

Challenger (re-review) claimed `add_argument` was missing — would crash CLI. Disproved: `python3 tools/ci_build/build.py --help` exits 0 with flag listed. False positive. N4 already done.

## Lockout

Luba (author) and Isidore (earlier lockout revision) and Coco (same PR) locked out. Leon did this fix.
