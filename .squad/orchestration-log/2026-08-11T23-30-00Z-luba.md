# Luba — Orchestration Log 2026-08-11T23-30-00Z

**Wave:** Upstream CI correction
**Model:** Opus
**PRs:** microsoft/onnxruntime #31973, #31974

## Task

Pull real CI logs for all Apple/arm64 failures on PRs #31973 and #31974.

## Findings

All failures occur **before compilation** (infra flakes):

### PR #31973 — coreml / build-and-test (arm64, x86_64, Release)
- `cpuinfo` archive download failure (GitHub CDN timeout).
- Same job passed on control PR #31985 at same time — confirms transient network issue.

### PR #31974 — iphone_simulator (arm64)
- `XNNPACK` archive download failure (FetchContent).
- Same job passed on #31985 — infra flake.

### PR #31974 — Build Linux arm64 Debug / build_test_pipeline
- Job timeout at step 1453/1459 (linking `onnxruntime_mlas_test`).
- No `##[error]` annotation, no compile or link error — runner hit wall-clock job limit.
- Passed on #31985 — infra flake (slow runner).

## Actions

No code changes needed. Recommended `gh run rerun` where possible (refused for fork-PR jobs — only retrigger available is a push).

## Outcome

All three failures confirmed infra flakes. No action on code required.
