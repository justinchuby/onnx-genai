# Orchestration Log — Mariette

**Timestamp:** 2026-08-12T03:00:00Z
**Wave:** Rejection-response wave
**PR:** microsoft/onnxruntime #31988 (`nxrt/cuda-matmulnbits-sm-cols`)
**Model:** Opus
**Head after task:** `dc1e173e4b`

## Task

Fix B1/B2/B3 blockers on MatMulNBits cols-per-CTA PR.

## Changes

- **B1 (Admission):** Shared-memory gate now always uses `kColsPerThreadBlock`=8 (not `cols_per_block`). The selected 2/4/8 value only affects launch grid. Acceptance-set regression proves new == upstream across 20,800 combinations.
- **B2 (Occupancy):** Replaced `kTargetCtasPerSm=12` with `cudaOccupancyMaxActiveBlocksPerMultiprocessor` per instantiation. Falls back to `kFallbackWavesPerSm=8` if API fails.
- **B3 (Tests):** `AcceptanceSetMatchesUpstream`, `OnlyMod8Accepted`, `WideN_Cols8Coverage`, `ForcingHook_AllValues`, `NumericParity_SKIP`, `OccupancyModel_SKIP`.

## Status

**Parked pending GPU access.** Occupancy model cannot be validated without hardware. Recommendation: park until CC 8.6/8.9 consumer GPU and CC 8.0/9.0 datacenter GPU available.

## Lockout

Cohaagen/Sebastian/Chew/Deckard/Leon/Batty locked out. Mariette did this fix.
