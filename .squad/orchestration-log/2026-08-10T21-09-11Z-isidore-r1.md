# Isidore (Bindings) — Round 1

**Timestamp:** 2026-08-10T21:09:11Z  
**Branch:** squad/ep-plugin-export  
**Routed by:** Coordinator  
**Why:** N3 (macro export guards) reassigned after Nabil locked out of lib.rs

## Outcome

Both `CreateEpFactories` and `ReleaseEpFactory` macro-generated entry points wrapped in `catch_unwind(AssertUnwindSafe(...))`. `ReleaseEpFactory` return type corrected to `void`. Macro hygiene: all paths fully qualified, `$crate`-safe. `cargo build -p onnx-runtime-ep-cpu-plugin` passes cleanly.
