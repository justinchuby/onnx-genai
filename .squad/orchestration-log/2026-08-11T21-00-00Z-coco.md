# Orchestration Log — Coco

**Timestamp:** 2026-08-11T21:00:00Z
**Wave:** PR #762 — B1/B2 correction
**Model:** Claude Opus
**Requested by:** @justinchuby

## Task
Fix Challenger's B1 (forgeable sentinel) and B2 (filter_map rank destruction).

## Commits
- `38625fb38` — absent_outputs: HashSet<ValueId>; Vec<Option<usize>> rank preservation; build_conv fails closed

## Outcome
B1: Replaced string sentinel with out-of-band `absent_outputs: HashSet<ValueId>`. ValueIds are arena indices assigned by reader; model content cannot influence them.
B2: filter_map → map → Vec<Option<usize>> at claim time; unwrap_or(0) with DIM_UNKNOWN constant for get_kernel trait; build_conv fails closed on None dims.
added conformance_mixed_partition compiled-node counter via dlsym as best effort under ORT 1.27 API constraints.
