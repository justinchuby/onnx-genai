# Marsten — GLM-4-9b native decode smoke

- Smoked real GLM-4-9b int4 native decode on baseline `569507c` and fix `1fe314f`.
- Output was coherent and byte-identical across baseline/fix; throughput ~101→102 tok/s (+1.24%, noise).
- CUDA graph active: captures=1, replays=61, fallbacks=0.
- GLM-4-9b dense native support is confirmed working. ORT-genai could not load the export due to missing `genai_config.json`, so this is native-only evidence.
