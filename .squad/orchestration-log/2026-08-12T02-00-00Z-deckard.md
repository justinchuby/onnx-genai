# Orchestration log — Deckard — 2026-08-12T02:00:00Z

**Wave:** Apple framework infrastructure + TensorRT fix (branch `squad/ep-plugin-parity-cuda`)
**PR:** microsoft/onnxruntime#31988 (CI triage)

## Task
Diagnose CUDA and TensorRT Linux x64 Release build failures on PR #31988.

## Findings
- **Strict-aliasing and unused-parameter `-Werror` errors** in `matmul_4bits_common.cuh` under
  CUDA 13.0. Fixed with `memcpy`-based type punning (strict-aliasing) and `(void)param;` casts.
- **TensorRT `blockIdx` errors** — initial assessment: CUDA-13 base-codebase issues (not ours).
  **This was disproved.** Leon's cross-PR comparison showed #31678 green / #31988 red, proving
  the TensorRT break was introduced by our change.

## Artefacts produced
- `.squad/decisions/inbox/deckard-31988-build-fix.md` (merged this session; contains strict-aliasing fix record)
