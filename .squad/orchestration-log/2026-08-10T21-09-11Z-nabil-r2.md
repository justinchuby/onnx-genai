# Nabil (ORT Plugin EP Eng) — Round 2

**Timestamp:** 2026-08-10T21:09:11Z  
**Branch:** squad/ep-plugin-export  
**Routed by:** Coordinator  
**Why:** `GetSupportedDevices` returned 0 devices → ORT segfault

## Outcome

`CreateEpDevice` implemented. ORT now registers and runs our EP. Root fixes: (1) proper CPU device filtering via `HardwareDevice_Type`, `CreateEpDevice`, `EpDevice_AddAllocatorInfo`; (2) ALL 20 vtable slots populated (ORT calls them without null-checking); (3) `CreateAllocator` returns ORT's default allocator. Full chain verified: Register → GetEpDevices → CreateSession → Run → correct output [6,8,10,12]. Tests `ort_register_ep_library` and `ort_loads_our_ep_and_runs_model` pass.
