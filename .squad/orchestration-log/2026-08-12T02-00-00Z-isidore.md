# Orchestration log — Isidore — 2026-08-12T02:00:00Z

**Wave:** Apple framework infrastructure (branch `squad/ep-plugin-parity-cuda`)
**Model:** Claude Opus
**PR:** microsoft/onnxruntime#32001 (lockout revision)
**Locked-out agents:** Luba (author), Luv (reviewer)

## Task
Fix all three substantive review issues (S1/S2/S3) in PR #32001 under reviewer lockout.

## Fixes

| Item | Fix |
|------|-----|
| S1 | Replaced `FATAL_ERROR` with `message(WARNING ...) + set(onnxruntime_USE_APPLE_ACCELERATE OFF)`. Matches `onnxruntime_USE_SVE`/`onnxruntime_USE_KLEIDIAI` idiom (cmake/CMakeLists.txt:581, 611). |
| S2 | Added `--use_apple_accelerate` to `build_args.py` and forwarding in `build.py` as `-Donnxruntime_USE_APPLE_ACCELERATE=ON`. |
| S3 | Removed `target_compile_definitions(onnxruntime_mlas PRIVATE MLAS_USE_APPLE_ACCELERATE=1)` — no consumer exists yet. |

## Outcome
Head: `d16a108252`. PR remains draft. All three fixes verified on Linux x86-64.
Apple CI is the only way to validate `find_library(Accelerate)` resolution and actual
framework linking.

Also triaged `iOS_CI_on_Mac` failure on PRs #31993 and #31988 — INFRA FLAKE (SSL cert
failure downloading FXdiv; no code change needed).

## Artefacts produced
- `.squad/decisions/inbox/isidore-32001-fixes.md` (merged this session)
- `.squad/decisions/inbox/isidore-objc-static-analysis.md` (merged this session)
