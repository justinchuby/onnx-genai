# Orchestration Log — Isidore

**Timestamp:** 2026-08-12T08:00:00Z  
**Issue/PR:** #762  
**Model:** Opus  
**Branch:** squad/ep-plugin-parity-cuda  
**Head:** 3826e1127

## Task

CUDA registry version bounds and nxrt struct_size ABI hardening.

## Outcomes

- CUDA registry `end_version` changed from `99` to `i32::MAX` with per-family justification (standard ONNX ops have schema changes absorbed by IR; custom/contrib ops have no upstream evolution).
- `memoffset_of_create_ep()` replaced with `std::mem::offset_of!(NxrtEpFactoryVtable, create_ep)` — authoritative regardless of padding/alignment changes.
- New release guard in `NxrtExecutionProvider::drop`: validates `struct_size` covers `release`+`ctx` before calling; undersized → deliberate leak (not UB, not arbitrary code execution).
- `undersized_factory_vtable_skips_release` test: proves guard prevents release; proves unguarded path would call it. Non-vacuous (both arms exercise the atomic flag).
- Tests: 283 passed, 0 failed across 5 EP crates.

## Status

✅ Complete. Gaff delta review: CUDA i32::MAX safe for current op list; vtable test non-vacuous; struct_size guard genuinely closed.
