# Orchestration Log — batty-19

**Timestamp:** 2026-07-14T14:50:00Z
**Agent:** Batty (batty-19, claude-opus)
**Task:** GELU (Erf-based) fusion — fuse GELU Erf decomposition into `com.microsoft::Gelu`
**Requested by:** Coordinator (Justin Chu)

## Summary

Implemented the GELU fusion pass completing Phase-2 OpFusion on bert_toy.

## Deliverables

- New `RewriteKind::Gelu` DAG matcher in `optimizer/src/fusion.rs` — matches the diamond `Mul(X,0.5)` × `(1+Erf(Div(X,√2)))` with strict constant guards (±1e-6) and diamond closure (same `ValueId` for X on both branches).
- New CPU kernel `ep-cpu/src/kernels/gelu.rs` reusing the `erf` helper (made `pub(crate)`); registered `("Gelu","com.microsoft",1)`.
- Shape-inference: no change needed (Gelu is shape-preserving unary, already covered).
- Decline guards: FastGelu/tanh out of scope; exact constant checks; diamond closure enforced.

## Results

- bert_toy: 6 Gelu fused, 0 Erf surviving; LayerNorm=12 / FusedMatMulBias=32 / FusedAttention=5 unaffected.
- Parity: fused-vs-unfused 3.353e-8; fused-vs-hand 0.0; bert_toy vs-reference 1.192e-7 (bound 2e-3); drift 1.416e-7 (DRIFT_ATOL 1e-5). Tolerances NOT loosened.
- Tests: optimizer 46, ep-cpu 115, session parity all green.

## Commit

`8e8d806` "Fuse exact-GELU Erf decomposition into com.microsoft::Gelu"

## Review

Roy (roy-21) 🟢 GREEN. Advisory (Roy A2, pre-existing): `fusion.rs:101` inert `#[derive(Clone, Debug)]` glued into doc-comment (introduced by `e9bf155`); harmless, fix separately.
