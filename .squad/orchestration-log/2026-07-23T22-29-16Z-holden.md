# Holden — Fresh review of DeepSeek dtod fix

- Verdict: 🟢 GREEN, merge-ready.
- Verified cudarc `new_stream` uses `CU_STREAM_NON_BLOCKING`, making the default-stream D2D copy unordered without the fix.
- Confirmed regression fails without fix (`got -999`) and passes with it; CUDA gate `202/0`, clippy clean.
- Perf/capture check: Qwen 899→902 tok/s, Phi 322→321 tok/s noise; decode captured with zero fallbacks, so `dtod` is off replay path.
- Qwen argmax remained `12095` (` Paris`).
