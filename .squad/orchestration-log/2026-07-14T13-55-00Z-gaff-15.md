# Orchestration Log — gaff-15

**Timestamp:** 2026-07-14T13:55:00Z
**Agent:** gaff-15 (claude-opus)
**Phase:** ORT2 Phase-2

## Task
Review the external-data path-traversal guard in `weights.rs` (§19.2), authored by deckard-21.

## Outcome
**REVIEW COMPLETE** — verdict 🟡 YELLOW (approved, 3 non-blocking advisories). Target commit `340d7b0`; merged to origin/main as `ba3f67a`.

## Summary
Full security review of the loader traversal guard:
- Audited all untrusted-path-to-open/mmap sites in the loader (exactly 2: `weights.rs:131` and `epcontext.rs:241`); both now guarded — PASS.
- Verified lexical correctness (throwaway probe: rejected absolute + `..` forms; allowed legit relative paths) — PASS.
- Error handling / TOCTOU: reject-before-open, no partial side effects — PASS.
- capi `map_loader_error` wildcard catch: new variant handled, build clean — PASS.
- Test quality: variant-specific assertions + byte-real positive tests — PASS.

## Advisories (non-blocking)
1. Lexical-only guard: symlinks not resolved (parity with epcontext; accepted).
2. capi: explicit `ExternalDataPath → InvalidGraph` arm would be more self-documenting.
3. DRY: `resolve_external_path` is near-verbatim in `weights.rs` and `epcontext.rs`; candidate for a shared `path_safety` helper.
