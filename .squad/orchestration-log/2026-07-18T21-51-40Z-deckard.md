# Deckard orchestration

- **Agent:** Deckard (sync, gpt-5.6-terra)
- **Task:** Reserve the `onnx-genai` PyPI name with a minimal pure-Python placeholder and dispatch-only sdist publish job.
- **Result:** Added `python/onnx-genai/` version `0.0.0` with import name `onnx_genai`; added `publish-onnx-genai-sdist` to `.github/workflows/publish.yml` using the `pypi` Trusted Publishing environment.
- **Commit:** `e9618e3`, landed to main (`493c213..e9618e3`).
- **Publish:** Coordinator dispatch with `publish_onnx_genai=true` succeeded as run `29662713813`; `onnx-genai` 0.0.0 is live on PyPI.
- **Decision note:** `.squad/decisions/inbox/deckard-onnx-genai-pypi.md`.
