# Orchestration log — Leon — 2026-08-12T02:00:00Z

**Wave:** Apple framework infrastructure + TensorRT fix (branch `squad/ep-plugin-parity-cuda`)
**Model:** Claude Opus
**PR:** microsoft/onnxruntime#31988

## Task
Clear the TensorRT build blocker on PR #31988.

## Diagnostic
`dispatch_adjacent_difference.cuh:58:41: error: 'blockIdx' was not declared in this scope`
plus ~40 similar. Include chain: `matmul_nbits_cols_per_block_test.cc` (host `.cc`)
→ `matmul_4bits_common.cuh` → `<cuda_bf16.h>` → CUB device headers. Host compiler
cannot resolve CUDA device intrinsics.

## Verdict: OURS
Cross-PR comparison: PR #31678 (unrelated) has TensorRT green; PR #31988 red.
Disproved Deckard's earlier assumption that these were CUDA-13 base-codebase issues.

## Fix
Extracted `SelectColsPerBlock`, `kColsPerThreadBlock`, and `kTargetCtasPerSm` into
`matmul_4bits_cols_per_block.h` — host-only, no CUDA device includes. Test now includes
only this header; `.cuh` re-exports via `#include`. Invariants preserved (no routing/
output/split-K changes).

## Outcome
Head: `34fe91e8dd`. TensorRT blocker cleared.

## Artefacts produced
- `matmul_4bits_cols_per_block.h` (new host-only header)
- `.squad/decisions/inbox/leon-31988-tensorrt.md` (merged this session)
