# Orchestration log — Batty

**Timestamp:** 2026-07-30T12:45:00-07:00
**Branch:** `qwen3-perf-followups`
**Requested by:** Justin Chu

Batty owned the interactive REPL renderer follow-up in `crates/onnx-genai-cli` (`live_turn.rs` and `output.rs`). Commits: `9cc5aea8`, `234d4db7`, `aed09009`.

## Result
- Fixed inline viewport scrollback overwrite by reserving rows with ratatui's actual wrapped-line count.
- Unified streaming and final reasoning classification with one `ReasoningStream`/`ReasoningMarkers::split` state machine so the post-`</think>` final answer is not dimmed.
- Changed live token handling so `LiveTurn::push` buffers tokens while drawing is coalesced to the refresh cadence plus final flush; 250 tokens now produce about 3 draws rather than one full frame per token.

## Durable facts
This Qwen3 chat template does not open `<think>` for the current live turn, so `opened_by_template=false` is required for the live turn. The default CLI build uses ORT for `--backend auto`; native requires `--features native-backend`.
