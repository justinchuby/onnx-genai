# Orchestration Log — Batty — 2026-08-12T06:00:00Z

**Issue:** #762 (EP plugin parity CUDA)
**Branch:** squad/ep-plugin-parity-cuda
**Commit:** b906ab2bb
**Mode:** Lockout revision (corrective wave completion)

## Summary

Completed the #762 corrective wave with six changes:

1. **EP graph assignment assertion** — Added `assert_ops_assigned_to_our_ep` to
   `add_skip_layer_norm_mul_routed` test in `optional_slots.rs`. Proves Add,
   SkipLayerNormalization, and Mul are assigned to `cpu_ep`. Both
   `record_ep_graph_assignment_info=1` and `disable_cpu_ep_fallback=1` set in setup.
   Nabil's stated reason for skipping ("not exposed in Rust bindings") was false —
   `OrtApi::Session_GetEpGraphAssignmentInfo` exists and was already used at
   `layernorm_dynamic_axis.rs:155`.

2. **Kernel registry `end_version`** — Was `end_version: since` (each op matched only
   its introduction opset). Fixed to `i32::MAX`, matching ORT's convention.

3. **Factory `struct_size` validation in loader** — Added bounds checks before reading
   `factory.name` and calling `factory.release`/`factory.ctx`. Mirrors `provider_adapter.rs`.

4. **`NXRT_REQUIRE_ORT_TESTS=1` fail-loud gate** — Tests panic instead of silently skip
   when ORT absent and env var set. Coordinator empirically verified: renaming one of 16
   `ort-prebuilt` dirs was a false negative; only renaming all 16 proved the gate fires.

5. **Initializer-backed MatMul fixture** — `matmul_initializer_weights` covers prepacking path.

6. **`.gitignore` negations** — Added 5 missing tracked-fixture entries.

## Outcome

278 passed / 0 failed (+1 from `conformance_matmul_initializer_weights`). Clippy + fmt clean.
