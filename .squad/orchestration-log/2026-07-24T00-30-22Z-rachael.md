# Rachael — DeepSeek MLA correctness and capture planning

- Shipped stream-ordered async `copy_reshape` (`24531c4`).
- Root-caused DeepSeek greedy-decode nondeterminism to in-place KV-aliasing RAW race in default-domain Attention `build_kv`.
- Fixed by staging aliased KV growth into disjoint scratch plus copy-back; added regression test; merged to main as `621936f`.
- Wrote MLA graph-capture enablement plan and began follow-up branch `perf/deepseek-mla-capture-enable`.
