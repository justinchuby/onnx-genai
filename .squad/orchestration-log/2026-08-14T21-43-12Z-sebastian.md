# Orchestration Log — sebastian

**Timestamp:** 2026-08-14T21:43:12Z
**Agent:** Sebastian (📊 runtime/server performance & benchmark analysis)
**Task:** Marlin int4 GEMM perf/capture harness + reproducible BEFORE/AFTER B* tables (gates #957 Lever A)
**Requested by:** Coordinator (Justin @justinchuby)
**Mode:** background (paired with Deckard's kernel iteration)

## Summary

Built the perf/capture harness that measured Deckard's Marlin kernel end-to-end and produced the
authoritative BEFORE→AFTER capture B\* arc. **Outcome: harness merged (#962, df6d3afb); confirmed the
glm-4-9b M=8 verify graph reaches a SINGLE whole-graph capture at B\*=2.16× — spec-capture GO in practice.**

## Deliverables / Findings

- **`marlin_bench`** (`crates/onnx-genai-bench/src/bin/marlin_bench.rs`, `--features bench-native,cuda`):
  times a real `decode_verify` over M∈{1,2,4,8,16} + prefill L∈{128,512,1024}; prints median/p10/p90/max +
  device `compute_cap` so every number is arch-attributable and contamination is visible.
- **Capture B\* arc (glm-4-9b, H200 GPU7):** segments **41→120→1** (whole-graph, zero seams); B\*
  **8.76→4.99 (Marlin) →2.71 (split-K) →2.63 (lm_head dense-plan) →2.16× (small-M retune)**; captured M=8
  verify wall stable ~21.9 ms; byte-identical greedy tokens throughout.
- **Prefill ~2×:** glm 218→426 tok/s @ L=1024, halving the glm-vs-ORT prefill gap 121×→62×.
- **Per-model:** glm (block-128) clean practical GO at 2.16×; qwen (block-32) capture fully solved
  (segments=1, zero unsupported) but B\*≈4.62–4.72× — an honest denominator effect (fast tuned block-32
  M=1 GEMV: capture speeds M=1 34% vs the M=8 verify 18%), not a coverage gap.
- **Flaky-test catch:** the qwen exact-token parity test is flaky ~25% because it asserts equality vs a
  nondeterministic tiled reference at a near-tie (tok19); Marlin's fixed split-K reduction is the
  DETERMINISTIC path — harden the assertion (tolerance / deterministic oracle), not a Marlin regression.

## Outcome

Merged PR #962 (df6d3afb) on base `94c69fdc`. Fed the corrected per-model attribution back to Deckard
(update-10). Drop `sebastian-marlin-bench.md` merged into decisions.md and deleted.
