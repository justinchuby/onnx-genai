# Orchestration Log — Iran

**Session:** 2026-08-11T12-05-00Z  
**Agent:** Iran (CPU/systems optimization)  
**Model:** Sonnet (background)

## Tasks

1. **B4 fail-closed gate** on PR #762: CUDA plugin to return zero factories.
2. **Deduplicate NarrowToFloat/FloatToNarrow** on upstream PR #31974 (commit `6dd19a6f56`).
3. **B5 stat precision fix** on upstream PR #31974: BFloat16/MLFloat16 `ComputeJob` stat narrowing.
4. **LayerNorm B1/B2 kernel fix** on upstream PR #31973: centered two-pass with double-sum.

## Outcomes

- **B4**: CUDA plugin fails closed in both `cuda`-on and `cuda`-off configs; `CanCopy` also returns false for device EPs. Four defects documented for future hardware-gated work.
- **Deduplication (commit `6dd19a6f56`)**: `onnxruntime/core/util/narrow_float_utils.h` created; `NarrowToFloat`/`FloatToNarrow` removed from `layer_norm_impl.cc` and `skip_layer_norm.cc`.
- **B5 fix**: `WriteStat<U>` called directly (U=float); `if constexpr` prevents `ComputeImpl<T,T>` instantiation for narrow types. 17/17 BFloat16 and 96/96 LayerNorm tests pass.
- **B1/B2 kernel fix**: Welford replaced with centered two-pass + double-precision first-pass sum. Worst-case error: 5.95e-03 (vs 28.2% for Welford). 14.3× faster than scalar baseline.
