# Orchestration — quaid-3

- Fixed the #676 oracle test in PR #692 by dropping the autoregressive engine and teacher-forcing on a fresh engine.
- Confirmed fresh-engine teacher forcing returns oracle token 33803; reused-engine teacher forcing returns 279 because attention KV is restored but Mamba conv/recurrent state is not.
- Flagged the underlying engine cache-reuse bug for follow-up as #695.
