# Orchestration Log — Coco

**Timestamp:** 2026-08-12T08:00:00Z  
**Issue/PR:** #762  
**Model:** Opus  
**Branch:** squad/ep-plugin-parity-cuda  
**Head:** d1421ac88

## Task

Close three test-integrity gaps in EP plugin scratch/routing.

## Outcomes

- `scratch_alloc_bytes` extracted as single public function at `compute.rs:601`; both production sites and all canary tests call it directly; former test copy deleted. Formula drift structurally impossible.
- `validate_write_dtype` wired into two new tests (absent and present TensorMut); documented as test-only contract helper (runtime enforcement requires raw-pointer Kernel trait restructuring).
- `build_subgraph_routing` returning `None` now fails at Compile via `fail_status` instead of deferring to Run. Dual-role slots rejected explicitly.
- Validation: 222 passed, 0 failed; clippy clean; fmt clean; Miri clean (173 lib tests).

## Status

✅ Complete. Gaff delta review: scratch_alloc_bytes and routing genuinely closed; validate_write_dtype flagged as substantive (dead in production) but not blocking.
