# Orchestration — Deckard-4 — 2026-08-15T05:27:00Z

Assigned/in flight: **GEMV-v2** on `squad/int4-gemv-wideload-v2`.

Context: #986 banked the first wide-load step for glm block-128, raising base decode to **192.4 tok/s** and native GEMV streaming to **1.40 TB/s**. ORT's comparable kernel still streams at **2.42 TB/s**, so the remaining base-vs-base gap is memory-level parallelism / weight streaming.

Objective: deepen the MLP pipeline enough to approach ORT streaming and beat ORT base decode (~250 tok/s), with byte-identical/portable/default-on requirements preserved and `ONNX_GENAI_GEMV_WIDELOAD=0` as the A/B fallback lineage.
