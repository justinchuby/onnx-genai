# Orchestration Log — leon-15

**Timestamp:** 2026-07-14T14:50:00Z
**Agent:** leon-15 (gpt-5.6-sol)
**Role:** ORT2 loader/capi engineer
**Task:** DRY external-path traversal guard + explicit capi mapping (Gaff advisories B/C)

## Summary

Refactored the near-verbatim `resolve_external_path` guards in `weights.rs` and `epcontext.rs` into a shared `guarded_join` helper in a new `crates/onnx-runtime-loader/src/pathsafe.rs` module. Each call site retains its distinct error variant (`LoaderError::ExternalDataPath` / `LoaderError::EpContextPath`). Explicitly mapped both variants to `OrtErrorCode::InvalidGraph` in the C API `map_loader_error` function.

## Work product

- **NEW** `crates/onnx-runtime-loader/src/pathsafe.rs` — `guarded_join(base, rel_path)` helper
- `crates/onnx-runtime-loader/src/weights.rs` — replaced local guard with `guarded_join`
- `crates/onnx-runtime-loader/src/epcontext.rs` — replaced local guard with `guarded_join`
- `crates/onnx-runtime-capi/src/lib.rs` — explicit `ExternalDataPath`/`EpContextPath` → `InvalidGraph` mapping

## Outcome

- Commit: `e60dd6b` → cherry-picked to main as `d6854c9`
- Reviewer: Deckard (deckard-22) 🟢 APPROVE
- All builds + tests + clippy green
- Closes Gaff advisories B and C from external-data path-traversal review
