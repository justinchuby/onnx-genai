# Orchestration Log — leon-17

**Timestamp:** 2026-07-14T14:50:00Z
**Agent:** Leon (leon-17, gpt-5.6-sol)
**Task:** Fix — break shape-inference-loader publish cycle (revision of deckard-23)
**Requested by:** Coordinator (Justin Chu); Deckard locked out per Reviewer Rejection Protocol

## Summary

Broke the `onnx-runtime-shape-inference` ↔ `onnx-runtime-loader` publication cycle that caused Roy (roy-22) to reject deckard-23's artifact.

## Fix

Made `onnx-runtime-shape-inference`'s dev-dependency on `onnx-runtime-loader` path-only (no `version` field). `cargo publish` omits `[dev-dependencies]` entries that have no version from the packaged manifest — so loader does not appear in the published shape-inference manifest.

## Verification

- Packaged shape-inference manifest: loader ABSENT from dev-deps section. ✅
- 75 shape-inference tests still pass, including `bert_toy_fully_resolves` (which exercises the loader-backed path). ✅
- Valid publish order confirmed: ir → shape-inference → loader → optimizer → ep-api → ep-cpu → session → capi. ✅
- `docs/CRATE_RESERVATION.md` updated with path-only rationale and corrected order. ✅
- IR publish dry-run succeeds. ✅

## Commit

`183a876` "fix(release): break shape-inference-loader publish cycle"

## Review

Roy (roy-23) 🟢 GREEN. Advisory: direct shape-inference packaging against crates.io will fail until IR is published first (expected — must follow documented sequence).
