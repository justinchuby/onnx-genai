# Deckard (Systems) — Round 1

**Timestamp:** 2026-08-10T21:09:11Z  
**Branch:** squad/ep-plugin-export  
**Routed by:** Coordinator  
**Why:** `OrtNodeComputeInfo::Compute` returned `ORT_NOT_IMPLEMENTED` — real compute path needed

## Outcome

Real `CreateState`/`Compute`/`ReleaseState` lifecycle implemented in `compute.rs`. `kernel_ctx.rs`: complete OrtKernelContext ↔ TensorView bridge. 14 unit tests added. `cargo check` + `clippy` clean for owned files.
