# Orchestration — Batty-3 — 2026-08-15T05:27:00Z

Assigned/in flight: **spec-decode W=9 contract fix** on `squad/spec-decode-w9-contract`.

Context: #988 fixed the captured-verify graph-slot crashes and Gaff approved capture stability, but Chew rejected correctness because qwen W=9 (`spec_tokens=8`) diverges from plain M=1 greedy at token[2] (**plain 9370 vs spec 2810**). Forcing row-0 fallback did not repair the divergence, so accepted DRAFT tokens are outside the current M=1 contract proof.

Objective: restore the binding contract **captured speculative output == plain M=1 greedy** for qwen W=9 before any additive spec-decode performance claim. Deckard is locked out of revising #988; Chew should re-review the contract fix.
