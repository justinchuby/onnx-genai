# Orchestration Log — deckard-23

**Timestamp:** 2026-07-14T14:50:00Z
**Agent:** Deckard (deckard-23, gpt-5.6-sol)
**Task:** Crate-name reservation prep — set `onnx-runtime-*` to `0.1.0-dev.0` and prepare publish runbook
**Requested by:** Coordinator (Justin Chu)

## Summary

Prepared the eight `onnx-runtime-*` crates for crates.io name reservation via a prerelease version.

## Deliverables

- Set all 8 `onnx-runtime-*` manifests to `version = "0.1.0-dev.0"`.
- Exact-pinned all inter-crate workspace deps to `=0.1.0-dev.0` (plain `^0.1.0` excludes prereleases).
- `[workspace.package] version` left at `0.1.0`; `onnx-genai` crates untouched and have no `onnx-runtime-*` deps.
- New runbook `docs/CRATE_RESERVATION.md` with token setup, index-propagation waits, and stable-release bump path.
- `Cargo.lock` updated for the 8 runtime crates.

## Commit

`8988abd` "chore(release): prepare nxrt crate name reservation"

## Review Outcome

Roy (roy-22) 🔴 RED — publish cycle: `docs/CRATE_RESERVATION.md` documented invalid order (shape-inference before loader, but shape-inference had a dev-dep on loader). Deckard locked out per Reviewer Rejection Protocol; revision assigned to Leon (leon-17).
