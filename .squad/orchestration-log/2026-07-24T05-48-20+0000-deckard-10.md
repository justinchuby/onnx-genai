# Deckard-10 — profile-native backend selection

- Added `--backend native|ort|auto` to `profile_native` for same-harness native-vs-ORT decode A/B testing.
- Initial change at `4c2849a1`; final merged implementation is `d03261c7` after header-compatibility and invalid-argument fixes.
- Native output header remains byte-identical; ORT and auto use a separate `backend=` line.
