# Orchestration Log — Mariette

**Timestamp:** 2026-08-12T02:30:00Z
**Agent:** Mariette (Apple-platform engineer)
**Model:** Opus
**Task:** Lockout revision on PR #31993 — rescope to macOS arm64 only
**Spawned by:** Coordinator (@justinchuby scope correction)

## Task

Revise PR #31993 (`nxrt/mlas-apple-f16-cast`) to remove Intel Mac / universal2 / iOS scope.

## Outcome

- Removed `#else` branch in `test_cast_fp16.cpp` that asserted null dispatch pointers on non-ARM64 Apple (x86_64 slice test, out of scope).
- Rescoped commit messages and PR body from universal2/iOS/Intel to macOS arm64 only.
- Compile-time gate `#if defined(__APPLE__) && defined(MLAS_TARGET_ARM64)` left unchanged — it is what prevents the kernel reaching targets without FEAT_FP16.
- Positive `ASSERT_NE(...Kernel, nullptr)` dispatch assertions survive — test remains non-vacuous.
- Head: `68ee0de`. PR remains draft.

## Lockout

Mariette (author) and original reviewers were barred from this revision per lockout protocol. Mariette self-revised under the lockout revision role (distinct from original authorship).

## Status

✅ Complete. PR #31993 remains draft pending GPU/Apple benchmarks.
