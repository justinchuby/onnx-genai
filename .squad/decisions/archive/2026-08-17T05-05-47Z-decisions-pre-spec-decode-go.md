# Decisions — live standing directives

Last consolidated: 2026-07-27T00:30:00Z (Scribe native-byte-identical-spec-decode FINAL VERDICT merge; local state; live decisions.md was 46,147 B before merge, 49,080 B after merge. Merged 3 inbox drops: luv-byteident-verify, wallace-spec-wire-e2e, wallace-to-luv-downproj-byteident-regression. Deleted processed drops, kept decisions/inbox/README.md. Archive gate: live file remains under 51,200 B, so no archive was performed.)

## 2026-07-27 — Native byte-identical spec-decode — FINAL VERDICT: NO-GO (full coverage)

**This supersedes/deduplicates the prior native byte-identical/spec-decode campaign headers.** The remaining lm_head/o_proj/down coverage gaps were closed and the whole-graph verdict is now definitive.

- **FINAL:** byte-identical greedy speculative decode is **NO-GO** on qwen2.5-14b int4. Best net remains **0.74×** across three independent E2E measurements; it is a slowdown, not a speed feature.
- **MECHANISM:** the M=W verify floor is ~37 ms (~4.5× M=1) and is **OCCUPANCY/latency-bound, NOT bandwidth-bound** (ncu down-proj DRAM 8.16%). Weights-once batched verify cannot reduce wall time; the per-row oracle is the fastest byte-identical path. Verify floor > mean_accept ~4 ⇒ structural loss.
- **COVERAGE:** all coverage gaps are closed: lm_head, o_proj, and down-proj now have batched paths. Luv's isolated full-coverage kernels are weights-once byte-identical (q/o 1.16×, o_proj 1.18×, down 1.21×, lm_head-rms 1.33×; bit-exact suite 26/26), confirming coverage is **not** the limiter.
- **MODEL FACT:** `qwen2.5-14b-instruct-int4-zp` is **SYMMETRIC** (337 MatMulNBits nodes, no zero_points — directory/name is a misnomer). Zero-point coverage is moot for this target, though still valuable later for glm-4-9b/asymmetric work.
- **REUSABLE ASSETS BANKED:** Wallace's fused `[1,1+W]` whole-graph captured verify driver and capture machinery are **DONE** and correct (0% divergence with per-row). The verify kernel is swappable via `ONNX_GENAI_SPEC_BATCHED_VERIFY`, so any future wall-reducing/higher-occupancy verify path plugs in with zero driver change.
- **ONLY REMAINING LEVERS:** (i) higher acceptance via an EAGLE-style trained draft head — mean_accept ~5–6 could clear the floor, but this is a PyTorch training/checkpoint effort outside kernel retune. `NativeProposerSession` already supports EAGLE-style shared-KV + `hidden_output` wiring; only the trained checkpoint is missing. (ii) non-byte-identical Marlin verify (~30 ms), still marginal at mean_accept ~4 and breaks bit-exactness.
- **WARNING:** branch `squad/spec-decode-integration` @ `c2895e10` and commit `8c8611a8` carry a down-proj byte-identity regression on the symmetric down path (95.8% divergence). **Do NOT open or merge a PR from them without restoring bit-exactness.**

Processed drops: Luv `luv-byteident-verify.md`; Wallace `wallace-spec-wire-e2e.md`, `wallace-to-luv-downproj-byteident-regression.md`.

Last consolidated: 2026-07-27T00:00:00Z (Scribe native-byte-identical-spec-decode Option 1 NO-GO merge; local state; live decisions.md was 42,959 B before merge. Charter archive gate is SIZE-based ≥50 KB, so no archive was performed despite the stale age-based prompt threshold; merged 5 inbox drops: luv-byteident-verify, sapper-drafter-feasibility, wallace-gqa-workspace-verify-bug, wallace-spec-wire-e2e, wallace-to-luv-zp-verify-coverage. Deleted processed drops, kept decisions/inbox/README.md.)

## 2026-07-27 — Native byte-identical speculative-decode (Option 1) — NO-GO

**Final campaign verdict: NO-GO for qwen2.5-14B byte-identical greedy speculative decode with a high-acceptance standalone drafter.** Sapper's feasibility spike was a CONDITIONAL GO only if the verify wall could be reduced in whole-forward E2E; Luv proved isolated bit-exact GEMV kernels can be faster, but Wallace's captured fused-driver measurements showed the composed forward still loses. Best real E2E was **0.73×** (1.5B draft, W=4); a fully accepting prompt still did not clear the net-win gate.

Durable conclusions:
- The verify wall is fundamental for 14B byte-identical greedy single-path spec decode: the M=W verify forward remains about **4.5–5×** an M=1 forward while realistic mean_accept is about **3–4** (best aggregate Wallace run 4.0, Sapper expectation ~3.0–4.2). Break-even would require verify ≲1.8× M=1, which this model does not provide.
- `qwen2.5-14b-instruct-int4-zp-onnx` is **symmetric**, not asymmetric-zp: 337 MatMulNBits nodes, no zero_points tensors. The `-zp-` directory name is a misnomer; zp coverage is harmless for truly asymmetric models but not the blocker here.
- Only q/o/lm_head-style GEMVs amortize weights-once in isolation (q/o about **1.18×**, lm_head about **1.75×**). The large-K MLP path (K/N≈13824 gate/up/down), attention, norm, rotary, residual, and some folded o/lm_head epilogues do not yield a whole-forward weights-once win; the byte-identical batched path was **41.1 ms**, slower than the per-row oracle at **37.3 ms**.
- Wallace completed the standalone draft wiring and the fused `[1,1+W]` captured verify driver. The verify kernel is swappable via `ONNX_GENAI_SPEC_BATCHED_VERIFY` / `ONNX_GENAI_SPEC_PERROW_VERIFY` / default Marlin selection, so any future kernel can be tested without driver changes; all paths remain opt-in/default-OFF.
- The remaining plausible lever is **higher acceptance from a trained drafter/head** (EAGLE/Medusa-style or another real high-acceptance draft model), which is outside this kernel-retune campaign. Prompt lookup or same-family 0.5B/1.5B standalone drafting does not clear the gate.
- Separate follow-up: captured verify can crash on longer prompts due to GQA workspace reservation growing past the capture-time bucket (`prepared 675840` vs `requires 696320` bytes). This is tracked as GitHub issue **#1084** in the memory/workspace-planner domain and was not fixed in this campaign.

Processed drops: Sapper `sapper-drafter-feasibility.md`; Luv `luv-byteident-verify.md`; Wallace `wallace-spec-wire-e2e.md`, `wallace-to-luv-zp-verify-coverage.md`, `wallace-gqa-workspace-verify-bug.md`.


Last consolidated: 2026-08-15T20:05:00Z (Scribe specdecode-evidence-stop + tyrell-attention-NO-GO merge; local state; live decisions.md was 45,422 B before merge, 52,491 B after merge → crossed the 50 KB SIZE gate, so ARCHIVED the superseded per-PR post-mortem bodies of the base-decode perf wave (#999/#1003/#996/#1004/#998/#1007, GEMV NO-GO drafts, combined-stack, post-wave measurement, attention-floor probes; 11,258 B) to `.squad/decisions-archive/2026-08.md`, leaving a pointer + the retained FLOOR-REACHED standing summary → live file now **41,982 B**, well under 50 KB. No standing conclusion lost (all retained in the FLOOR-REACHED summary + Spec-decode evidence-backed conclusion). Merged 5 inbox drops: tyrell-gqa-attention-diagnosis (NEW → "GQA decode-attention full-rewrite GO/NO-GO" subsection, resolves the big-ticket attention-rewrite item to NO-GO) + leon-spec-verify/batty-specdecode-w-workspace/deckard-specfix-graphslot/sebastian-specdecode-e2e (spec-decode thread — leon+batty were ALREADY fully represented from prior sessions; deckard+sebastian largely represented/superseded — all four consolidated into a new "Spec-decode: evidence-backed conclusion" subsection with the decisive break-even math). Deleted all 5 merged drops, kept decisions/inbox/README.md.) Merged 5 inbox drops: tyrell-gqa-attention-diagnosis (NEW → added "GQA decode-attention full-rewrite GO/NO-GO" subsection, resolves the big-ticket attention-rewrite item to NO-GO) + leon-spec-verify/batty-specdecode-w-workspace/deckard-specfix-graphslot/sebastian-specdecode-e2e (spec-decode thread — leon+batty were ALREADY fully represented from prior sessions; deckard+sebastian were largely represented/superseded — consolidated all four into a new "Spec-decode: evidence-backed conclusion" subsection capturing the decisive break-even math). Deleted all 5 merged drops, kept decisions/inbox/README.md. Archive gate [charter §1 SIZE-based, ≥50 KB]: see this consolidation's before/after byte report.)

Last consolidated: 2026-08-15T19:32:00Z (Scribe base-decode-ORT-parity merge; local state; live decisions.md was 48,366 B before merge. Merged 6 inbox drops (holden-device-token-loop [#1036 MERGED], rachael-review-1036 [APPROVE], bryant-ort-gap-rebaseline [PARITY re-baseline], roy-rmsnorm-resident [NO-GO], pris-rmsnorm-warpshuffle [#1038 MERGED], rachael-review-1038 [APPROVE]) into a new "2026-08-15 (evening) — base-decode reaches ORT parity" section; deleted the 6 merged drops, kept decisions/inbox/README.md; left in-flight tyrell drop untouched. Archive gate [charter §1 SIZE-based, ≥50 KB]: see this consolidation's before/after byte report.)

Last consolidated: 2026-08-15T16:28:00Z (Scribe multi-arch + interleave-dequant + PDL/device-token-loop merge; local state; live decisions.md was 37,288 B before merge — under the 50 KB charter gate, so NO archive [charter §1 is SIZE-based; the prompt's "≥20,480 B → archive older than 30 days" age rule is DECLINED per charter: every live entry is dated 2026-08-14/15 so an age filter no-ops, and the file is under the byte gate regardless]. Merged 4 inbox drops (coordinator-deepseek-status, coordinator-multiarch-strategy, deckard-interleave-dequant [#1019 + #1029], gaff-pdl-feasibility) into a new "2026-08-15 — multi-arch + interleave-dequant + PDL program" section; deleted the 4 merged drops, kept decisions/inbox/README.md.)

Prior consolidation: 2026-08-15T13:07:55Z (Scribe worktree-swept-inbox DEDUP merge; local state; live decisions.md was 35,163 B before merge — under the 50 KB charter gate, so NO archive [charter §1 is SIZE-based; the prompt's "≥51,200 B → archive older than 7 days" age rule is DECLINED per charter, and the file is under the byte gate regardless]. Coordinator swept 34 decision drops from all git worktrees into decisions/inbox/. DEDUP result: 33 of 34 were ALREADY represented in decisions.md or the archives (from prior spawn-manifest consolidation waves) and were deleted as stranded duplicates; only **1 was NEW** — batty-specdecode-w-workspace.md, whose primary-source root-cause of the W-dependent qwen spec divergence (cold GQA workspace leaked verify-width W into M=1 fallback; `native_workspace_query_rows` fix) was recorded nowhere. Merged as one concise bullet enriching the spec-decode closure section. Inbox now holds ONLY README.md. No primary-source detail was lost by the earlier manifest-based merges except this one Batty item.)

Prior consolidation: 2026-08-15T12:59:26Z (Scribe attention-floor campaign-close merge; local state; live decisions.md was 30,875 B before merge — under the 50 KB charter gate, so NO archive [charter §1 is SIZE-based; the prompt's "≥51,200 B → archive older than 7 days" age rule is DECLINED per charter, and the file is under the byte gate regardless]. Decisions inbox held ONLY README.md — the expected deckard-4 drops (deckard-gqa-group-reuse.md, deckard-gqa-cpasync-pipeline.md) were NEVER written, so 0 drops merged; campaign-close recorded directly from the spawn manifest. Added the two final attention-floor probes + the "INCREMENTAL BASE-DECODE PERF CAMPAIGN — FLOOR REACHED" summary.)

Prior consolidation: 2026-08-15T12:06:52Z (Scribe post-wave + spec-decode-close merge; local state; live decisions.md was 25,801 B before merge — under the 50 KB charter gate, so no archive was needed [charter §1 is SIZE-based; the prompt's "≥20,480 B → archive older than 30 days" age rule is DECLINED per charter: all entries are dated 2026-08-14/15 so an age filter no-ops]. Merged #1007 GQA-core (Holden+Rachael), the deckard-4 post-wave DEFINITIVE measurement, and the leon spec-decode HONEST CLOSURE; deleted 3 processed drops, keeping decisions/inbox/README.md.)

Prior consolidation: 2026-08-15T09:30:00Z (Scribe base-decode perf-wave merge; live decisions.md was 18,039 B before merge; merged #996/#999/#1003/#1004/#998 drops).

Prior consolidation: 2026-08-15T06:43:00Z (research-driven ORT-gap arc; merged #991/#992/#993 drops + GEMV-v2 findings).

Standing governance rules and active directives. Full narrative is archived; keep this file to current decisions plus durable rules.

## Ledger health rule

Archive by SIZE, not age. Age-only archiving can silently no-op during high-volume campaigns because most entries are recent. When the live ledger crosses the spawn-budget gate, preserve full history in an archive and keep `decisions.md` to standing directives, active decisions, and pointers. Assemble from inbox drops, dedupe, then delete merged drops; leave `decisions/inbox/README.md`.

## 2026-08-14/15 — glm-4-9b-int4 decode-vs-ORT program → ARCHIVED

The early per-PR narrative for this program (#978 split-K GEMV, #980 cp.async NO-GO, #981 block SkipSimplifiedLayerNorm, #986 128-bit wide-load, #991 lm_head cuBLASLt, #992 wide GEMV v2, #993 captured-spec re-review, early spec-decode drafts #984/#988, and the initial strategic directives) was moved to `.squad/decisions-archive/2026-08.md` on 2026-08-15T19:32:00Z (SIZE gate). It is fully superseded by the FLOOR-REACHED campaign close, the multi-arch/interleave-dequant program (#1019/#1029), and the 2026-08-15 ORT-parity re-baseline below. The standing directives it established (spec-decode is additive-only; CUDA-graph kernel tracing is the decode-mix authority) are retained in the standing-rules sections at the foot of this file.

## 2026-08-15 — base decode perf wave (4 PRs MERGED; #998 spec REJECTED)

Session merged record. Out-of-box glm base decode now **~207.6 tok/s short / ~172 KV2048** (default-on), opt-in fp16 stack ~232 short / 182 KV2048; honest ORT gap closed **1.30×→1.08×** (92.8% of ORT ~250 graph-on). Residual dominator: **GroupQueryAttention ~39% at KV2048** (the last lever), int4 GEMV ~35% (at its byte-identical wall), lm_head ~1% (solved).

### PR-by-PR bodies for this wave (#999/#1003/#996/#1004/#998/#1007, GEMV NO-GO drafts, combined-stack, post-wave measurement, attention-floor probes) → ARCHIVED

The detailed per-PR post-mortems for the base-decode perf wave were moved to `.squad/decisions-archive/2026-08.md` on 2026-08-15T20:05:00Z (SIZE gate). They are fully superseded by the FLOOR-REACHED campaign summary immediately below, which retains every standing conclusion (final standing table, exhausted-levers list, big-ticket items). #998 spec is further consolidated in the Spec-decode evidence-backed conclusion section.


### INCREMENTAL BASE-DECODE PERF CAMPAIGN — FLOOR REACHED (deckard arc close, 2026-08-15)

**The incremental base-decode perf campaign is CLOSED at its evidenced floor.** Both residual dominators (GroupQueryAttention, MatMulNBits) are at their walls; every remaining incremental lever has been measured negative with ncu/nsys evidence.

**HONEST FINAL STANDING (main `892876f5`, GPU1 idle H200):**

| config | short tok/s | KV2048 tok/s |
|---|---:|---:|
| **default out-of-box glm base** (no flags) | **210** | **186** |
| fp16 opt-in (`ONNX_GENAI_GEMV_FP16=1`) | 225 | 197 |
| ORT graph-OFF (reproducible) | 196 | 187 |
| ORT graph-ON (certified, NOT locally reproducible) | ~250 | — |

- Default native is **AHEAD** of reproducible ORT graph-off; fp16 opt-in further ahead.
- Still **1.11–1.19× BEHIND** certified ORT graph-on (~250) — **NOT locally reproducible**: local ORT rejects the glm graph-on due to the `rotary_embedding_dim` attribute.
- Residual dominators both at floor: **GroupQueryAttention ~43–45%**, **MatMulNBits ~32–33%**.

**EXHAUSTED LEVERS (full list, all NO-GO with evidence):**
- **GEMV ×3:** #1000 cp.async multicol prefetch (−4.2%); #1002 PORT-1 offline interleave (0 register savings); earlier #980 naive cp.async / depth-4 prefetch. → int4 M=1 GEMV is **OCCUPANCY-BOUND**, not prefetch/dequant-bound; 197.6 is the byte-identical fp32 ceiling.
- **Attention ×4:** bandwidth/shared-staging (no reuse); warp padding (12/16 regressed vs 8); group-head CTA reuse (regs 43→118, occ 45%→21.5%, e2e regressed); 2/3-stage cp.async KV-prefetch (latency hidden but regs/smem cost, e2e flat). → `gqa_decode` is **LATENCY/ISSUE-bound** at a register/smem/occupancy Pareto frontier.

**Big-ticket items remaining (both structural, both await Justin's direction):**
- Structural attention rewrite (wgmma/TMA, multi-week) → **NO-GO, see 2026-08-15 Tyrell diagnosis below.**
- DeepSeek MLA + `causal_conv1d` (needs model artifacts).

### 2026-08-15 — GQA decode-attention full-rewrite GO/NO-GO (Tyrell, perf-analysis, READ-ONLY) — **NO-GO**

Read-only ncu characterization of `gqa_decode_attention_f16` on glm-4-9b-int4, H200. **Verdict: do NOT fund a full attention-kernel rewrite.**
- **Kernel already IS ORT's algorithm.** Capture-safe split-KV **flash-decode + fixed-order combine** (structurally == ORT `flash_fwd_splitkv`(+combine)): coalesced `half2` loads (~0% excessive sectors), fp32 online-softmax accumulation, length-adaptive on-device split count, `exp2f`/`RCP_LN2`, `single_split_direct` fast path. **No tensor cores — correct, M=1 decode is a matrix-*vector*** (MMA has no rows to fill).
- **Memory-*latency* bound in every regime, DRAM 1–2% of peak** (not bandwidth/TC/coalescing). Context-dependent bottleneck: short/mid ctx (the common decode regime) = **MIO-throttle + short-scoreboard** (SFU `exp2f` + `__shfl_xor` butterfly + shared epilogue); long ctx = **long-scoreboard** (global K/V load latency). Occupancy register-capped (62.5% theo / 33–42% achieved) but latency- not occupancy-bound (raising occupancy regressed).
- **The two highest-ceiling structural levers are already measured NO-GOs** (Deckard lever-2 wave): cp.async K/V pipeline (issue eff up but +19 regs/+8KB smem → occupancy collapse, e2e flat) and GQA group-head K/V reuse (regs 43→118, occ 45%→21%, 172→162 tok/s). Occupancy/coalescing are non-levers. Only visible structural fat = the **serial merge/combine kernel** (upper-bound 15–40% of attention wall under ncu isolation), which ORT also pays and #1003 already tiled.
- **Ceiling ~+2% E2E** (attention is 11.9% of decode per Bryant; full close to ORT's 9.7% → +2.2%); realistic expectation **+0.5–1.5% with genuine net-zero probability** (matches Deckard's four consecutive NO-GOs on this kernel family).
- **Parity downgrade risk:** any rewrite reorders fp32 partials → results become near-equal, NOT byte-identical. Closing this converts attention from **byte-identical to tolerance-gated parity** (ULP/rel-error + token-agreement gate), a categorically weaker guarantee needing Leon/verify sign-off. Risk/reward does not clear.
- **If Justin insists:** authorize only a small time-boxed **merge/combine-pass probe** (the sole non-regressing angle) with a hard **≥+1% E2E graph-on kill-criterion** and a pre-agreed tolerance gate. Nothing larger.

### Speculative decode — CLOSED honestly as opt-in DETERMINISM mode, NOT a speed feature (leon, 4th attempt, PR #1006 DRAFT)

**Saga CLOSED.** After three prior 🔴 rejections (Sebastian/Deckard/Batty margin tweaks; Rachael #998 per-token loop), leon's 4th attempt delivered a decisive diagnosis + a byte-identity reference, but proved the **speedup is fundamentally unwinnable** for prompt-lookup drafting.

### Spec-decode: evidence-backed conclusion (consolidated 2026-08-15T20:05Z — leon/batty/deckard/sebastian threads)

**DECISIVE, evidence-backed STOP. Capture + byte-identity are SOLVED; the remaining gap is fundamentally acceptance-bound, not a kernel/GEMM problem.**
- **SOLVED — whole-graph capture:** the width-W verify captures **whole-graph (segments=1), fallbacks=0, zero capture-time device alloc** (opt-in `ONNX_GENAI_SPEC_BATCHED_VERIFY` / `ONNX_GENAI_SPEC_CAPTURED_VERIFY`, default OFF → zero prod risk).
- **SOLVED — byte-identity to plain M=1 greedy** by routing each verify row through the exact M=1 reduction family (correct reference is **Marlin-prefill-OFF greedy**; the divergence was a K-reduction *reassociation*, not a near-tie): **qwen 5/5** (W=5–9). **glm 3/5** — the 2 misses (W=6@111, W=9@157) are an **attention-path** reassociation (q_seq=W flash-attn vs q_seq=1 gqa_decode), NOT the GEMV; closing them needs an fp32 verify-attention, tractable but not built since speedup is blocked regardless. Batty's earlier W-dependent qwen divergence was root-caused as a **cold GQA workspace width-leak** (`native_workspace_query_rows` reserved verify-width W into later M=1 fallback), fixed by keeping cold prefill prompt-shaped only.
- **SPEEDUP is acceptance-bound and unwinnable at prompt-lookup rates — for ANY kernel.** A captured M=W verify forward is inherently **~4.5× (Marlin, not byte-identical) / ~8× (byte-identical per-row)** an M=1 step (qwen2.5-14b captured replay walls: M=1 **7.9 ms**, Marlin M=8 **35.9 ms** = 4.5×, byte-identical M=8 **65 ms** = 8.3×). The "weights-read-once → verify ≈ M=1 cost" premise is **false** here: the GEMV is compute/occupancy-bound at these sizes. **Break-even** (no thrash): Marlin needs **mean_accept > 4.5**, byte-identical needs **> 8.2**. **Measured mean_accept (prompt-lookup, normal + repetitive prompts) = 2.4–3.3** → Marlin spec ≈ 0.72×, byte-identical ≈ 0.40× even at zero thrash. The baseline Marlin captured spec is *also* slower than greedy at these rates, so "preserve the speedup" is vacuous.
- **Second-order thrash:** verify and M=1 decode graphs share the **single device-graph slot**; miss-path M=1 decodes evict the captured verify graph → re-warm+re-capture, pushing E2E to 0.04–0.21× with huge run-to-run variance (W=9: 9.5s↔32s↔41s). Sebastian's **hit-density gate** mitigates the generic-prose regression (−40%→−6%) by only engaging on clustered hits; a **dedicated second graph-slot** (structural EP/session change, perf-only) is the highest-leverage harness fix (ceiling ~`111×W/B*`) but still cannot make byte-identical spec win at mean_accept < 8.
- **Spec-additive wins exist but do not change the base-vs-ORT story:** on high-acceptance repetitive/copy prompts spec reaches 347 tok/s (Sebastian) / 326 tok/s (Deckard, 2.88×) — but these are additive-on-top demos, NOT a base-decode ORT win. **Standing directive: NEVER claim an ORT-win via speculative decode** — base decode is the ORT-comparison basis.
- **Verdict:** PR #1006 stays **DRAFT**. Ship byte-identity only as an **opt-in determinism/reproducibility oracle**, never a default speed feature. Spec-decode can only ever pay off with a **high-acceptance drafter** (real matched draft model, EAGLE/tree drafting, or batch>1) that clears mean_accept > 4.5 — and **even then the win is marginal**, floored by the ~4.5× Marlin verify cost. Prompt-lookup alone (mean_accept 2.4–3.3) cannot clear break-even. *(leon-spec-verify, batty-specdecode-w-workspace, deckard-specfix-graphslot, sebastian-specdecode-e2e drops merged here; the earlier per-attempt narrative below and #998 rejection above were already captured from prior sessions.)*

- **Byte-identity IS achievable + capture-safe** (opt-in `ONNX_GENAI_SPEC_PERROW_VERIFY=1`, default OFF → zero prod regression). Root-cause reframed: the prior reviewers' reference — plain greedy with **Marlin prefill ON** — is itself **run-to-run non-deterministic**; the production default (**Marlin prefill OFF) IS deterministic** and is the correct reference. The verify divergence is a **K-reduction reassociation** (batched M=W GEMM sums K in a different order than M=1 GEMV), **not** a margin/tie problem — which is why the prior three attempts were ill-posed. Routing every verify row through the exact M=1 GEMV is byte-identical by construction: **qwen 5/5 (W=5–9)**; **glm 3/5 — W=6@111 & W=9@157 diverge in the ATTENTION path (q_seq=W flash-attn vs greedy q_seq=1 gqa_decode), NOT the GEMV.** Whole-graph capture segments=1, fallbacks=0; f64 oracle 25/0.
- **SPEEDUP is FUNDAMENTALLY unwinnable for prompt-lookup:** M=W verify is inherently ~M× cost (M=1 7.9 ms / Marlin M=8 35.9 ms / byte-identical per-row M=8 65 ms). Break-even needs mean_accept **>4.5**; prompt-lookup gives only **2.4–3.3**. Shipped **opt-in only** as a determinism/correctness oracle, NOT a speed feature (per-row is ~6× slower → never default; repeats the #998 regression otherwise).
- **If ever revisited:** biggest kernel-independent lever = give verify its **own device-graph slot**; precondition for spec-decode to pay off = a **real draft model or batch>1** (prompt-lookup alone cannot clear break-even). The completing fast path is a batched M=W fp32-accumulate GEMV (weights read once) + fp32 verify-attention for glm's W=9 tie — large but well-scoped, not impossible.
- **2026-08-15 primary-source root-cause of the earlier W-dependent qwen divergence (Batty, base `aceed718`):** the W=9 qwen index-2 divergence was NOT an acceptance/margin/verify-logits bug (the first captured row-0 at W=9 matched a fresh M=1 decode: `argmax=104887`, margin `1.375`). It was a **cold-generation workspace leak** — `native_workspace_query_rows` pre-reserved the GQA workspace at the speculative verify width W, and that width-dependent layout leaked into later M=1 fallback decode. Fix: keep the cold prefill workspace **prompt-shaped only**; reserve the width-W GQA workspace at the actual captured-verify call site (`run_verify_captured_cuda`) after M=1 bindings are restored. After the fix qwen W=5–9 all match plain greedy for 160 tokens (before: `哈`×20 first failed at W=7, W=8/9 diverged at idx 2); GLM generic/repetitive stayed byte-identical (repetitive kept its 100% accept / 197.96 vs 111.94 tok/s spec win). This is the "margin tweak" mischaracterization corrected: the enabling fix was a workspace-shape bug, not a tie threshold.
- **Standing directive:** NEVER claim an ORT-win via speculative decode — spec is additive-on-top only; base decode is the ORT-comparison basis.

## 2026-08-15 — multi-arch + interleave-dequant + PDL/device-token-loop program

### DeepSeek-V2 support status — ahead of ORT at op level, blocked at LOAD by MLA KV-geometry (Coordinator, for Justin)

DeepSeek support is substantially already built native (`qmoe.rs`, `qmoe_gemm.rs`, `qmoe_grouping.rs`, `block_quantized_moe.rs`, `compressed_sparse_attention.rs`, `sparse_kv_gather.rs`, + deepseek-v4 MTP scaffolding in config.rs). The DeepSeek-V2 realshape QMoE int4 fixture decoded successfully on native CUDA in **July 2026** (QMoE + Attention + RoPE + TopK/Scatter/Gather, full greedy, ~313 tok/s); **ORT GenAI 0.14.1 cannot even load it** (`Unsupported model_type: deepseek_v2`) — native is *ahead* of ORT on DeepSeek-V2.
- **Regression (origin/main `6e3dd30f`):** now fails at LOAD in the new **Resource Governor** — "cannot derive KV memory budget: per-layer KV page geometry unknown (16 tokens/page, no byte size)." Root cause: MLA has an **asymmetric KV cache** — key head_size **192** (qk_nope 128 + qk_rope 64), value head_size **128**; governor's KV-page byte derivation assumes symmetric `head_size` (config declares only `head_size: 128`).
- **Action:** Filed **#1012** for the memory workstream (not our domain — did NOT touch governor code); fix direction: compute KV-page bytes as `num_kv_heads * (key_head_size + value_head_size) * dtype_bytes` from actual per-tensor K/V shapes. Related #783 (KV stride descriptor), #652 (native KV accounting). No env bypass exists.
- **Family validated on current main (native CUDA, GPU3):** `deepseek-coder-1.3b-int4-cuda` loads+decodes clean **847.9 tok/s** byte-identical; `deepseek-r1-distill-qwen-1.5b-int4-cuda` **725.7 tok/s** (Qwen arch); `deepseek-v2-realshape-qmoe-int4` (MLA) blocked ONLY by #1012. Net: coder + R1-distill run today; V2 needs the #1012 governor fix to load.

### 2026-08-15T14:28Z — Multi-arch (A100 sm_80 + Blackwell sm_100/sm_120) optimization strategy (Justin via Coordinator)

Extend the CUDA EP beyond Hopper (H200, sm_90) to A100 and Blackwell consumer+professional. **No A100/Blackwell hardware available now** (Justin verbatim: "i don't have access right now. you may reference known optimizations for them").
- **Approach:** build arch-guarded kernels from KNOWN/published optimizations; compile-validate on H200; correctness via arch-independent invariants; leave ready-to-benchmark when hardware lands. Only sm_90 gets live perf now.
- **Sequence:** (1) arch-INDEPENDENT wins first (TRT-LLM interleave+bias-baked dequant — ~35% fewer dequant instrs, attacks the proven issue-bound bottleneck, carries to every arch); (2) SM-version dispatch scaffolding; (3) per-arch paths.
- **Per-arch notes (TRT-LLM/FLA survey):** A100 sm_80 = cp.async (LDGSTS), NO TMA, 3rd-gen TC, FLA tiles BS=32/BV=128 (our cp.async KV pipeline was NO-GO on Hopper because TMA was cheaper; A100 has no TMA alternative → latency-hiding expected net-positive, re-test when hw lands). Hopper sm_90 = TMA+wgmma, PDL untapped, tiles BS=64/BV=256. Blackwell-DC sm_100 (B200/GB200) = 5th-gen TC, tcgen05 MMA, native NVFP4/FP6, 2-SM MMA (FP4 path needs FP4 activations). Blackwell-consumer sm_120 (RTX 50/GB202) = 5th-gen TC, no 2-SM MMA, GDDR7 → separate tile tuning. PDL (sm_90+) overlaps next-kernel launch with current tail.
- **Binding:** beat ORT under EQUAL non-speculative base conditions first; interleave-dequant is the honest lever; spec-decode remains additive-only.

### PR #1019 — interleaved+biased int4 dequant GEMV, opt-in (Deckard; branch `squad/interleave-dequant` off `6e3dd30f`) — WIN +2.4% glm

TRT-LLM FastInterleaved + biased int4 dequant on the M=1 int4 decode GEMV, gated opt-in `ONNX_GENAI_INTERLEAVE_DEQUANT=1` (default unchanged). Attacks the ISSUE/LATENCY-bound GEMV (~32% of decode; L1TEX scoreboard ~61%, DRAM ~2% peak) by cutting dequant INSTRUCTION COUNT — a lever **BEYOND ORT** (ORT does not interleave).
- **How:** (1) offline nibble interleave (cached per-weight like Marlin `ensure_repacked`): permute packed word `[e7..e0]→[e0,e2,e4,e6,e1,e3,e5,e7]` so 4× LOP3 emits natural pair order, eliminating the per-lane `prmt.b32`; (2) biased converter folds symmetric −8 into LOP3 constants, dropping the `sub.f16x2` loop → 9 instrs/8 vals vs ~14. For ORT symmetric int4 the +8 bias-bake is a no-op on stored data; only the interleave is needed.
- **Correctness (hard gate PASSED):** byte-identical to default multicol (new device test `int4_interleaved_dequant_is_bit_identical_to_multicol`); glm+qwen token IDs identical off/on.
- **Evidence (H200, median of 5, fallbacks=0):** glm decode OFF 211.5→ON 216.6 = **+2.4%**; ncu −13.9% instructions, −8% kernel time (long-scoreboard stall 2.3→3.2 = pushed to memory-latency floor). qwen block32 = correct NO-OP (never enters branch). E2E<per-kernel because only wide gate/up-class projections route to multicol on glm; qkv/down use split-K (untouched).
- **Dispatch gotcha:** the gate ONLY reroutes nodes already selecting wide multicol; forcing split-K narrow-N onto multicol regressed ~2×.
- **Status:** OPT-IN (glm base is knife-edge; proven-then-default). Follow-up (compounding): extend to split-K wide (captures glm qkv/down) and block-32 `scales_f16` (qwen).

### PR #1029 — compounding interleave+biased dequant to split-K wide GEMV (Deckard; branch `squad/interleave-dequant-2` off `eada3c5d`; 🟡 needs-review) — MERGED, +8.8% glm, BYTE-IDENTICAL

Realizes #1019's compounding follow-up: extends the opt-in interleaved+biased dequant from wide-multicol-only to the **split-K wide GEMV** entry (`matmul_nbits_gemv_f16_general_bs_splitk_wide → *_interleaved`), capturing glm's qkv/down projections #1019 left untouched. Dispatch keys off the ALREADY-selected kernel entry (not re-derived shape predicates), mapping `WIDE_MULTICOL→*_interleaved`, `SPLITK_WIDE→*_splitk_wide_interleaved` — guarantees identical launch geometry, avoiding the ~2× split-K-bypass regression.
- **Correctness (BYTE-IDENTICAL, not just tolerance):** interleave is a pure per-word layout change → identical dequant values in identical positions; each lane's fp32 partial and K_SPLIT shared-mem reduction order unchanged → bit-identical. New device test `int4_interleaved_dequant_is_bit_identical_to_splitk_wide`; 3 interleave device tests pass. Cache-aliasing gotcha found+fixed: `ensure_interleaved` key `(ptr,bytes,ordinal)` can give stale hits if a freed test buffer is reallocated at the same address/size — test helper leaks `packed_dev` (production weights never freed, so no aliasing).
- **Evidence (H200, glm-4-9b-int4, median of 5, fallbacks=0):** glm decode OFF 211.4→ON **230.1 = +8.8%** (vs #1019's +2.4% alone — split-K coverage is what compounds); byte-identical token stream; ncu split-K wide `sm__inst_executed.sum` 6,148,096→5,246,976 = **−14.7%** (−16.8% on wide down-proj).
- **block-32 `scales_f16` fp16-accum (qwen-class): EVALUATED, NO-GO → EXCLUDED/REMOVED.** qwen2.5-14b-int4 is SYMMETRIC (3 inputs, no zero_points despite "-zp" dir name) so a pure bias-fold is byte-identical, but decode perf was within noise (~154 tok/s off vs on): qwen's big projections DO hit the `scales_f16` entry but it isn't issue-slot-bound the way the block!=32 fp32 wide path is (dropping 4 `sub.f16x2` isn't on its critical path). Per "exclude no-speedup paths" the block-32 kernel/helper/entry/test were removed; flag is a pure no-op on block-32.
- **fp16-accum wide-multicol (`use_gemv_fp16`):** DEFERRED — already accuracy-gated/off-by-default; interleaving would break byte-identity, low value.
- **Arch-independence:** weight-layout + ALU change, no H200 intrinsics → +8.8% should carry to A100/Blackwell; advances "arch-INDEPENDENT wins first."
- **Status:** OPT-IN (glm base knife-edge). Cumulative flag-ON: glm **230.1 tok/s (+8.8%)**, qwen ~154 (neutral). Default-on only after broader multi-model + multi-arch confirmation.

### PDL (Programmatic Dependent Launch) launch-overlap feasibility spike — NO-GO (Gaff; branch `squad/pdl-launch-overlap` off `6dfc30cd`, post-#1029)

Diagnosis-first spike on glm-4-9b-int4 H200: is the remaining dominant axis the launch/latency chain (long serial chain of tiny per-layer kernels), addressable by PDL (sm_90+: `cudaGridDependencySynchronize` + `cudaTriggerProgrammaticLaunchCompletion` + `cudaLaunchAttributeProgrammaticStreamSerialization`)? Gate: build PDL **only if** inter-kernel gaps > ~5% of decode AND not already recovered by graph capture.
- **Decode runs under CUDA graph capture BY DEFAULT** (`native_decode/load.rs` auto-enables when topology is graph-safe; `fallbacks=0`) — this is the certified base path benched vs ORT.
- **Captured gap decomposition** (nsys `--cuda-graph-trace=node`, ~80k instances): kernel active **78.3%**; **per-node launch gap 4.30%** (median 416 ns/node — PDL's entire domain); **cross-replay gap 17.4%** (host token-feedback at the replay-boundary node, argmax readback→next-token→relaunch — NOT a kernel→kernel boundary, PDL cannot help). Distribution bimodal: tiny ~416 ns per-node gaps or huge >100 µs replay-boundary gaps; nothing in 5–100 µs.
- **Counterfactual:** `ONNX_GENAI_CUDA_GRAPH=0` (eager) → per-node launch gap ~30% (median 1280 ns/node), 108.8 tok/s; captured collapses it to 4.30% (416 ns/node), 184.7 tok/s (+70%). Capture already occupies PDL's domain.
- **Verdict NO-GO (fails both gate clauses):** PDL-addressable gap is 4.30% < ~5% and near the grid-dispatch floor (realistic upside ~1–2%, inside host noise); capture already recovers ~85–90% of the launch chain; the real remaining idle (17.4%) is cross-replay host token-feedback, out of PDL's reach. PDL would only help the eager fallback (degraded path we don't ship). **Decision: do NOT build PDL machinery** — no intrinsics, no launch-attribute plumbing, no flag. Diagnosis-only, no source touched.
- **Arch note:** PDL is Hopper+Blackwell sm_90+ only; wherever decode runs under capture, capture subsumes PDL's benefit.
- **Where to look next (higher-leverage):** the 17.4% cross-replay host-feedback gap → **on-device greedy/sampling** to avoid the argmax→host→relaunch round-trip, or CUDA-graph conditional nodes to fold token-feedback into the captured graph. (Holden now owns this device-resident token-feedback loop.)

## 2026-08-15 (evening) — base-decode reaches ORT parity; rmsnorm-traffic levers evidenced

### PR #1036 — device-resident token-feedback loop, opt-in (Holden; branch `squad/device-token-feedback` off `6dfc30cd`, 🟡 needs-review) — MERGED, ~1% base decode, BYTE-IDENTICAL

Realizes the follow-up Gaff's PDL spike handed off: attack the 17.4% cross-replay **host token-feedback gap** (between two captured-graph replays the host is on the critical path — D2H sync to read the sampled token + H2D write of next token/position before the next replay). Lever keeps the sampled token on-device across `k` captured replays so the host leaves the per-step critical path, gated opt-in `ONNX_GENAI_DEVICE_TOKEN_LOOP=k` (default off, `k≤16`).
- **How:** new NVRTC `device_token_writer` kernel (single-thread d2d: reads on-device argmax `greedy_result`, folds token id as i64 into persistent `input_ids`, sets attn-mask `1` at next position, optionally writes `position_ids` [skipped for GLM which derives position from mask], appends to a host-drainable log, ORs capture-error word). Chained-replay orchestration: prime step 0 host-side (one async H2D, no sync), then `k×` `[replay → device_argmax launch-only → device_token_writer]` back-to-back, then ONE D2H drain of `k` tokens + OR'd capture-error. NativeLoopAdapter lookahead buffer preserves per-token EOS/stop/callback. sm-agnostic (plain d2d, capture-safe: argmax+writer launched *between* replays, not inside the captured graph). Armed only when device-loopable (capture engaged, mask frozen to physical width, batch 1, i64 io); else falls back to single captured step. fallbacks=0 on both models.
- **Parity bug byte-identity caught + fixed:** writer set `mask[next_position]=1` on *every* step incl. the last, landing one bit past `current_len`; because mask is frozen to physical width the stray `1` survived `session.reset()` and inflated the next generation's derived prefill length → corruption. Fix: `clear_trailing_mask_bit(current_len)` at chain end. After fix: byte-identical.
- **Parity gate (PASSED):** greedy token stream ON == OFF — glm-4-9b-int4 identical at k=2/4/8 (128 tok, exact diff); qwen2.5-14b-int4 (real rank-1 position binding) identical at k=4, armed (chained_steps=256, fallbacks=0).
- **Evidence (H200, glm, interleaved off/on 5-run medians ×4):** off ~211.9 vs k=8 ~214.0 = **+0.84–1.36%**; distributions do NOT overlap (real signal, not noise). k=4 ≈ k=8 (diminishing past k=4). nsys cross-replay big-gap idle 14.86%→3.72% (208→64 gaps, one host round-trip per 8 replays), BUT wall-clock win only ~1%.
- **HONEST correction (durable):** the **17.4% cross-replay host gap is largely an nsys artifact** — profiler instrumentation slows the *host*, inflating the host-on-critical-path region; in un-profiled execution the host round-trip is already mostly overlapped with GPU work, so eliminating it recovers only ~1%. Mechanism is correct and works as designed; the opportunity was simply smaller than the profiler implied. Not the decisive ORT-gap closer the 17.4% suggested — "small but real."
- **Status:** OPT-IN, default-off path byte-unchanged. Reviewer question raised: whether ~1% justifies the maintenance surface vs archiving as documented no-go-on-magnitude.

### Review PR #1036 — APPROVE (Rachael, independent; `4fb8882c`, +835/−1, 10 files)
Verified: (1) **default path provably byte-unchanged** — env unset → `k=0`, no device binding allocated, loop entered only under `device_loop_k>=2`, new EP trait method additive with `KernelFailed` default; (2) **token-writer parity** — device-written input_ids/position_ids/mask byte-identical to host `write_decode_inputs` across chain boundary, last step, KV-growth, capacity boundary, mid-chain retirement (both GLM mask-derived + qwen rank-1 position); trailing-bit fix genuinely required (`rewind(0)` zeros only `[0,logical_len)`, stray bit sits one past); (3) **capture-error latching** — detection-before-consumption preserved (chain bails+invalidates before any token handed out); (4) **EOS/stop mid-chain** — shared `run_decode_loop` applies per-drained-token checks, post-EOS tokens never emitted; (5) governor/KV-budget/planner NOT touched; (6) rustfmt clean. Two non-blocking NOTES: **N1** no automated (hardware-gated) parity regression test — add a device-gated loop-on-vs-off parity test before ever promoting to default; **N2** contained post-EOS KV/`current_len` overshoot to `past_len+k` — safely absorbed by reset/rewind, does NOT reach paged KV-mirror path, but re-check if promoted to default or wired to KV mirroring.

### ORT-gap RE-BASELINE — native at PARITY with ORT graph-on, AHEAD graph-off (Bryant; read-only, off `origin/main` `c46486e9`)
**The "1.09x behind / 250 tok/s ORT" figure is STALE — do NOT keep quoting it.** On current main, glm-4-9b-int4 native best-config decode is **~0.7% behind** ORT-genai 0.14.1 graph-on (parity within shared-node variance), and **~3% faster** than ORT graph-off. Both engines are overwhelmingly int4-decode-GEMV/bandwidth bound, converging on the same wall.
- **Headline (128-tok steady, greedy, medians, 1×H200, fallbacks=0):** native OFF 211.85 / native ON (`ONNX_GENAI_INTERLEAVE_DEQUANT=1`) **229.98** / ORT graph-on **~231.6** / ORT graph-off ~205.2. Ratios: ORT_best/native_best = 231.6/229.98 = **1.007** (parity); interleave lever = **+8.6%** (reproducible); native_off/ORT_off = 1.032 (native beats ORT graphs-off by ~3%). Entire residual "gap" is ~1.5 tok/s in the graph-on comparison. The stale 250/1.09x predated the interleave lever or used a different ORT build/config.
- **Fairness:** ORT ran on the SAME weights (`glm-4-9b-int4-cuda-ortfair/` hardlinks `model.onnx.data`), same 128 tokens, same greedy, same steady-median, single H200. `bench-scripts/ort_glm_bench.py`, og 0.14.1 in `.ort-genai-cuda-venv` (needs CUDA-12 libs on `LD_LIBRARY_PATH`).
- **Per-kernel localization (nsys `--cuda-graph-trace=node`, native ON decode):** int4 GEMV **75.8%** (44.4% splitk_wide_interleaved + 31.3% wide_multicol_interleaved), GQA attention **11.9%**, rmsnorm **6.6%**, elementwise 4.0%, silu_mul 1.8%. ORT decode: MatMulFloatInt4 62.2% + separate Dequantize4Bits 5.1% ≈ 67% int4, flash_fwd_splitkv 9.7% attention, SkipLayerNorm ~2.9%. **Both int4-GEMV-bound** — confirms why launch/host-loop levers (PDL NO-GO, device token loop ~1%) barely moved the needle.
- **Next-target ask:** primary = int4 decode GEMV (75.8%, but at ncu-proven issue-slot floor and ORT's equal → thin upside; any win must be bytes/step or memory-issue efficiency, not occupancy/launch). Secondary (better risk/reward) = **rmsnorm** — native `skip_rmsnorm` is ~2× ORT's per-kernel share (6.6% vs 2.9%); closing it could flip the sub-1% deficit into a lead.

### rmsnorm-traffic NO-GO #1 — device-resident (register-cached) skip-RMSNorm (Roy; branch `squad/rmsnorm-decode-traffic` off `88cce1d2`; reverted, no PR)
Tested Bryant's rmsnorm secondary via the "redundant `y` DRAM round-trip" hypothesis: fp16 `skip_rmsnorm_f16_block_half4` writes residual `sv` to global in pass 1, re-reads it in pass 2; cache `sv` in registers, write `y` once → remove ~1/3 DRAM traffic byte-identically. Opt-in `ONNX_GENAI_RMSNORM_FUSED_RESIDENT=1`, `_resident` sibling kernels with compile-time elements-per-thread cap + airtight in-kernel fallback.
- **Parity PASSED (byte-identical):** new GPU unit tests (register + fallback paths) green; glm-4-9b-int4 128-tok greedy byte-identical OFF vs ON, fallbacks=0.
- **NO-GO on H200 — slight regression:** resident kernel median 2976 ns → 3648 ns (CAP=8, +22.6%) / 3072 ns (CAP=2, +3.2%); share moved WRONG way 4.6%→5.2%; E2E ~128.4→~121 tok/s (~5–6% regression). **Root cause: the premise is false on H200** — the 60 MB L2 keeps the 8 KB `y` vector resident, so pass-2 reread is an L2 hit, not a DRAM round-trip; there's no DRAM traffic to remove, and the register-pressure/restructure cost exceeds the (free) L2 round-trip. **The native 6.6%-vs-2.9% rmsnorm gap is NOT redundant-`y`-DRAM-traffic** — likely the block kernel's fixed overhead of spreading one decode row over a 1024-thread block (two `__syncthreads` passes + 1024-wide shared tree). Future lever should target THAT (narrow single-/few-warp decode reduction, or fuse norm into MatMulNBits epilogue). Consumer-GPU caveat (unmeasured): low-L2 parts might break even with CAP=2. Branch reverted to origin/main; governor/KV files untouched.

### PR #1038 — warp-shuffle tail for skip-RMSNorm block reduction, opt-in (Pris; branch `squad/rmsnorm-warpshuffle` `597c83a2`) — MERGED, BIT-IDENTICAL, −7.6% kernel / +0.49% E2E
Different rmsnorm lever than Roy's (attacks the reduction tail, not the `y` round-trip): replaces the shared-memory tree's final intra-warp levels with `__shfl_down_sync` warp folds, gated opt-in `skip_rmsnorm_warp_reduce_enabled()` (default OFF). Split each dtype kernel (`f32`, `f32_dense`, `bf16`, `f16_block_half4`) into `_tpl<WarpTail>` with a `<false>` wrapper preserving the original body.
- **Bit-identical (verified analytically + by passing byte-for-byte GPU parity test):** handoff offset has no off-by-one (`offset>=32` runs in shared mem, leaving `red[0..31]` = 32 per-warp partials, exactly the pre-offset-16 state); shfl pairing/order matches the shared tree level-for-level; rounding identical (`__fadd_rn` both sides; under NVRTC defaults — no `--use_fast_math`/`--ftz` — a bare `a+b` is bit-identical to `__fadd_rn`, incl. denormals); lane-mask sound (every block launch has `nt` = power-of-two ≥ 32, full mask always names only active lanes); single-warp `nt==32` degenerates correctly. New test `warp_reduce_tail_is_byte_identical_to_shared_tree` (byte `assert_eq!`, f16/bf16/f32 × hidden {128..4096}) passes on H200.
- **Evidence:** −7.6% on the skip_rmsnorm kernel, +0.49% E2E. Default-off path byte-for-byte unchanged.
- **Review APPROVE (Rachael, independent):** verified all bit-identity + default-safety claims above. Non-blocking: coverage gap — single-warp `nt==32` (hidden≤32) not exercised (proven correct analytically); `record_kernel_variant!` still records base name for `_warp` entry (telemetry label only).

## Native-vs-ORT fairness rule

Native-vs-ORT claims must compare the same artifact, quantization, accuracy level, and steady-state methodology with oracle-correct output. If one engine crashes, rejects the graph, runs CPU, disables CUDA graphs, or uses a different weight file/config, report a capability/config gap rather than a throughput multiplier. For ORT-genai decode, verify CUDA provider and share-buffer/cuda-graph fast path are active before quoting tok/s.

## Benchmark and profiling discipline

Separate measured, estimated, and projected. Same-run PR-vs-base deltas beat absolute numbers under shared-host load. For CUDA-graph decode, `ONNX_GENAI_PROFILE_OPS=1` is a host/eager dispatch view and can mis-rank kernels; use `nsys --cuda-graph-trace=node` for kernel mix and `ncu --graph-profiling node --set full` for stall mechanism. A SIMD/accelerated path without a reachability test is equivalent to an unwired placeholder.

## Numerics and portability discipline

Default-on CUDA decode optimizations must be portable or explicitly arch-gated with byte-identical fallback. Token byte-identity is an argmax stability claim, not a numeric invariant; numeric changes need oracle/tolerance justification. Preserve Rule 11: unsupported devices must fall back without behavior loss. Env knobs used for A/B must be documented, deterministic under capture, and not hide default regressions.

## Testing and CI standing directives

- `cargo test --workspace` silently truncates on failure; use `--no-fail-fast` for full-suite evidence.
- Run new tests in isolation before trusting full-suite green. Assert on what code did, not summaries.
- An agent self-report is not evidence; verify with code, command output, and tests.
- Reviewer lockout is enforced: authors do not revise their own rejected artifacts.
- CI is asynchronous; required local targeted tests/builds/hardware probes remain blocking, but do not idle solely waiting for CI.
- Never commit `.squad/` files to external repos; if that happens, purge history rather than only deleting in a follow-up commit.

## Active historical pointers

For detailed per-PR narrative, use archives rather than expanding this live file. Primary locations: `.squad/decisions-archive/2026-07.md`, `.squad/decisions-archive/2026-08.md`, and `.squad/decisions/archive/`. The complete live ledger immediately before the 2026-08-15T03:10 decode-arc compaction is `.squad/decisions/archive/2026-08-15T03-10-00Z-decisions-pre-decode-arc.md`.
