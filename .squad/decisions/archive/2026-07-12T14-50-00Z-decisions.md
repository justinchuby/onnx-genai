# Decisions

Canonical, append-only record of accepted team decisions. Only the Coordinator (via Scribe merge) writes here. Agents drop proposals in `decisions/inbox/`.

---

### 2026-07-12T13:52:00-07:00: Decisions archive rollover
**By:** Scribe
**What:** Archived prior canonical decisions to `decisions/archive/2026-07-12T13-52-00Z-decisions.md` because `decisions.md` reached 156336 bytes.
**Why:** Keep the hot decisions file small while retaining the full historical record under `.squad/decisions/archive/`.

---

### 2026-07-12T13:12:00-07:00: Engine decomposition and DecodeBackend seam
**By:** Batty
**What:** Decomposed `onnx-genai-engine`'s former god `engine.rs` into focused modules: `config.rs` for public request/result/config DTOs, `session.rs` for session/draft state, `decode.rs` for decode-path detection plus the internal `DecodeBackend` seam, `decode_loop.rs` for the shared token commit/generation loop, `kv_bridge.rs` for paged-KV mirroring/materialization/rewind, `processors.rs` for processor-chain/token-selection helpers, and `speculative.rs` for the draft/verify implementation. `engine.rs` is now the orchestrator and re-exports the same public API types as before.
**Why:** This makes DESIGN §26 batched serving and future decode backends additive: `DecodeBackend` exposes `current_len`, `max_context`, `decode`, `rewind`, and `reset`, and is implemented for the existing past/present `DecodeSession` and `StaticCacheDecodeSession` paths. Direct/session/priority/pipeline generation now use `decode_loop::{run_decode_loop, step_decode_loop}`; speculative commit uses the same `commit_selected_token` path after draft verification. Behavior was validated unchanged with `cargo check --workspace`, `cargo test --workspace`, and focused tiny fixtures: `tiny-llm` past/present tokens remain `[22, 22, 20]`, `tiny-llm-scatter` static-cache tokens remain `[23, 15, 28]`, and speculative tiny-llm output remains equal to baseline greedy.

---

### 2026-07-12T13:32:00-07:00: Batched static-cache active-row compaction
**By:** Deckard
**What:** `BatchedStaticCacheDecodeSession` now supports active-row compaction for STATIC-CACHE TensorScatter models. I chose compaction over row-view/gather because ORT IoBinding binds whole OrtValues; after packing live rows into physical slots `0..active_count`, we can bind prefix aliases shaped `[active_count, MAX_LEN, KV_DIM]` for KV inputs/outputs and run the model with a smaller batch. The public surface is additive: `set_active_rows(&[row_ids])`, `deactivate_row(row)`, `compact()`, `assign_row(row)`/`admit_row(row)`, `step_active`, `step_active_select`, plus slot/active diagnostics (`active_rows`, `active_batch_size`, `physical_slot`, `logical_row_for_physical_slot`, `inactive_compute_fraction`). Existing full-B `prefill`/`step`/`step_select` continue to work.
**Why:** Row-view/gather over arbitrary batch rows is not directly exposed by the ORT C IoBinding API we use, while prefix aliases are already compatible with our `Value::alias_with_shape` mechanism. Compaction copies each moved KV row (`MAX_LEN * KV_DIM` per KV tensor) only when active membership/order changes, then avoids per-step model compute for holes. Slot mapping is explicit: stable logical row ids map to physical slots; `compact()` frees inactive logical rows by removing their physical mapping; `admit_row()` assigns a freed slot, zeros that KV region, resets its cursor, and marks it active. Correctness was validated on `tests/fixtures/tiny-llm-scatter/`: B=4, deactivate 2 rows, active-only `[2,1,V]` logits/tokens for the remaining rows matched their pre-deactivation/unbatched traces, and admitting a replacement row into a freed slot matched its single-row trace. With 2 active of B=4, active-only compute is estimated at 50% of full-B for each decode step after the one-time compaction copy; after admitting a third row, estimated compute is 75% of full-B (25% saved).

---

### 2026-07-12T13:44:00-07:00: Server batch driver owns static-cache continuous batching
**By:** Rachael

**What:** `onnx-genai-server` now moves the Engine into one dedicated `onnx-genai-batch-driver` thread and talks to it through bounded Tokio channels instead of sharing generation through `Arc<Mutex<Engine>>`. For STATIC-CACHE models, stateless `/v1/chat/completions` requests submit `GenerateRequest`s to one `ContinuousBatchManager`; the driver drains incoming submissions, queues beyond `max_batch=4`, steps active rows, and routes token/final events back to each request's output channel. Streaming requests turn token events into SSE chunks; non-streaming requests collect the final result and keep the existing OpenAI response/tool parsing path. Past/present models, and X-Session-Id requests until the engine manager accepts SessionId submissions, run on the same driver thread through the existing per-request engine path to preserve persistent KV semantics.

**Why:** A single owner keeps the static-cache batched session's unsafe Send/Sync assumptions sound while letting concurrent HTTP requests share batched forward passes. Bounded command/output channels provide server-side back-pressure, while the manager's FIFO queue holds requests that exceed the active row capacity. Tool calls remain turn-based for OpenAI compatibility: a parsed `<tool_call>` completes the assistant turn with `finish_reason: "tool_calls"`; role=`tool` follow-ups are separate submissions rather than mid-token pauses.

**Validation:** `cargo check --workspace` and `cargo test --workspace` passed. Added `concurrent_static_cache_chat_completions_share_batched_driver`, which fires four concurrent `/v1/chat/completions` requests against `tests/fixtures/tiny-llm-scatter` and verifies each response matches an independent direct static-cache generation.

---

### 2026-07-12T13:24:00-07:00: Stage A batched static-cache decode loop
**By:** Sebastian

**What:** Added `Engine::generate_batched_static(Vec<GenerateRequest>) -> Result<Vec<GenerateResult>>` for fixed-batch STATIC-CACHE models. The path builds one `BatchedStaticCacheDecodeSession`, pre-fills request rows, then advances all active rows with one batched static-cache forward per decode step. Row logits are demuxed and each row applies its own processor chain, sampling options, stop/EOS/context/max-token checks, generated-token state, and result assembly.

**Correctness:** `crates/onnx-genai-engine/tests/batched_static_decode.rs` asserts batched rows on `tests/fixtures/tiny-llm-scatter/` exactly equal running each request alone through the existing single-sequence engine. This preserves Deckard's ORT-level batched==unbatched property through engine processors and stop handling.

**Row activity:** Finished rows call `BatchedStaticCacheDecodeSession::deactivate_row`, so inactive rows are no longer sampled, committed, or returned to the server-facing result path. Physical ORT compute is still fixed-B because the current ORT batch allocates/binds full `[B, MAX_LEN, KV_DIM]` buffers and has no row-view/prefix-batch compaction API. True compaction remains Stage B backend work: add logical-to-physical row mapping plus packed active row views or a cheap cache-row move so `(B-active)/B` compute is reclaimed without replaying live contexts.

**Measurement:** Ignored micro-measurement `batched_static_decode_reports_tiny_scatter_throughput` on `tiny-llm-scatter` with 8 rows × 8 generated tokens reported sequential `19851.12 tok/s` (`3.224ms`) vs batched `123562.14 tok/s` (`517.958µs`), `6.22x` aggregate speedup on this tiny CPU fixture. Both paths excluded model-load time; the fixture is small enough that this is a loop/ORT-call-count signal, not a real-model GPU throughput claim.

**Stage B/server contract:** The server/scheduler can batch compatible static-cache requests by passing a fixed set of arrived requests to this API and receiving results in input order. Continuous batching, mid-batch join/leave, PAUSED-on-tool, non-static past/present batching, and efficient compaction are intentionally deferred; Stage B should drive stable row handles and add packed active-row compaction before treating paused or finished rows as free capacity.

---

### 2026-07-12T13:32:00-07:00: Continuous batching manager for static-cache serving
**By:** Sebastian

**What:** Added an engine-level continuous-batch manager for STATIC-CACHE models. `Engine::run_continuous_batch(requests, max_batch)` is the synchronous Stage B driver; `Engine::continuous_batch_manager(max_batch)` exposes the lower-level `ContinuousBatchManager` with `submit`, `step`, `poll`, queue length, active length, and idle/work checks. The manager keeps FIFO pending requests plus fixed logical row slots, pre-fills an admitted request into a freed row, advances rows with pending logits one token per step, deactivates rows on EOS/stop/max/context, emits `ContinuousBatchEvent::{Token, Finished}`, and returns final driver results in input order.

**Why:** Stage A fixed-batch decode only runs the initial N requests to completion. Stage B needs row join/leave so an agent swarm can keep the batch full as requests finish at different times, while preserving each row's processor chain, sampling options, stops, constraints, context limits, and result correctness.

**Correctness:** `crates/onnx-genai-engine/tests/batched_static_decode.rs` now runs 16 `tiny-llm-scatter` requests through `max_batch=4` and asserts every result exactly equals running the same request individually through `Engine::generate`.

**Throughput:** Real fixture measurement (`cargo test -p onnx-genai-engine --test batched_static_decode -- --ignored --nocapture`) on `tests/fixtures/tiny-llm-scatter/`: sequential 18,232.10 tok/s (128 tokens, 7.020584ms), Stage A static batch16 174,873.42 tok/s (731.958µs), continuous max_batch=4 50,878.63 tok/s (2.515791ms, 2.79x vs sequential), continuous max_batch=8 59,989.44 tok/s (2.133709ms, 3.29x vs sequential). Deckard's active-row compaction API is present; the manager uses `step_active` when all remaining active rows need the next decode, so draining tails can skip inactive physical rows. Mid-admission prefill still uses fixed logical-row `step_select`, so this tiny CPU fixture is dominated by unbatched admission overhead and remains below Stage A's one-shot prefill/full-N batch.

**Stage C contract:** The server/runtime should own one `ContinuousBatchManager` per compatible static-cache model/config, call `submit` as requests arrive, call `step` from the runtime loop while work exists, and drain `poll` for per-token streaming plus final results. PAUSED-on-tool should retain the logical row and generated state but mark it non-runnable; on resume, reactivate/requeue that row before stepping. True async channels, HTTP concurrency, and tool pause/resume state transitions are Stage C, not part of this batch.

---

### 2026-07-12T13:52:00-07:00: §26-COMPLETE multi-agent serving and engine decomposition
**By:** Scribe
**What:** DESIGN §26 Multi-Agent Serving is complete across all three stages, with the engine refactor landed first. Batty decomposed `engine.rs` from 3286 lines to a focused 1275-line orchestrator across `config`, `session`, `decode`, `decode_loop`, `kv_bridge`, `processors`, and `speculative`, introduced the `DecodeBackend` trait, and moved direct/session/priority/pipeline/speculative paths onto the shared decode loop. Sebastian delivered Stage A `Engine::generate_batched_static` fixed-batch STATIC-CACHE decode with batched results matching individual generation and a measured 6.2x tiny-fixture throughput gain, then Stage B `ContinuousBatchManager` with `submit`, `step`, `poll`, FIFO admission, finish/evict, and active-row stepping. Deckard delivered ORT active-row compaction for `BatchedStaticCacheDecodeSession` through `set_active_rows`, `compact`, `admit_row`/`assign_row`, `deactivate_row`, `step_active`, and slot diagnostics, saving about 50% compute at 2 active rows of 4. Rachael delivered Stage C server integration: one engine driver thread owns the Engine and `ContinuousBatchManager`, bounded channels replace the global generation mutex, concurrent STATIC-CACHE HTTP requests share batched forward passes, streaming and non-streaming responses receive token/final events, and tool turns, request caps, CSPRNG sessions, and past/present fallback remain preserved.
**Why:** §26's serving contract is now explicit and complete: compatible static-cache requests batch through `DecodeBackend`/`BatchedStaticCacheDecodeSession` and `ContinuousBatchManager`, the server serializes unsafe runtime ownership in one driver while exposing concurrent HTTP behavior, and `cargo test --workspace` stayed green throughout. Remaining serving-related work moves to §27/§28 advanced speculative paths, paged attention, and extensibility traits rather than baseline batched serving.

---

---

### 2026-07-12T13:52:00-07:00: Extensibility traits for speculation, sampling, and processors
**By:** Batty

**What:** Added the remaining DESIGN §25 internal seams while preserving the public generate flow and default behavior.

- `SpeculativeProposer` in `crates/onnx-genai-engine/src/speculative.rs` is the draft-source seam:
  - `propose(&mut self, &SpeculativeProposerContext) -> anyhow::Result<SpeculativeProposal>` returns linear candidate tokens today, with optional `positions`/`tree` fields reserved for tree/speculator layouts.
  - `accept(&mut self, &SpeculativeAcceptContext)` and `rewind(&mut self, target_tokens)` are lifecycle hooks for accepting/rewinding proposer state.
  - The existing draft-model speculative path is now `DraftModelProposer`, mapping the old greedy autoregressive `propose_draft_tokens` behavior onto `SpeculativeProposer::propose`; draft KV synchronization maps onto `rewind`.
- `Sampler` in `crates/onnx-genai-engine/src/sampling.rs` is the final token-selection seam:
  - `sample(&mut self, logits: &[f32], context: &ProcessorContext) -> TokenId` plus `name()`.
  - `GreedySampler` delegates to the existing argmax/tie-lowest `sample_greedy` behavior.
  - `CategoricalSampler` delegates to the existing deterministic-`rng_value` `sample_categorical` behavior.
  - Decode loops still select greedy vs categorical from unchanged `GenerateOptions`.
- Processor/constraint registration is exposed through `ProcessorChain::builder()`, `ProcessorChainBuilder::register_processor`, `register_constraint`, and `ProcessorChain::add_constraint`. Built-in processor construction and ordering remain unchanged: repetition/frequency/presence, stop/constraint, temperature, top-k, top-p, min-p.

**Why:** §27 MTP/Medusa/EAGLE/prompt-lookup proposers can now implement `SpeculativeProposer` and feed the same verify/commit loop without editing acceptance or target KV rewind logic. Custom samplers can implement `Sampler` and be wired into new decode paths without changing token commit behavior. Custom logit processors or constraints can be registered on a processor chain without forking built-ins, while the default chain remains byte-for-byte behavior-preserving.

**Validation:** `cargo check --workspace` and `cargo test --workspace` passed. Existing tiny fixture assertions remain unchanged: `tiny-llm` greedy `[22, 22, 20]`, `tiny-llm-scatter` greedy `[23, 15, 28]`, and draft-model speculative tiny-llm equals baseline greedy.

---

### 2026-07-12T13:58:00-07:00: Deterministic ORT threading for batched-vs-individual tests
**By:** Sebastian

**What:** Added an opt-in `SessionOptions::with_intra_op_threads(n)` builder and `Engine::from_dir_with_session_options(...)` so tests can load decode sessions with explicit ORT options. The Stage A fixed-batch and continuous-batch batched-vs-individual tests now construct both individual and batched engines with `intra_op_threads=1`.

**Why:** ORT CPU's default multi-threaded intra-op reductions can legitimately change floating-point reduction order between a batched forward and individual forwards. Near a logit tie, greedy argmax can pick a different token, making exact batched==individual assertions flaky under full parallel `cargo test --workspace`. Single-threaded ORT keeps reduction order deterministic for these correctness tests while leaving production defaults unchanged (`intra_op_threads=0`, ORT decides).

**Validation:** `cargo test -p onnx-genai-engine --test batched_static_decode` passed once, then passed 20/20 looped runs with 0 failures. `cargo test --workspace --quiet` passed 3/3 looped runs with 0 failures.

---

### 2026-07-12T14:10:00-07:00: Deterministic ORT comparison tests
**By:** Deckard
**What:** The real-model comparison tests in `crates/onnx-genai-ort/tests/decode_session.rs` now create sessions with `SessionOptions::default().with_intra_op_threads(1)`: `bound_decode_logits_match_naive_repass`, `bound_decode_rewind_matches_replay`, `static_cache_decode_reuses_buffers_and_rewinds_deterministically`, `batched_static_cache_matches_unbatched_rows_and_reuses_slots`, and `batched_static_cache_active_compaction_skips_inactive_rows_and_admits_replacement`. The test binary also shares one ORT `Environment`, matching ORT's one-environment-per-process guidance and avoiding concurrent environment initialization races.
**Why:** Single-threaded intra-op execution fixes ORT's floating-point reduction order so exact greedy-token comparisons across naive, bound, batched, unbatched, full-reference, and compacted forwards remain strong and deterministic. This setting is test-only; production session defaults remain multi-threaded/ORT-selected for performance. Validation completed with 20/20 consecutive `cargo test -p onnx-genai-ort` runs and 5/5 consecutive `cargo test --workspace` runs, with zero failures including the previously flaky active-compaction test.

---

### 2026-07-12T14:20:00-07:00: Model-free prompt-lookup speculative decoding
**By:** Batty

**What:** Added `NgramProposer`, a `SpeculativeProposer` that takes the last configured `ngram` tokens, searches backward for the most recent earlier occurrence, and proposes up to `max_tokens` from the continuation after that match. No match returns an empty proposal, so the shared greedy-speculative loop performs one normal target step. Added additive `SpeculativeMode::{None, DraftModel, PromptLookup { ngram, max_tokens }}` selection to `EngineConfig` and an optional per-request override in `GenerateOptions`; legacy `draft_model` configuration still selects `DraftModel`.

**Why:** Prompt lookup accelerates repetitive and structured generation without loading a draft model while retaining the existing target verification, correction-token, and KV rewind correctness path. Deterministic `tiny-llm` tests showed prompt-lookup output exactly equals plain greedy token-for-token. On repeated token context `[3,26,11,9,29,3,26,11,9,29]`, output matched baseline and recorded 2 accepted speculative tokens in one multi-token acceptance. `cargo check --workspace` and `cargo test --workspace` passed.

---

### 2026-07-12T14:28:00-07:00: §25/§27 extensibility, deterministic tests, and prompt-lookup milestone
**By:** Scribe
**What:** Recorded the current milestone set: Batty completed §25 extensibility seams (`SpeculativeProposer`, `DraftModelProposer`, `Sampler` with `GreedySampler`/`CategoricalSampler`, `ProcessorChain::builder`, `register_processor`, `register_constraint`, and `ProcessorChain::add_constraint`) while preserving facade behavior; Sebastian and Deckard made exact real-model comparison tests deterministic by running ORT test sessions with `intra_op_threads=1` and preserving production defaults; Batty added §27 model-free prompt-lookup speculative decoding through `NgramProposer` and `SpeculativeMode::{None, DraftModel, PromptLookup { ngram, max_tokens }}`.
**Why:** DESIGN §1-27 is now substantially implemented without changing default generation behavior. The deterministic-test convention keeps exact-equality real-model tests strong while avoiding ORT floating-point tie flakes: real-model exact-equality tests must set ORT `intra_op_threads=1`. Remaining §27 work requires special MTP/Medusa/EAGLE models, and §28 speculators/paged attention plus fixture/doc refresh remain open.
**Model policy:** Spawned agents should use task-appropriate models, with `gpt-5.5` as the floor and `gpt-5.6`/`opus-4.8` for harder work, per user direction.
---

### 2026-07-12T14:50:00-07:00: PUBLISHED to crates.io: onnx-genai v0.1.0 and seven sub-crates
**By:** Scribe
**What:** Published `onnx-genai v0.1.0` plus all seven sub-crates to crates.io: `onnx-genai-metadata`, `onnx-genai-kv`, `onnx-genai-scheduler`, `onnx-genai-ort`, `onnx-genai-ort-sys`, `onnx-genai-engine`, and `onnx-genai-server`. The release contract is `.github/workflows/publish.yml` using the protected `crates` environment and `CARGO_REGISTRY_TOKEN`; it publishes leaves-first, checks crates.io before each publish, skips versions already present, uses a User-Agent header for registry API calls, and is safe to re-run idempotently. Future releases are performed by bumping the workspace version and re-running the workflow.
**CI/Audit:** `.github/workflows/ci.yml` now runs fmt/build/test on push and PR; clippy runs non-blocking until warning cleanup makes `-D warnings` viable. `.github/workflows/audit.yml` runs weekly cargo-audit and on dependency changes; fresh `cargo audit` found 0 vulnerabilities. Audit code regularly via scheduled cargo-audit plus periodic review passes.
**Security:** Holden's review found two availability risks in the batched driver; Rachael hardened them with bounded active+pending admission (`max_pending`, HTTP 429 + `Retry-After`) and non-blocking driver delivery over bounded channels, dropping slow/closed clients instead of stalling the shared engine driver.
**Speculative runtime:** §27/§28 now includes working prompt-lookup n-gram speculation (`NgramProposer`, greedy-identical), `MtpProposer` and `SpeculativeMode::Mtp`, a full ignored tiny-MTP package/e2e fixture (`tiny-mtp-full`) that matches greedy, an EAGLE-3 fixture with proposer TBD, and speculator config auto-discovery for vLLM-style metadata. Remaining optional speculative work: EAGLE-3 proposer and optimized full-MTP hidden-output decode.
**Model policy:** Agents use task-appropriate models with `gpt-5.5` as the floor.
**Why:** The repository is now published, continuously validated, routinely audited, and has the complete runtime milestone set recorded in the canonical team log.
---

### 2026-07-12T14:50:00-07:00: Release and audit conventions
**By:** Scribe
**What:** Recorded repository conventions: publishing is via `.github/workflows/publish.yml` using the protected `crates` environment; bump the workspace version and re-run the workflow for future releases. Audit code regularly through scheduled cargo-audit plus periodic review passes.
**Why:** These contracts should be durable and visible to future agents before they change release or security workflows.

---

### 2026-07-12T15:52:00-07:00: MTP proposer through shared greedy verification
**By:** Batty
**What:** Added `MtpProposer`, a `SpeculativeProposer` backed by Deckard's `MtpDecodeSession`. Its proposal is linear: the target model's raw greedy next token is first and guaranteed, followed by up to `k` MTP draft tokens. Added `SpeculativeMode::Mtp(MtpConfig)` with the MTP-head ONNX path, target hidden-state output name, exact target embedding/LM-head f32 weight paths and dimensions, MTP KV mode, and speculative width. Engine loading validates the target hidden output and head hidden size, uses the output-preserving legacy target decode path, extracts the last target hidden state, and sends the proposal through the existing greedy verify/rewind/commit loop.
**Why:** MTP changes only candidate production; target verification remains the source of truth, so accepted output stays greedy-correct like draft-model and prompt-lookup speculation. Unit coverage loads `tests/fixtures/tiny-qwen35-mtp/model.onnx` with `intra_op_threads=1`, uses deterministic target projections, and verifies the guaranteed token plus four drafts `[27, 11, 2, 27]`; a separate trait/config test verifies `MtpProposer` conformance and `SpeculativeMode::Mtp` selection. A full Engine end-to-end test is deferred because the committed fixture is a head-only, hidden-threaded graph that cannot serve as the main decoder.

**End-to-end model contract:** A real package must provide (1) a target decoder that emits normal `logits` and a Float32 last-layer hidden-state tensor shaped `[1, sequence, hidden]` on every prefill/decode call, (2) an MTP head whose hidden size matches that output and whose cache mode matches `MtpDraftKvMode`, (3) the exact target token embedding `[vocab, hidden]` and shared LM-head `[hidden, vocab]` weights as little-endian f32 files, and (4) a tokenizer/vocabulary consistent with those matrices. Full heads may use `GrowCache`; the tiny fixture must use `HiddenThreaded`.

**Validation:** `cargo check --workspace` and `cargo test --workspace` pass.

---

# Deckard CI workflow

- Date: 2026-07-12T16:02:00-07:00
- Decision: Added `.github/workflows/ci.yml` for Rust quality gates on pushes to `main` and pull requests.
- CI job: single `ubuntu-latest` Rust job using `dtolnay/rust-toolchain@stable` with `rustfmt` and `clippy`, plus `Swatinem/rust-cache@v2`.
- Blocking steps: `cargo fmt --all -- --check`, `cargo build --workspace`, and `cargo test --workspace`.
- Clippy status: non-blocking. Local `cargo clippy --workspace --all-targets 2>&1 | tail -20` completes successfully with warnings, but enforcing `-D warnings` still exposes several pre-existing warnings beyond the trivial ones fixed here. The CI step still runs `cargo clippy --workspace --all-targets -- -D warnings` with `continue-on-error: true` and a TODO to make it blocking after baseline cleanup.
- Local validation: `cargo fmt --all -- --check` passes after formatting. `cargo test --workspace` passes.
- ORT in CI: no special environment is required for the default Linux job. `onnx-genai-ort-sys` auto-downloads the prebuilt ORT package during build with checksum verification; this requires normal network access and should benefit from cargo/build caching. A broader OS/toolchain matrix can be added later if needed.

---

### 2026-07-12T15:00:00-07:00: MTP ORT execution vertical slice (MtpDecodeSession)
**By:** Deckard
**What:** Built the ORT-level MULTI-TOKEN PREDICTION (MTP) execution slice in
`crates/onnx-genai-ort/src/mtp.rs` (additive; existing sessions untouched).
Batty wraps this as an `MtpProposer` next.

#### 1. Real MTP head I/O (fixture `tests/fixtures/tiny-qwen35-mtp/model.onnx`)

The fixture is **one ONNX graph = the MTP head only**. There is **no** main
decoder, **no** `logits` output, and **no** embedding/LM-head weights in it — the
target's shared embedding + LM head live outside the head graph (matches Pris's
report). Exact signature verified via ORT 1.27.0 (`Float32`, `com.microsoft`
Attention, opset 24):

Inputs:
- `inputs_embeds`           `f32 [B, T, 16]`   — embedding of the previous token
- `hidden_states`           `f32 [B, T, 16]`   — target/prev-step hidden state
- `attention_mask`          `i64 [B, P+T]`
- `position_ids`            `i64 [B, T]`
- `past_key_values.0.key`   `f32 [B, 1, P, 8]`
- `past_key_values.0.value` `f32 [B, 1, P, 8]`

Outputs:
- `mtp_hidden`              `f32 [B, T, 16]`
- `present.0.key`           `f32 [B, 1, P+T, 8]`
- `present.0.value`         `f32 [B, 1, P+T, 8]`

Config: `hidden=16, kv_heads=1, head_dim=8, layers=1, vocab=32`. Graph internals:
concatenates RMSNorm(inputs_embeds) ⊕ RMSNorm(hidden_states), projects via
`fc.weight_t [32,16]`, then one gated Qwen3.6 attention layer + SwiGLU MLP →
`mtp_hidden`. It applies **no** vocab projection.

#### 2. CRITICAL fixture limitation (differs from an ideal MTP head)

The tiny drafter's mask subgraph (Mobius `da92170`, the "materialized zero-dim
Reshape" workaround) is **degenerate**: it only executes for `past_len=0,
seq_len=1`. Every other `(P, T)` fails in the Attention node with
`inconsistent q_sequence_length (between attn_mask and Q)` (root cause:
`CumSum` axis 0 + cross-rank `Shape` subtraction that only collapses correctly at
size 1). I empirically mapped this: `(0,1)=OK`; `(0,2) (0,3) (1,1) (2,1) (1,2)
(3,1) = FAIL`. Position ids are inert in the `(0,1)` case (single-token
self-attention → rotation cancels).

**Consequence:** you cannot grow the head's KV across draft steps with THIS
fixture. Draft chaining threads state **through the `hidden_states` input** with
an **empty** past each step. Full-size Qwen3.6 MTP heads (whose mask supports
`P>0`) can instead grow the single-layer cache — the API supports both.

#### 3. Data flow (self-speculative MTP, k drafts)

```
target forward → target_hidden h_t, target_logits
 t0 = argmax(LM_head(h_t))           # GUARANTEED token (target's own next token)
 h = h_t ; prev = t0
 for i in 0..k:                      # k DRAFT tokens
     e   = embed(prev)              # target embedding table
     mtp = MTP_head(inputs_embeds=e, hidden_states=h, past=∅, pos=i)  # -> mtp_hidden
     ti  = argmax(LM_head(mtp))     # target shared LM head
     draft[i] = ti ; prev = ti ; h = mtp
```
The head consumes the target hidden + embedded token; its OUTPUT hidden feeds the
next step's `hidden_states`. LM head + embedding are the TARGET's (not in the
head graph).

#### 4. MtpDecodeSession API (`onnx_genai_ort::mtp`, re-exported at crate root)

```rust
// Introspection
MtpDecodeSession::detect(&Session) -> Result<Option<MtpHeadSignature>>
MtpHeadSignature { hidden_size, kv_heads, head_dim, layers, dtype }

// Construction
MtpDecodeOptions { kv_mode: MtpDraftKvMode, batch_size: i64 }   // default: HiddenThreaded, 1
enum MtpDraftKvMode { GrowCache, HiddenThreaded }
MtpDecodeSession::new(&Session, MtpDecodeOptions) -> Result<Self>

// State
fn signature(&self) -> &MtpHeadSignature
fn mode(&self) -> MtpDraftKvMode
fn past_len(&self) -> usize            // 0 in HiddenThreaded
fn reset(&mut self)
fn rewind(&mut self, target_len: usize) -> Result<()>   // verify/reject support

// Low-level: one head forward -> mtp_hidden (seq_len*H floats)
fn step(&mut self, inputs_embeds: &[f32], hidden_states: &[f32], position_start: i64)
    -> Result<Vec<f32>>

// High-level: chain k drafts from a target hidden + committed token
fn propose<E: TokenEmbedder, L: LmHead>(
    &mut self, target_hidden: &[f32], target_token: u32, k: usize,
    embedder: &E, lm_head: &L) -> Result<MtpProposal>

MtpProposal { guaranteed_token: u32, draft_tokens: Vec<u32>, draft_hiddens: Vec<Vec<f32>> }

// Target-side hooks (wrap the TARGET model's embedding table / shared LM head)
trait TokenEmbedder { fn hidden_size(&self) -> usize;
                      fn embed(&self, token: u32, out: &mut [f32]) -> Result<()>; }
trait LmHead { fn vocab_size(&self) -> usize;
               fn logits(&self, hidden: &[f32], out: &mut [f32]) -> Result<()>; }
fn argmax(&[f32]) -> Option<usize>
// Dense reference impls: LinearEmbedder([vocab,hidden]) / LinearLmHead([hidden,vocab])
```

- `propose` internally: `reset()`, then loops `embed(prev) → step → LM_head →
  argmax`, threading `mtp_hidden` into the next `hidden_states`.
- `step`/`propose` build `attention_mask` (ones) and `position_ids` themselves.
- KV binding uses `IoBinding`; `GrowCache` rebinds each `present.*` output as the
  next step's `past_key_values.*` input (zero-copy) like `DecodeSession`;
  `HiddenThreaded` binds an empty `[1,KVH,0,D]` past every step.

#### 5. How Batty should drive it (MtpProposer + verify loop)

1. Load the head model → `Session`; build `MtpDecodeSession::new(&head,
   MtpDecodeOptions::default())`. **Use `HiddenThreaded` for this fixture**
   (the default); switch to `GrowCache` only for full heads whose mask supports
   `P>0`.
2. Wrap the TARGET model's embedding table and shared LM head as `TokenEmbedder`
   / `LmHead` (or `LinearEmbedder`/`LinearLmHead` for tied-weight matrices). The
   target forward is your existing `DecodeSession`; take its committed token
   `t0 = argmax(target_logits)` and its hidden state `h_t`.
   - NOTE: the target model must EXPOSE a hidden-state output for `h_t`. The tiny
     `tiny-llm` fixture does not; wire this when the real target exports hidden
     states (out of scope here — `mtp.rs` is head-only per task boundary).
3. Per target step: `propose(&h_t, t0, k, &embedder, &lm_head)` → `k` draft
   tokens. Feed `[t0, draft_0..draft_{k-1}]` to the target as the speculative
   window.
4. Verify: run the target over the window, accept the longest matching prefix.
   On rejection at position `j`, call `session.rewind(j)` (GrowCache) to drop
   rejected head KV, or just re-`propose` from the new committed hidden
   (HiddenThreaded). `draft_hiddens` are exposed if the verifier wants to reuse
   drafted hidden states.

#### 6. Real-fixture run result (validated)

Test `crates/onnx-genai-ort/tests/mtp_session.rs` (4 tests, all green;
`intra_op_threads(1)` for determinism):
- `detects_mtp_head_signature`: `{hidden:16, kv_heads:1, head_dim:8, layers:1,
  Float32}`.
- `single_step_produces_hidden_state`: one head forward → finite `mtp_hidden[16]`.
- `proposes_k_draft_tokens_from_real_fixture`: with deterministic synthetic
  target LM head/embedding, `guaranteed_token=13`, `k=4` drafts
  `[27, 11, 2, 27]`, reproducible across two runs; guaranteed token is a stable
  argmax of the target hidden.
- `manual_chain_matches_propose`: low-level `step` loop reproduces `propose`.

`cargo check --workspace` + `cargo test --workspace` green.

**Why:** MTP is the smallest useful `SpeculativeProposer` addition (Pris). This
slice gives Batty a self-contained, target-agnostic head runner so the engine
only supplies the target embedding/LM head + hidden state and drives
accept/rewind.

---

### 2026-07-12T15:20:00-07:00: crates.io publication preparation
**By:** Deckard
**What:** Prepared all eight workspace crates for crates.io with per-crate descriptions, keywords, and categories plus workspace-inherited MIT license and repository metadata. Every internal workspace dependency now has `version = "0.1.0"` alongside its path, and member crates consume internal dependencies through `workspace = true`.

The confirmed publish order is:

1. `onnx-genai-ort-sys`
2. `onnx-genai-metadata`
3. `onnx-genai-kv`
4. `onnx-genai-scheduler`
5. `onnx-genai-ort`
6. `onnx-genai-engine`
7. `onnx-genai`
8. `onnx-genai-server`

`cargo publish --dry-run` passed for the two true leaf crates, `onnx-genai-ort-sys` and `onnx-genai-metadata`. The sys crate packaged 6 files (23.1 KiB) and verified successfully; its build script downloads ONNX Runtime 1.27.0 when no local installation is configured and validates the platform archive against a pinned SHA-256 checksum. Metadata packaged 12 files (38.2 KiB) and verified successfully.

The six dependent crates passed `cargo package --list`, confirming their intended package file sets. Full `cargo package`/publish dry-runs cannot succeed until their versioned internal dependencies exist in crates.io; Cargo reports the expected “no matching package named `onnx-genai-metadata` found” starting with `onnx-genai-kv`. Run full dry-runs in dependency order after each prerequisite becomes visible in the registry.

Added `.github/workflows/publish.yml`, triggered manually or by a `v*` tag. It uses the protected `crates` environment and `CARGO_REGISTRY_TOKEN`, publishes in dependency order, skips versions already present through the crates.io API, retries failed publishes up to six times, and waits 30 seconds after successful/observed publication for index propagation.

Validation passed with `cargo check --workspace` and `cargo test --workspace`.

Trigger manually with `gh workflow run publish.yml --ref main`, or push a version tag such as `v0.1.0`.

**Why:** crates.io requires complete package metadata and registry-compatible versions for path dependencies. Dependency-ordered, retrying, existence-guarded publication makes the release workflow repeatable while accommodating crates.io index propagation and avoiding failures when a version was already published.

---

### 2026-07-12T14:48:00-07:00: Speculator model auto-discovery
**By:** Deckard
**What:** Added typed `SpeculatorConfig`, `SpeculatorVerifier`, and forward-compatible `ProposalType` parsing for EAGLE-3, P-EAGLE, MTP, DFlash, and unknown proposal types. `detect_speculator` prefers the native `inference_metadata.{yaml,yml,json}` `speculative` section (with `method`/`tokens_per_step` compatibility aliases), then best-effort falls back to HuggingFace `config.json.speculator_config`. `ModelDirectory::load` exposes the resulting `SpeculatorDescriptor`, including source, verifier identity, proposal token count, and `SpeculatorProposerStatus`. Known proposal types map to an explicit `NotYetSupported` proposer kind rather than failing; unknown values are preserved.
**Why:** Discovery and configuration plumbing should be additive and should keep this project's inference metadata authoritative while interoperating with vLLM Speculators packages. To execute these models next, the engine needs EAGLE-3/P-EAGLE/MTP/DFlash implementations of `SpeculativeProposer`, ONNX input/output binding for verifier hidden states and speculator state, model/session loading, vocabulary mapping where present, and wiring from `SpeculatorProposerKind` into proposer construction and the existing verify/accept/rewind loop.

---

### 2026-07-12T14:36:00-07:00: Recurring RustSec audit and fresh security review
**By:** Holden
**What:** Added `.github/workflows/audit.yml`. It runs every Monday at 06:00 UTC, on pushes changing `Cargo.lock` or any `Cargo.toml`, and by manual dispatch. The job installs locked `cargo-audit` and runs the default `cargo audit` policy: vulnerabilities fail the job, while informational warnings do not. No advisory ignores are configured.

The fresh local `cargo audit` pass scanned 216 dependencies and found 0 vulnerabilities. It reported two allowed unmaintained warnings already present through `tokenizers`: `number_prefix` (`RUSTSEC-2025-0119`) and `paste` (`RUSTSEC-2024-0436`).

Focused review findings:

- **Medium — unbounded queued generation can exhaust server memory:** `crates/onnx-genai-engine/src/batched.rs:99,171-177` stores pending requests in an unbounded `VecDeque`, while `crates/onnx-genai-server/src/lib.rs:260-269` continuously drains the bounded command channel into it. Sustained requests faster than inference can retain arbitrarily many prompts and response routes. Fix: enforce a total active-plus-pending request limit and reject excess submissions with 429/503; do not drain more commands than the manager's bounded capacity.
- **Medium — a slow SSE client can stall the single global engine driver:** `crates/onnx-genai-server/src/lib.rs:303-317` uses blocking sends from the sole driver thread to per-request bounded channels. A client that stops consuming without disconnecting can fill the SSE/output buffers (`lib.rs:1139,1151-1167`), block its route, and halt generation and command handling for every client. Fix: make event delivery non-blocking or isolate per-client forwarding; cancel/drop a request whose output queue is full or receiver is slow.

The MTP path in `crates/onnx-genai-ort/src/mtp.rs` contains no direct unsafe block and consistently routes tensor creation, shape validation, binding, and ownership through the checked ORT wrappers; no high-confidence FFI memory-safety issue was found. `ContinuousBatchManager` had no attacker-reachable panic identified beyond its resource-bound issue. The publish workflow only runs on trusted tag pushes/manual dispatch, uses `environment: crates`, read-only repository permission, and does not expose the token in logs; no high-confidence token leak or untrusted-PR execution path was found.

**Why:** Published crates need continuing RustSec coverage rather than a one-time audit. The recurring job catches newly disclosed dependency vulnerabilities, while the focused review identifies two availability risks introduced by the single-owner continuous-batch server design without changing source code outside the requested workflow.

---

### 2026-07-12T14:48:00-07:00: Advanced model feasibility is MTP/EAGLE yes, paged-KV no
**By:** Pris
**What:** Investigated Mobius `da92170` and built two deterministic tiny ONNX fixtures.

- **MTP — FEASIBLE as a separate Qwen3.6 self-speculative head, not as fused k+1
  logits from one target forward.** Mobius exposes the task name `qwen35-mtp`
  (`Qwen35MtpTask`) and model API
  `build_from_module(Qwen35MtpModel(config), config, task=Qwen35MtpTask())`.
  The main Qwen3.6 checkpoint does not auto-route to the auxiliary head; Mobius'
  golden harness forces registry architecture `Qwen35MtpModel`.
  Inputs are `inputs_embeds [B,T,H]`, `hidden_states [B,T,H]`,
  `attention_mask [B,P+T] int64`, `position_ids [B,T] int64`, and
  `past_key_values.0.{key,value} [B,KVH,P,D]`. Outputs are
  `mtp_hidden [B,T,H]` and `present.0.{key,value} [B,KVH,P+T,D]`.
  There is deliberately no `logits` output and no bank of k extra prediction
  heads: the runtime must apply the target's shared LM head to `mtp_hidden`,
  select a token, embed it with the target embedding, and call the MTP head
  autoregressively for each proposed token.

- **EAGLE-3 — FEASIBLE.** Mobius exposes `--task eagle3-draft` /
  `Eagle3DraftTask`; supported architecture names auto-route through
  `Eagle3LlamaForCausalLM`, `LlamaForCausalLMEagle3`, `Eagle3Speculator`, or
  `Eagle3DraftModel`. Inputs are `inputs_embeds [B,T,H]`,
  `fused_hidden [B,T,3H]`, `recycled_hidden [B,T,H]`,
  `attention_mask [B,P+T] int64`, `position_ids [B,T] int64`, and
  `past_key_values.0.{key,value} [B,KVH,P,D]`. Outputs are
  `draft_logits [B,T,draft_vocab]`, `next_hidden [B,T,H]`, and
  `present.0.{key,value} [B,KVH,P+T,D]`. The target must export the three
  checkpoint-selected auxiliary hidden states (Mobius supports target
  `output_layer_indices` -> `hidden_states.{idx}` outputs), and the orchestrator
  must retain the checkpoint's `d2t` draft-to-target vocabulary map; Mobius
  currently drops `d2t`/`t2d` and has a TODO for an ORT-GenAI sidecar.

- **Paged/block KV — NOT FEASIBLE in Mobius.** There is no paged/block-table
  task, CLI flag, `block_table`, `slot_mapping`, page-size, or page-index input.
  The only non-growing cache option is `--static-cache [--max-seq-len N]`,
  which emits contiguous `key_cache.{i}` / `value_cache.{i}`
  `[B,max_seq_len,KVH*D]`, shared `write_indices [B]`,
  `nonpad_kv_seqlen [B]`, and contiguous
  `updated_{key,value}_cache.{i}` outputs via TensorScatter. True paged
  attention requires a new Mobius task/attention lowering with physical page
  tensors plus block-table/slot-mapping inputs and a runtime/EP kernel that
  gathers logical sequence positions from non-contiguous pages.

Built fixtures:

- `tests/fixtures/tiny-qwen35-mtp/`: 27,546-byte ONNX + 12,288-byte data.
- `tests/fixtures/tiny-eagle3/`: 16,731-byte ONNX + 15,360-byte data.
- Repro: `scripts/build_tiny_advanced_drafters.py`, using Mobius,
  `onnxscript`, and `onnx_ir` (never `onnx.helper`).

Both fixtures load and execute a one-token, empty-past CPU forward under
ONNX Runtime 1.27.0 with finite outputs. The repro script materializes known
static Reshape dimensions because this Mobius revision serializes several known
dimensions as invalid zero copy-placeholders in these tiny drafter graphs.

**Why:** Implement **MTP next**. It is the smallest useful addition to the
existing `SpeculativeProposer` seam: one target hidden-state output, one
single-layer drafter cache, and reuse of the target embedding/LM head. EAGLE-3
is buildable but requires three target feature outputs, draft-vocabulary remap
artifact handling, and more orchestration state. Paged KV is blocked on model
builder/operator support rather than engine-only work.

---

### 2026-07-12T16:02:00-07:00: Tiny full MTP package runs end to end
**By:** Pris
**What:** Mobius has no CLI `include_hidden_states` / `return_hidden` flag and no
single task that emits a target decoder plus Qwen3.5 MTP head. Its programmatic
API can build the package as two graphs. `ArchitectureConfig.output_layer_indices`
causes `CausalLMTask`/`HybridCausalLMTask` to name auxiliary outputs
`hidden_states.{layer}`, but the stock Qwen3.5 text model does not implement that
capture and the generic capture is pre-final-norm rather than the post-final-norm
tensor required by MTP. The reproducible fixture therefore uses
`build_from_module` twice: a tiny `GPT2CausalLMModel` subclass returns its
post-final-norm hidden as the task's auxiliary `hidden_states.0`, and
`Qwen35MtpModel` is built with `Qwen35MtpTask`. Both use hidden=16/vocab=32, and
the target's exact embedding and transposed LM-head matrices are exported as
little-endian f32 files. Repro:
`PYTHONPATH=/Users/justinc/Documents/GitHub/mobius/src python scripts/build_tiny_mtp_full.py`.

Layout (79,611 bytes total):
- `tests/fixtures/tiny-mtp-full/model.onnx{,.data}`: target decoder.
- `tests/fixtures/tiny-mtp-full/mtp/model.onnx{,.data}`: Qwen3.5 MTP head.
- `embedding.f32` `[32,16]`, `lm_head.f32` `[16,32]`, tokenizer and manifest.

Target I/O:
- inputs: `input_ids i64 [B,S]`, `attention_mask i64 [B,P+S]`,
  `position_ids i64 [B,S]`, `past_key_values.0.{key,value} f32 [B,2,P,8]`.
- outputs: `logits f32 [B,S,32]`, `present.0.{key,value} f32 [B,2,P+S,8]`,
  `hidden_states.0 f32 [B,S,16]`.

MTP-head I/O:
- inputs: `inputs_embeds f32 [B,S,16]`, `hidden_states f32 [B,S,16]`,
  `attention_mask i64 [B,P+S]`, `position_ids i64 [B,S]`,
  `past_key_values.0.{key,value} f32 [B,1,P,8]`.
- outputs: `mtp_hidden f32 [B,S,16]`,
  `present.0.{key,value} f32 [B,1,P+S,8]`.

`crates/onnx-genai-engine/tests/mtp_full.rs` is ignored by default and runs the
complete target-hidden -> MTP proposal -> target verification loop. Explicit
execution passed and matched plain greedy exactly; `cargo test --workspace`
also passed. Use `MtpDraftKvMode::HiddenThreaded` because the current tiny
Qwen3.5 head's materialized-mask workaround only supports empty-past,
single-token calls.

No engine/ORT source change is required for correctness: Batty's legacy
output-preserving target path already extracts `hidden_states.0`, and Deckard's
head runner consumes it. The optimized `DecodeSession` runner still cannot
surface auxiliary hidden outputs; enabling optimized MTP later requires binding
a configured named hidden output alongside logits, returning its last
`[B,S,H]` row from the runner, and removing the guard in
`next_session_token_logits_and_hidden`.
**Why:** This is the smallest committed, deterministic package that exercises
the real MTP orchestration contract while staying far below the fixture size
limit. A stock full Qwen3.5 target is not currently obtainable through a Mobius
flag alone, and its tiny portable Attention mask has the same non-unit sequence
failure as the existing head, so GPT-2 is used only as the verified target
backbone for runtime integration coverage.

---

### 2026-07-12T14:38:00-07:00: ONNX fixture builders use onnxscript
**By:** Pris
**What:** Converted `scripts/build_tiny_vlm.py` from `onnx.helper` to direct `onnxscript.ir` graph construction. Confirmed `tests/fixtures/tiny-llm/generate_tiny_llm.py` and `scripts/build_tiny_scatter.py` already build through Mobius' onnxscript-based builder, then regenerated both committed fixture directories with their fixed seed. All regenerated tiny-LLM, tiny-scatter, and tiny-VLM ONNX files were byte-identical to their prior outputs, so no golden token assertions changed. The static-cache signature remains `key_cache.0`, `value_cache.0`, `write_indices`, and `nonpad_kv_seqlen`, with the same updated-cache outputs.
**Why:** Repository model-building Python must use onnxscript/onnx-ir rather than low-level `onnx.helper`. `cargo test --workspace` and the ignored tiny-VLM integration tests passed; deterministic decode and batched-static tests also passed 5/5 loops, and a repository-wide Python grep found no remaining `onnx.helper`, `helper.make`, or `from onnx import helper` usage.

---

### 2026-07-12T14:44:00-07:00: Bound generation admission and non-blocking stream delivery
**By:** Rachael
**What:** Added configurable `max_pending` generation admission to `onnx-genai-server`, covering active plus queued requests. Requests beyond the cap are rejected with HTTP 429, a clear capacity message, and `Retry-After: 1`. Per-request driver output remains bounded, and the sole engine driver now uses `try_send` exclusively: a full or closed route is dropped immediately, its static-batch row is drained without further delivery until the manager retires it, and its admission permit remains held until retirement. Fallback generation aborts immediately when delivery cannot proceed.
**Why:** A semaphore prevents the continuous-batch pending queue from growing without bound. Non-blocking delivery prevents a slow or disconnected SSE consumer from stalling the shared driver, so other rows continue stepping and completing while abandoned work remains resource-accounted.

---

### 2026-07-12T14:28:00-07:00: Refresh user documentation for completed runtime features
**By:** Roy
**What:** Rewrote `README.md` to describe the verified Phase 1-3 completion and substantially complete Phase 4 feature set, architecture, limitations, security posture, and usage. Verified documented commands and routes: `scripts/build_qwen.sh` with optional `STATIC_CACHE=1 MAX_SEQ_LEN=...`; `onnx-genai generate --model ... [--max-new-tokens|--temperature|--top-p|--top-k|--stop|--stream]`; `onnx-genai-server --model ... [--model-id|--addr|--max-output-tokens|--max-sessions]`; `GET /health`, `GET /v1/models`, `POST /v1/chat/completions`, `POST /v1/sessions`, `DELETE /v1/sessions/{id}`; and `ONNX_GENAI_EP=cpu|webgpu|coreml`.
**Why:** The prior README still reported Phase 1 as in progress and omitted the implemented decoding, serving, agent, long-context, pipeline, batching, security, and execution-provider capabilities. The refresh distinguishes implemented storage paging from future paged-attention kernels and lists remaining §27/§28 speculators, rejection sampling, and automatic hardware matching to avoid overclaiming.
