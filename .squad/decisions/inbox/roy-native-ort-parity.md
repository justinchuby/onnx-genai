### 2026-07-23: Real-weight native CUDA decode is faithful to the trusted reference
**By:** Roy
**What:** Ran 128-token greedy decode through the same `profile_native` engine loop for native CUDA and ORT CUDA on Phi-4-mini and Qwen2.5 0.5B/1.5B/7B real weights. The prompt was:

> Explain why deterministic greedy decoding is useful when validating a new transformer inference backend. Include concrete numerical failure modes and how you would distinguish harmless floating-point near-ties from implementation bugs.

GPU 0 was idle (0 MiB, 0% utilization) before the runs. GPU 1 was occupied by VLLM (129,465 MiB); it was avoided. No contention was observed on GPU 0. Qwen runs used `ONNX_GENAI_CUDA_GRAPH=0` for both backends because ORT CUDA graph capture failed on the 0.5B package with `ort_value must contain a constructed tensor`; this only disables capture and does not change the model math. The 0.5B native sequence was also identical with capture enabled.

| Model | Native/ORT common prefix | Same IDs at aligned positions | First divergence (0-based) | First IDs (native / ORT CUDA) | Classification | Evidence |
|---|---:|---:|---:|---|---|---|
| Phi-4-mini | 128/128 | 128/128 | none | — | ✅ exact parity | All 128 generated IDs identical. |
| Qwen2.5-0.5B | 128/128 | 128/128 | none | — | ✅ exact parity | All 128 generated IDs identical. |
| Qwen2.5-1.5B | 22/128 | 23/128 | 22 (token 23) | 81917 / 22406 | ✅ native matches the exact-Q4 independent f32 oracle | At the split, native top-2 logprobs were `81917=-2.18533`, `22406=-2.20095` (gap 0.015625). ORT CUDA rounded them to an exact tie at `-2.19943` and selected 22406. Independent PyTorch f32 using the **exact deployed ONNX block-32 int4 weights dequantized to f32** selected native's 81917 with logits `19.60641` vs `19.53702` (gap 0.06939). Dense unquantized HF weights instead select 22406 by 0.68665; that is a quantization-induced model difference, not a valid oracle for the deployed Q4 graph. |
| Qwen2.5-7B | 19/128 | 20/128 | 19 (token 20) | 2797 / 12966 | ✅ native matches both independent f32 oracles | Native CUDA top-2 were `2797=-1.15276`, `12966=-1.32464`. Independent PyTorch f32 with exact dequantized ONNX Q4 weights selected 2797 with logits `22.18464` vs `22.00753` (gap 0.17711), closely reproducing native's 0.171875 margin. Dense HF f32 also selected 2797 (`22.20324` vs `21.45139`). ORT CUDA instead selected 12966. |

**Why:** Native CUDA is token-exact with ORT CUDA on Phi-4-mini and Qwen2.5-0.5B. At the two observed first-divergence steps only—generated-token index 22 (token 23) for Qwen1.5B and index 19 (token 20) for Qwen7B—an independent PyTorch implementation executing the exact deployed quantized weights in float32 selects native CUDA's token. The oracle assumes the artifacts' symmetric block-32 int4 layout (implicit zero-point 8, per-block scales). The Qwen1.5B dense-HF disagreement was root-caused to comparing different models: the deployed ONNX package is Q4, while the HF checkpoint is dense. These results establish native's faithfulness at the measured divergence points, not a blanket claim for every possible position or prompt.

**Regression lock:** `scripts/check_native_ort_parity.py` runs all four native/ORT CUDA cases against `tests/parity/native_ort_cuda_golden.json`. Phi/0.5B require exact 128-token equality. Qwen1.5B/7B lock the exact observed first-divergence index, both 128-token backend goldens, and require native's token at that index to equal the exact-Q4 f32 oracle. `scripts/qwen_q4_f32_oracle.py` independently regenerates the f32 oracle from the committed teacher-forced prefixes; use `--weights hf-dense` to demonstrate the dense/Q4 distinction.

**Validation:** Built with `cargo build --release -p onnx-genai-bench --features bench-native,cuda --bin profile_native`; the four-model parity harness passed. Native trace through the Qwen7B split exercised fp16/int4 decode (`gemv_f16_general`, `attention_gqa_decode_fp16_splitk`, `skip_rmsnorm_f16_warp_half4`, `gate_up_swiglu_fused`, `gemv_f16_down_projection`).
