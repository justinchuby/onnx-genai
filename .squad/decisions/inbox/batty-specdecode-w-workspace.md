### 2026-08-15: Keep speculative cold prefill workspace prompt-shaped
**By:** Batty
**What:** Fixed captured speculative decode's W-dependent qwen divergence by stopping cold generation workspace preparation from widening to the speculative verify width. `native_workspace_query_rows` now prepares only the prompt-shaped prefill workspace; the width-W GQA workspace reservation remains at the actual captured-verify call site (`run_verify_captured_cuda`) after M=1 decode bindings have been restored.
**Why:** Localization showed W=9 divergence reproduced with `spec_ngram=99` (`verify_steps=0`, `proposed=0`, `accepted=0`), while the first actual captured row-0 at W=9 matched a fresh M=1 decode (`argmax=104887`, margin `1.375000` for both). The corrupting layer was therefore not acceptance or captured verify logits: pre-reserving the cold GQA workspace at W leaked a width-dependent layout into later M=1 fallback decode. Before the fix qwen `哈`×20 first failed at W=7 and W=8/W=9 diverged at token index 2; after the fix W=5/6/7/8/9 all match plain greedy for 160 tokens. GLM generic stayed byte-identical, and GLM repetitive stayed byte-identical with a preserved speculative win (`100.0%` acceptance, `197.96 tok/s` vs `111.94 tok/s` plain in the local run).
