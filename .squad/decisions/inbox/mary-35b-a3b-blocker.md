### 2026-07-30: Qwen3.6-35B-A3B native CUDA enablement
**By:** Mary
**What:** Mobius exported the exact `Qwen/Qwen3.6-35B-A3B` checkpoint (`995ad96e`) as a 66 GiB fp16 three-component package, then Olive Q4_K_M quantized its decoder to 19.8 GB (`31,111` `MatMulNBits` nodes), producing a 21 GiB package at `/home/justinchu/mary-models/qwen3.6-35b-a3b-int4-cuda`. The unmodified export is invalid: each vision `PackedMultiHeadAttention` call supplies six inputs while its model-local function signature declares five; a probe overlay inserts the omitted optional `attention_bias` formal to continue admission. The first decoder CUDA blocker was `TopK` rejecting fp16 router scores at all 40 MoE layers. CUDA `TopK` now supports fp32, fp16, and bf16 with deterministic CPU parity, clearing all decoder CUDA placement failures. The next execution blocker is the existing backend-neutral pipeline gap: all three native component sessions load, but `PipelineDecodeLoopBackend` still owns ORT values/sessions and cannot invoke native every-step embedding + decoder components. Strict whole-package CUDA additionally rejects 27 vision `Attention` nodes whose floating masks do not match Q/K/V dtype.
**Why:** The model currently exports `mixture_of_experts.representation: dense_fallback`, expanding 256 experts per layer into 117,505 decoder nodes rather than a route-first/routed-expert operator; it therefore does not exercise issue #82's durable routed-expert contract or #63 expert offload. The fp16 router `TopK` support is a small general CUDA gap and is fixed independently. A native decode step remains blocked by the larger pipeline ownership refactor, while efficient MoE execution requires a Mobius routed-expert representation plus the issue #82 kernel contract. Exact repro: `CUDA_VISIBLE_DEVICES=4 ONNX_GENAI_REQUIRE_CUDA=1 ./target/release/profile_native --pipeline --model /home/justinchu/mary-model-overlays/qwen36-35b-a3b-native-probe --ep cuda --backend native --tokens 1 --warmups 0 --runs 1 --prompt 'The capital of France is'`.
