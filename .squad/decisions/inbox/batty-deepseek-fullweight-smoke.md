### 2026-07-23: Validate full-depth DeepSeek-V2-Lite QMoE without local source weights
**By:** Batty
**What:** The local Hugging Face snapshot contains only `config.json` and `configuration_deepseek.py`; no safetensors, index, tokenizer, or other source weights are present, and Foundry has no DeepSeek-V2-Lite checkpoint. I therefore exported the maximal faithful 27-layer real-config f16/int4-QMoE artifact with deterministic synthetic weights at `/home/justinchu/ds-e2e-artifacts/deepseek-v2-lite-full-f16/` from Mobius `bd88fa8`. Its graph has 26 QMoE, 27 Attention, 0 GroupQueryAttention, 27 MatMul, and 189 MatMulNBits nodes, with FC1 `[64,2816,1024]`, FC2 `[64,2048,704]`, and no per-expert MatMul expansion. Strict native CUDA generated 32 finite tokens with zero fallbacks at 26.66 tok/s; ORT GenAI rejects the `deepseek_v2` architecture, so this remains native-only.
**Why:** This exercises the int4-QMoE emitter and f16 router fix at the production 27-layer depth and real DeepSeek-V2-Lite geometry while accurately distinguishing full-depth synthetic validation from unavailable full checkpoint-weight validation.
