### 2026-08-18: DeepSeek-V2-Lite mask query axis resolves exactly for CUDA-graph prepare
**By:** Leon
**What:** Extended prepare-only workspace shape recovery through the deterministic DeepSeek-V2 additive-mask cone (`CumSum/Shape/Sub/Slice/Unsqueeze/GreaterOrEqual/And/Where/Cast`) so `v_model.Unsqueeze_18` resolves as `[batch, 1, sequence_len, past_seq_len + sequence_len]`. The `_d1` query axis is recovered exactly from `Shape(attention_mask)[1] - Shape(input_ids)[1]` slicing the mask CumSum, not bounded by a max-sequence guess.
**Why:** Deckard's topology-gated mask-capacity classifier made real V2-Lite capture eligible, but prepare-only workspace planning still failed on `_d1` for default-domain `Attention` input 3. Exact recovery avoids the previous under-reservation class while keeping dense planning unchanged: Qwen3 stayed `persistent=983040 step=0 returned=983040` and passed its CUDA golden lock. Real V2-Lite CUDA graph now captures and replays with byte-identical greedy tokens vs eager for the 128-token validation run; median decode improved from 57.38 tok/s eager to 101.76 tok/s captured on H200 GPU0 (`captures=4 replays=504 fallbacks=0`).
