### 2026-08-17: DeepSeek-V2-Lite MoE planner gate unblocked
**By:** Leon
**What:** Native prepare-only workspace planning resolves exact runtime shapes through statically deterministic `Reshape([-1, hidden]) -> Cast` producer chains before reserving planned MatMul workspace. Fresh origin/main already bounded DeepSeek-V2 Attention mask/KV axes by KV capacity; this follow-up handles the MoE gate flatten (`v_model.layers.*.mlp.moe.Cast_*`) so the real int4 QMoE artifact reaches native CUDA decode.
**Why:** The flattened MoE gate dimension is `batch * sequence`; shape inference lowers it to a derived symbol that is not directly bound as a graph input. Treating it as data-dependent blocked V2-Lite even though its extent is exactly recoverable from current bound inputs and the constant reshape target. Dense Qwen3 planning did not use the new recursive recovery and retained byte-identical workspace reservation (983,040 B). Numeric gate remains separate: native CUDA currently diverges from native CPU at generated token index 5 on the real V2-Lite artifact, so QMoE/numerics review is still required before treating the model as correctness-validated.
