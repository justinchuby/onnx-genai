### 2026-07-23: Approve 64-expert/top-6 QMoE CUDA parity coverage
**By:** Chew
**What:** 🟢 APPROVE — the added 64-expert/top-6 fp16/bf16 decode, prefill, empty/hot routing, changed-route replay, and scratch-sizing tests are meaningful CUDA-vs-CPU parity coverage. Independent reproduction on GPU 0 passed: `qmoe_gpu` 27/0 and CUDA library gate 192/0.
**Why:** The expected output is produced by `onnx-runtime-ep-cpu`'s `QMoEKernel`, which host-dequantizes experts and executes grouped CPU work, rather than CUDA's device route/group/gather/GEMV/GEMM/combine path. The synthetic default router activates 37 of 64 experts for the 8-row prefill case (27 empty); its 64-row scratch case activates all 64 (3–8 routes/expert). The dedicated hot case sends 16 routes to each of experts 0–5 and none to the other 58, so empty and multi-route expert paths are exercised. fp16 tolerance is abs 2e-5 + rel 6e-4, and bf16 is abs 2e-5 + rel 4.1e-3: approximately bf16's 0.39% quantization scale, not suspiciously loose. The `Case` and `qmoe_64expert_case` helpers parameterize expert count/top-k; the new cases are not copy-pasted.
