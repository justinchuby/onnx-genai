### 2026-08-28: DFlash uses an exact flat-block structural contract
**By:** Sapper
**What:** `dflash_flat_block` version 1 declares base target-hidden-conditioned parallel drafting; version 2 exclusively declares selector/convolution semantics. Proposal probabilities, shared target initializers, verifier logits, and every accepted-prefix state participant are explicit. Shared proposal batching is currently declined before execution and rows use the isolated path.
**Why:** DFlash must stay independent from token-context, MTP, prompt lookup, and candidate trees. Exact versions and one rollback participant set prevent optional tensors, names, or model-family identity from silently selecting semantics.
