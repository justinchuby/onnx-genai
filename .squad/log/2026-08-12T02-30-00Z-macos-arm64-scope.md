# Session Log — macOS arm64 scope correction

**Timestamp:** 2026-08-12T02:30:00Z
**Requested by:** @justinchuby

## Summary

Both open Apple PRs rescoped to macOS arm64 / Apple Silicon only.

- **PR #31993** (Mariette): Removed x86_64 Apple test slice. ARM64 gate and dispatch assertions unchanged. Head `68ee0de`.
- **PR #32001** (Coco): Added arm64 `elseif` condition. warn-and-disable on non-arm64 Apple. Head `52db6351b5`.

Coordinator independently verified both.

Both PRs remain draft.

## Durable policy recorded

`decisions.md` updated with standing constraint: Apple scope = macOS arm64 only. Intel Mac, universal2, and iOS are out of scope unless separately justified. Cross-referenced with earlier Accelerate/BNNS/vDSP eligibility entry.
