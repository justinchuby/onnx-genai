# Whole-step capture — 2026-07-24T05-11-20+0000

DeepSeek-V2-Lite int4 decode CUDA-graph capture reached **whole-step capture** on main: 727 eager seams → 0, with one captured segment. `661618b8` folded Reshape seams (275→7) and `3dc0843b` closed the mask island (7→0). Combined verification preserved deterministic coherent output and passed the CUDA library gate (205/0).

## Known cleanup item

`.squad/decisions/inbox/rachael-mask-island-closure.md`, and possibly Sebastian's and Tyrell's decision records, leaked onto the `main` source branch in merged PRs. They are harmless stray records but pollute the source tree. The authoritative inbox is on the state branch; do not delete the source-branch copies as part of this session.
