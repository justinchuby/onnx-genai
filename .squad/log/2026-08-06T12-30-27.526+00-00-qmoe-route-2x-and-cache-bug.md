# Session — QMoE route 2× and cache bug

- PR #684 merged: native CUDA `qmoe_route` now uses a block-cooperative top-k reduction instead of row-serial decode routing. Throughput improved 30.99 → 16.14 ms/tok (1.92×, ~62 tok/s) with byte-exact output.
- PR #692 merged: the #676 oracle test now uses a fresh engine for teacher-forced logits. This preserves the oracle token 33803 and avoids reused hybrid-Mamba state contamination.
- Issue #695 filed: hybrid-Mamba continuation cache reuse restores attention KV but not Mamba conv/recurrent state, producing wrong logits on reused-prefix continuations.
- Issue #610 scorecard updated for the new 35B-A3B sparse-QMoE result: roughly 24× over dense and ~28.6× over the ORT-GenAI dense-fallback ceiling.
