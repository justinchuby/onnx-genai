# 27B blocker chain and CUDA performance batch

Mary's Qwen3.6-27B INT4 native-CUDA chain cleared fixed recurrent state allocation (#436) and rank-3 Conv1D (#438); Microsoft Silu shape inference (#440) is in review, with `value#1414 not produced` now active. Cohaagen established that native CUDA beats ORT in the Foundry baseline and localized 7B decode; o_proj two-way split-K regressed and remains rejected. Harry's cfg-gated `LoopStatePair` import hotfix merged as #441. Lori approved #438 and continues #440 review.
