# onnx-genai PyPI name reservation

Deckard's pure-Python placeholder and dispatch-only Trusted Publishing job were merged and dispatched successfully. PyPI now has `onnx-genai` 0.0.0; the decision note was merged into the canonical decisions log.
