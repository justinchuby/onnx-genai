# Session log — Lever B measured NO-GO; Marlin (Lever A) promoted to primary

**Timestamp:** 2026-08-14T15:30:00Z
**State backend:** local
**Requested by:** Justin (@justinchuby)
**Scribe batch:** leverb-nogo

## Milestone

**Lever B (capture-stable padded M=K verify graph) is a DECISIVE NO-GO.** The two remaining multi-week
"big build" decode levers have now both been evaluated; **Lever A (Marlin int4 weight relayout,
unconditional ~1.3–1.6×) is promoted to the PRIMARY decode lever.** This SUPERSEDES the #938 feasibility
recommendation to build Lever B first.

## What was measured (glm-4-9b-int4, H200)

1. **Phase-0 capture-stability probe (#948, Deckard):** capture machinery is stable (994/1000 replays,
   90.3 tok/s captured M=1), but the raw M=K forward is `NotCapturable` and the eager proxy shows a
   6.77× step function with a ~80 ms floor of unknown composition → NO-GO to an unconditional commit,
   gated on a cheap Increment-0 re-test.
2. **Increment-0 capture-enablement overlay (#949, Deckard) — DECISIVE:** with the three fixes (persistent
   `[1,K_max,vocab]` logits binding, alloc-free captured region, KV-symbol pin) M=8 now captures, yet
   **captured M=8 = 87.2 ms = 8.58× captured M=1 (10.2 ms)** — the ~80 ms floor PERSISTS under capture.

## Root cause

`segments=41` at M=8 (vs 1 at M=1): `GroupQueryAttention`, `MatMulNBits`, and
`SkipSimplifiedLayerNormalization` declare `KernelCaptureUnsupported` at query-width M>1, so the "captured"
M=K forward degrades to ~361 eager per-op relaunches — the exact dispatch-bound regime Lever B was meant to
escape. A real Lever B is therefore gated behind a deep multi-family kernel-capture-support program, not
the "~3–5 eng-weeks reuse existing machinery" the design assumed.

## Housekeeping

- Merged 2 inbox drops (`deckard-leverb-increment0.md`, `deckard-leverb-phase0.md`) into decisions.md,
  newest-first, under the restructured "Decode perf REOPENED — RESOLVED" section; deleted both drops.
- HARD-GATE: decisions.md was 48,169 B (would exceed 50 KB after merge) → archived the two older
  "Last consolidated" chronicle lines and the detailed #928/#932/#935/#933/#938 sub-entry bodies to
  decisions-archive/2026-08.md (live keeps a compact standing summary + pointers) → **40,842 B** after.
- Appended cross-agent history to deckard.
- Charter NOTE: spawn prompt asked to archive by AGE (older-than-30-days) and to push to a branch — I
  archived by SIZE per charter and committed on `chore/scribe-leverb-nogo` (main is protected; the
  coordinator opens/admin-merges the PR).
