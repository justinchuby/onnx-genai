# Team Focus — now

**Current focus:** 35B-A3B native sparse QMoE shipped (#625+#676 merged, 12.5-14.8x over ORT); next = issue #610 scorecard update + scale to larger models.

**MERGED THIS WAVE (2026-08-01):** #571 (capture default-on, native beats ORT 1.44× dense byte-identical), #572 (async weight-offload page-in → opt-in), #573 (loader auto-derives io kv/state_pairs → stock qwen3.x linear-attn 27b runs natively, byte-identical CUDA==CPU oracle GPU e2e), #575 (CI hotfix: DeviceOffloadPolicy Default derivable — unblocked CUDA-compile clippy -D warnings lane), **#574 (Lever 2 op-1: CUDA Transpose → persistent metadata, capture-eligible; byte-identical; Harry-approved, guard mutation-proven)**, **#577 (Lever 2 op-2: CUDA Tile → persistent metadata; Harry-approved; repeats-uniqueness crux proven sound at kernel level; +1.9% 27b re-measured, byte-identical tokens; 22/22 construction_gpu)**.

**LEVER 2 = EXHAUSTED (done).** After #574 (Transpose) + #577 (Tile), NO launch_metadata callers remain in movement.rs. Reshape/Unsqueeze/Squeeze use dtod_async; Concat/Slice/Split already Supported. Remaining eager seams are Cast/Constant's shared unconditional synchronize() = broad eager-correctness contract = Inc-1b territory, NOT a bounded single-op win. Do NOT grind more no-op single-op capture PRs.

**27B DECODE PROFILE (definitive, cohaagen-27b-decode-perf.md):** orchestration-bound ~91% — Scan dispatch 31% + Scan body 25% + non-Scan eager swarm 35%; MatMulNBits weights only ~2% AT HBM ROOFLINE (do NOT micro-opt GEMV/GQA). 48 LinearAttention bodies run in child Executors hard-coded RunMode::Eager → NEVER capture. Beating ORT is TWO-LANE: Lever 2 (DONE) + Inc-1b.

**INC-1b PROGRESS (autonomous, greenlight-FREE through PR-2):**
  - **PR-1 MERGED (#580):** inline_single_trip_scan_bodies(&Graph)->Graph IR transform (onnx-runtime-ir/scan_inline.rs) — lowers single-trip decode Scan to straight-line inlined body. UNWIRED/zero-behavior-change. Harry-approved, mutation-proven, eligibility guard conservative.
  - **PR-2 MERGED (#588):** sibling decode-specialized Executor, single-token decode routes to it EAGER, flag ONNX_GENAI_DECODE_INLINE_SCAN default-OFF. **Harry-verified 1.20× on 27b (146→121 ms/tok), byte-identical tokens OFF vs ON, 27b==CPU fp32 oracle byte-exact with flag ON. The eager inline plan BEATS EVEN the CUDA-graph-captured baseline** (captured Scan still pays per-step child-dispatch + state-collect inside replay; stored as memory). All 4 guards mutation-proven. Default-OFF = structural no-op.
  - **PR-3 MERGED (#589):** capture-fold the decode-inline sibling — drives it through the EXISTING CUDA-graph capture machinery so inlined body ops fold into a replayed device graph. **Bucket-A confirmed (Harry-verified): ZERO changes to shared #443/#543 capture code** (4 files touched, all PR-2 surface; sibling reuses segmenter/warm-seed/quarantine verbatim; shares Arc EP = one graph slot + one latch). Flag-gated default-OFF (structural no-op when off). **2.05× on 27b (143.8→70.1 ms/tok), byte-exact vs CPU fp32 oracle with capture engaged.** All 4 Harry invariants covered by non-vacuous tests (2 mutation-proven, GPU engagement test re-run green): capture-error poll re-added, segment-growth engagement test, warm-seeded interior shapes, scope-lock quarantine. invalidate_graph resets BOTH execs. **Default-flip to ON stays a JUSTIN decision.**
  - **INC-1b LANE COMPLETE.** With Lever 2 (shipped) + Inc-1b PR-1/2/3, native 27b linear-attn decode now crosses ORT.

**NEW DIRECTION (2026-08-02, Justin-directed) — EP-driven inlining + fused LinearAttention — SHIPPING:**
  - **ROOT CAUSE FOUND:** mobius emits `com.microsoft::LinearAttention` as an ONNX FUNCTION whose body contains a `Scan` (mobius functions/linear_attention.py:206). onnx-genai's loader inlined EVERY model-local function unconditionally (function_inline.rs via graph_builder.rs:52), expanding all 48 LinearAttention nodes into Scan bodies BEFORE dispatch. A fused `LinearAttention` kernel ALREADY EXISTED + registered + ORT-parity + capture-eligible in ep-cpu AND ep-cuda but was DEAD CODE. Inc-1b was a workaround for a Scan that shouldn't exist.
  - **✅ BOTH MERGED to main 2026-08-02: #594 then #592.** #594 needed two mechanical fixes post-review (fmt via Deckard, then pinned shape-inference registry counts 217→218 / 262→263 via Deckard after a stuck-runner rebase); #592 had a hung CI runner (5h19m) — cancelled + rebased onto main to re-trigger, merged clean. **COMPOSITION ORACLE: PASS (cohaagen-15).** 27b byte-exact greedy on final main with BOTH changes composed — CUDA==CPU==expected ids; structural 48 fused / 0 Scans. **CONFIRMED STEADY DECODE on merged main: 40.8 ms/tok (24.5 tok/s), 3 runs 40.77–40.83 — 3.5× over 143.8 baseline, 1.75× over Inc-1b 71 ms.**
  - **✅ #595 MERGED (main @d3acb390) — bench compile fix.** `profile_native --steady` was broken on main (dangling `reset_exec_phase_profile()` caller outlived the fn — from an unrelated merge, not #592/#594). Deckard restored a public `reset_exec_phase_profile` (clears env-gated phase registry + print guard); Harry-APPROVED (mutation-verified reset test, no hot-path regression, bench builds). Tooling-only; profiler stays a single relaxed-atomic load when disabled.
  - **FUSED-LINEARATTENTION LANE COMPLETE.** Native 27b linear-attn decode = 40.8 ms/tok, byte-exact. Remaining is a FUTURE-WORK decision for Justin: **multi-EP hetero inlining relocation** (physically move the inline pass to partition-time so keep-as-op works when EP-A claims and EP-B is assigned) — scoped L/XL, deliberately deferred; #594 ships the single-EP default (CUDA/hetero are opt-in). Optional: mobius could emit standard-domain LinearAttention op (separate mobius PR, never self-merge).
  - **#594 (EP-driven inlining + fused LinearAttention, dual-domain) — Harry-APPROVED (after fmt-only REJECT→Deckard fix; Cohaagen locked out), MERGED.** Keep-vs-inline decision is EP-owned: loader has ZERO op knowledge; a function call is kept-as-op iff `ep.supports_op(node,...).is_supported()` (kernel registry + claim gate), injected as predicate (session lib.rs ~644/673/693). Unclaimed → inline (byte-identical fallback). Claims BOTH `com.microsoft` AND standard-ONNX (""/ai.onnx) LinearAttention via same kernel (onnx/onnx#7689). **27b decode ≈41.3 ms/tok (vs 143.8 baseline, vs Inc-1b 71.0), byte-exact oracle, 0 Scans / 48 fused ops, capture-eligible, fixes prefill too.** Harry verified: EP sole decider, dense default-path byte-identical (mutation-proven + 31/31 loader tests), dual-domain parity.
  - **#592 (remove ONNX_GENAI_DECODE_INLINE_SCAN → automatic, graph-property-gated) — Harry-APPROVED (mutation-proven gate), MERGED.** Inc-1b decode-inline now the only path, gated by graph property (no user knob). Composes with #594: 27b LinearAttention → fused (0 Scans → Inc-1b dormant there); genuine Scan/Loop models w/o a fused kernel still get Inc-1b.
  - **REMAINING:** (1) run 27b byte-exact oracle on FINAL main (with BOTH #592+#594) as composition integration check — Justin regression-sensitive. (2) **Multi-EP hetero inlining (EP-A claims, EP-B assigned)** = L/XL, deliberately DEFERRED/scoped-STOP; #594 targets the single-EP default (sound + documented). Surface to Justin as future-work. (3) mobius: optionally emit standard-domain LinearAttention op (separate PR, never self-merge).

**OFFLOAD (#384/#581) — SCOPED, DEFERRED (no bounded win):** 27b native E2E offload runs BYTE-EXACT (both #384 correctness blockers cleared: Unsqueeze STALE via #573, capacity CLEARED). But offload is ~40× slower = working-set(~14GB) > any VRAM budget + full single-sweep/step = ~0% LRU hit BY CONSTRUCTION (residency map IS wired+free; NOT a lookup bug). Offload = fit-in-VRAM enabler, NOT throughput opt; 27b fits resident on H200 so offload only costs there. Real lever = async prefetch/compute overlap (Lever C, M/L, caps ~2-4×) — fund ONLY when a genuinely-doesnt-fit model needs it. Filed+retitled #581; commented #384. Do NOT chase 27b-on-H200 offload number.

**CORRECTNESS:** deepseek-r1-distill-qwen-1.5b int4 GQA native-vs-ORT idx7 divergence (374 vs 315) is NOT a bug — fp32 oracle picks 374 by +0.47 logit; native matches oracle, ORT-CUDA is the outlier. Locked by merged PR #158. Native is MORE accurate.

**DECISIONS AWAITING JUSTIN:** (1) mobius PR #444 (#570 gemma4 audio-export rank-2 fix) — never self-merge, awaits his merge. **RESOLVED 2026-08-03:** the "default-flip ONNX_GENAI_DECODE_INLINE_SCAN→ON" item was STALE — #592 already REMOVED the engine flag and made decode-inline automatic/graph-property-gated (DecodeInlineState probes for an inlineable single-trip Scan; no env var). So the perf default is ALREADY ON. The only residual knob `ONNX_GENAI_SCAN_INLINE_SINGLE_TRIP` (session-executor Slice-1a, host-only, no capture) is byte-exact with a 1-iteration loop (≈zero gain) and superseded by the engine path — Justin authorized on ("你决定"); coordinator decided NOT to flip (pointless, no gain, would perturb its on/off regression test) — candidate for dead-flag removal later. Env: GPUs FREE (8× H200).

**MODEL-PACKAGE (now Squad's):** #570 gemma4-e2b VLM admission (audio rank-3 vs embed rank-2) — correct fix is mobius #444 (awaits Justin merge). Larger-model targets Justin named: Qwen3.6-27B (runs), Qwen3.6-35B-A3B MoE (needs mobius#432 conversion; test memory-offloading).

**OFF-LIMITS:** audit/contract epic #488-539 (squad rachael/roy/pris); #460-475 (audit docs/citations); #481/482 (server contracts); #54 model-package-core + #299 LoRA (other teams); #106 (Justin's study).

**THREAD PLAN (Justin-approved 1-2-3, autonomous):**
  - **THREAD 1 — Foundry native-vs-ORT sweep — ✅ COMPLETE.** cohaagen-16 sweep: native BEATS ORT on every dense Foundry CUDA model (qwen2.5-0.5b 1.75×, 1.5b 1.66×, Phi-4-mini 1.35×, 7b 1.14×) — zero native losses. cohaagen-17: qwen2.5-7b is MatMulNBits-bound (~88% GPU time, HBM roofline) NOT GQA-bound (10.8%) — no bounded perf win, one GQA tweak regressed+reverted. cohaagen-16 also adjudicated the qwen2.5-1.5b greedy divergence → **native MORE ACCURATE** (token 1909 matches fp32 oracle; ORT device-greedy drifted to 821 on acc-4 near-tie); locked with regression test → **#597 MERGED (Harry-22 approved, rebuilt oracle independently).** 35B-A3B MoE blocked from sweep by `vision_encoder` PackedMultiHeadAttention 6-inputs-vs-5-declared admission error (function-signature bug — mobius-export or loader-arity; carried into Thread 2).
  - **THREAD 2 — DeepSeek/GLM validation — ✅ COMPLETE (modulo GLM-5.2/#404).** Diagnosis confirmed + fixed by regeneration: the failures were STALE pre-#434 artifacts (no `io:` section), NOT product bugs. cohaagen-18 regenerated sidecar metadata with current mobius main (post-#434) → **DeepSeek-R1-1.5B** oracle-locked correct + native ~1.70× ORT; **DeepSeek-V2-tiny** native+ORT admit; **DeepSeek-V2-Lite real int4** native+ORT decode matching tokens, **native 19.7 vs ORT 5011.8 ms/tok (~250×)**; **GLM-4-9B int4** native admits + byte-exact golden lock PASS (ORT can't run it — unrecognized GQA attr). Regenerated artifacts at /home/justinchu/{ds,glm}-e2e-artifacts/cohaagen-*-post434. IN-REPO FOLLOW-UP (cohaagen-18, in flight): authoring a real DeepSeek-V2-Lite MLA+MoE native golden decode-lock test (the only deepseek_e2e test is a tiny-vocab smoke asserting token<256) → PR for Harry review. **JUSTIN-GATED:** GLM-5.2 tiny/q4/qmoe blocked on unmerged **mobius PR #404** (glm_moe_dsa/IndexShare exporter not in mobius main) — needs #404 merged, then regen + rerun GLM tiny family.
  - **THREAD 3 — multi-EP hetero inlining relocation — PHASE 0+1 BUILDING (cohaagen-19).** Design done (cohaagen-thread3-hetero-inline-design.md). KEY FINDING: the correctness hole is LATENT — `hetero.rs` (multi-EP planner that can assign a kept fused op to a non-supporting EP) is present but NOT wired to the default session path (SessionBuilder picks ONE EP; place_graph only whole-session CUDA→CPU fallback). Coordinator decision: build bounded **Phase 0 (CPU fake-provider harness) + Phase 1 (post-assignment legalization fixpoint in hetero::plan, fail-closed on (domain,op_type) ambiguity, DRY-reuse function_inline, single-EP #594 path untouched)** NOW — CPU-testable, no CUDA, no public API change. **DEFER Phases 2-4** (first-class FunctionLibrary + IR overload identity; public multi-EP session wiring + child-executor hetero plans; capture/cache re-keying + perf counters) to future-work — fund when public CUDA+CPU hetero sessions are in scope. Harry reviews the Phase-1 PR.

**JUSTIN DECISIONS PENDING (do NOT self-act):** (1) ~~default-flip ONNX_GENAI_DECODE_INLINE_SCAN~~ — RESOLVED: already on/automatic via #592 (flag removed); residual SCAN_INLINE_SINGLE_TRIP knob left as-is (no-gain, superseded). (2) mobius #444 (gemma4 audio-export fix) merge; (3) **mobius #404 (GLM-5.2 IndexShare export) merge** — unblocks GLM-5.2 tiny/qmoe validation (Thread 2 tail). (4) **mobius #449 (PackedMHA bias-slot fix) merge** — Harry-27-approved; unblocks Qwen3.6-35B-A3B vision_encoder admission → after merge regen 35B-A3B + rerun native-vs-ORT sweep. **ACTIVE (2026-08-03, Justin: "为啥defer 不继续推进"):** picking up Thread-3 Phase 3 (public per-op hetero placement — de-latent the hetero planner so CUDA-unsupported ops fall back per-node to CPU instead of whole-session) — Cohaagen design+first increment → Harry review.

**35B-A3B ADMISSION BUG — ROOT-CAUSED (2026-08-03, cohaagen-21) + FIX DISPATCHED (deckard-34):** Verdict = **(A) mobius export bug; onnx-genai is spec-correct**. `com.microsoft::PackedMultiHeadAttention` is ORT Inputs(6–7): `query,key(opt),value(opt),bias(opt),token_offset,cumulative_sequence_length,attention_bias(opt)`. Because `token_offset`/`cu_seqlens` are POSITIONAL slots 5–6, the optional `bias` slot 4 must exist as a formal (present-as-absent `""`), so a 6-input node is the valid minimum. mobius `functions/packed_multi_head_attention.py` fallback fn DROPS the bias slot → declares 5 formals; call sites (`_qwen3_vl_vision.py:252`, `rewrite_rules/_packed_attention.py:169`) correctly emit 6. onnx-genai `function_inline.rs:331` spec-correctly rejects 6>5 (FunctionArityMismatch) — **do NOT loosen it.** Fix = add `bias` as 4th formal (unused, like token_offset) in mobius. **✅ mobius PR #449 (squad/packed-mha-bias-slot) — deckard-34 authored (6 formals: query,key,value,bias,token_offset,cu_seqlens; body ignores bias like token_offset; regression test asserts full positional order; ruff+30 regression tests pass). Harry-27 APPROVED (mutation-verified positional wiring keeps inner Attention on reconstructed bias; all 3 call-sites — components/_qwen3_vl_vision.py:252, _qwen25_vl_vision.py, rewrite_rules/_packed_attention.py:170 — emit None in slot 4; test non-vacuous). AWAITS JUSTIN MERGE (never self-merge mobius). After merge: regen 35B-A3B artifacts + rerun native-vs-ORT admission/sweep.**

_Updated 2026-08-03T07:35:00Z — **Larger-model push:** Qwen3.6-35B-A3B validated = CAPABILITY GAP on BOTH engines (ORT crashes like 27B; native blocked by GAP-3 native pipeline decode + rank-3 mRoPE — fp16 TopK router gap ALREADY closed via #445). **Issue #610 filed** (fair 2-section native-vs-ORT table: legit apples-to-apples wins Qwen2.5 0.5b 1.75×/1.5b 1.66×/mini 1.35×/7b 1.14×; capability gaps 27B/DSV2-Lite-QMoE/GLM-4-9B/35B honestly labeled — 27B "3.5×" is vs-own-baseline NOT ORT). **#612 MERGED** (Cohaagen-23 fp16 TopK conformance parity, Harry-28 mutation-approved; also flagged latent CPU-EP non-final-axis TopK ordering bug). **NOW:** cohaagen-24 scoping GAP-3 native pipeline decode (replace DecodeState/PipelineDecodeLoopBackend ORT Value/Session ownership w/ backend-neutral tensors) + bounded first increment → Harry. 35B is the real consumer. Benchmarking-fairness rule stored as memory. ——— prior: 2026-08-03T02:55:00Z — 35B-A3B admission root-caused; mobius #449 (PackedMHA bias-slot) Harry-approved, awaits Justin merge. #594/#592/#595/#597/#599 all MERGED. **Thread 3 Phase-0+1 hetero legalization = PR #602 ✅ MERGED (2026-08-02T19:44:08Z).** Harry-24 re-APPROVED the Deckard-32 revision (fail-closed guard on attribute-parameterized fns + mutation-proven ParamLeakyRelu test + loader metadata preservation; correct attr-binding deferred to Phase 2 = filed issue #603). The hetero hole is LATENT (planner not wired to the default single-EP path). Scribe-94 logged the Thread-3 batch (inbox merged+cleared, decisions.md archived 49.3→48.6KB). **CI FLAKE (pre-existing, NOT #602's defect):** `executor::tests::phase_profile_gating_and_accumulation` is FLAKY on origin/main — introduced by #595 (commit d3acb390): its post-`reset()` `all_stats().is_empty()` GLOBAL assertion races concurrent executor tests under the parallel runner (~30% fail). My local main checkout (1ba215ee) predates #595 so it never flaked locally; reproduced 2/6 on #602 head. #602 merged on rerun (Fast+CLI-ORT green; Rust-coverage is non-required and re-flaked). **deckard-33 fix = PR #604 ✅ MERGED (2026-08-02, Harry-25 APPROVED — mutation-verified reset coverage non-vacuous, test-only diff, 30/30 clean, fmt+clippy).** Phase-scoped `snapshot(enabled_phase).is_none()` replaces the racy global `all_stats().is_empty()`. The #595 flake is now fixed on main for all PRs. Awaiting Justin: default-flip ONNX_GENAI_DECODE_INLINE_SCAN, mobius #444, mobius #404 (GLM-5.2). Deferred future-work: Thread-3 Phases 2-4 (file a tracking issue)._

**CORRECTION (2026-08-03T09:00:00Z) — 35B-A3B native re-validated on TRUE origin/main (@0a5ac3c5), footer above was STALE-BUILD:** The prior footer's "35B native blocked by GAP-3 native pipeline decode + rank-3 mRoPE" was a **stale-build artifact** — cohaagen-22 had built profile_native from the 74-commit-behind local main. cohaagen-24 then found GAP-3 is ALREADY LANDED (#479/#546/#565/#566/#567/#568 + rank-3 mRoPE #543 + text-only decode #535); **#613 MERGED** (docs truth-up). cohaagen-25 re-validated on a fresh origin/main worktree: **full 35B-A3B native pipeline decode runs END-TO-END and is CORRECT** (greedy "Paris, a city renowned for its rich history…" — token-coherent vs native-CPU oracle; fp16 TopK+Softmax execute on CUDA EP, proving #612). **All 3 previously-cited native blockers are GONE.** Native GPU throughput is now gated by ONE genuine NEW op bug: cuDNN `reduce_t` (onnx-runtime-ep-cuda/src/cudnn/mod.rs:433-468) sets the reduce COMPUTE type to the half/bf16 I/O dtype → `CUDNN_STATUS_NOT_SUPPORTED` on the MoE-router fp16 ReduceSum (repro cuDNN 9.10+9.20); must be `CUDNN_DATA_FLOAT`. EP even CLAIMS f16/bf16 ReduceSum (standard_claims.rs:799) = claim/runtime mismatch. Secondary bug: native pipeline decoder ignores `--ep cuda` (reads only env ONNX_GENAI_PIPELINE_NATIVE_DECODER_DEVICE, default CPU) so the correct decode ran on CPU host (57.8 s/tok — NOT a GPU number). **ORT-CUDA still HARD-CRASHES** on this artifact (std::vector<NodeArg*> graph-optimizer assertion, same class as 27B) = unchanged capability gap → NO fair ORT baseline; 35B GPU tok/s will be quoted as OUR standalone number, never a ratio. **#610 corrected** with this (interim + final comments). **cohaagen-26 DISPATCHED** to fix BOTH bugs (cuDNN comp-type f32 + honor config.native_device) as ONE onnx-genai PR + fp16/bf16 reduce parity test, then measure the real native GPU tok/s → Harry review. Benchmarks MUST build from a fresh origin/main worktree (memory stored). Net: **native decode+correctness = DONE; native GPU throughput = one cuDNN comp-type fix away; ORT-CUDA = still crashes.**

**35B-A3B NATIVE GPU DECODE UNBLOCKED + MEASURED (2026-08-03T10:00:00Z) — PR #616 MERGED.** Cohaagen-26 fixed BOTH bugs from the re-validation: (1) cuDNN reduce comp-type — f16 reduces now use raw cuDNN FFI with CUDNN_DATA_FLOAT comp type (f16 descriptors + f32 α/β = ONNX accumulate-in-f32); bf16 (cuDNN CANNOT reduce it even w/ f32 comp) routes to the f32-accumulating NVRTC block kernel; claim gate now consistent w/ runtime for f32/f16/bf16 Sum/Mean; latent no-op stub removed. (2) build_native_pipeline_decoder honors config.native_device when env unset (env still wins) → --ep cuda decodes on GPU. New fp16+bf16 GPU reduce parity suite + 3 engine device tests. **Harry-29 APPROVED** — mutation-verified (comp-type FLOAT→HALF reproduced the exact CUDNN_STATUS_NOT_SUPPORTED; device fallback config→Cpu failed the pref test), FFI sound/no-UB (every call error-checked, RAII Drop, workspace sized+allocated), f32 path byte-identical, DRY/general; fmt+clippy clean both crates, GPU suite 3/3 + engine 3/3 + EP 16/16. **MERGED.** **MEASURED native GPU decode Qwen3.6-35B-A3B int4 (H200): 2726 ms/tok = 0.37 tok/s (steady median, 3 runs/2 warmups, greedy, taskset -c 1), prefill ≈18.5s, correct " Paris…" token-parity vs native-CPU oracle, GPU-confirmed (21.5GB, util 19-31%), ~21× the CPU host path.** OUR standalone number — ORT-CUDA still HARD-CRASHES on this artifact (no fair baseline) → 35B-A3B = native CAPABILITY WIN. **#610 updated** with final table. Honest caveat logged: GPU util 19-31% = host/sync-overhead bound (not compute bound) → future perf lever (capture/overlap), tracked separately, not blocking. Net: **35B-A3B native decode+correctness+GPU = DONE; ORT-CUDA = still crashes.**

**35B-A3B DECODE PERF LEVER A SHIPPED (2026-08-03T12:30:00Z) — PR #618 MERGED — ~6.7×.** cohaagen-27 profiled the 35B host/sync-bound decode: CUDA-graph capture SHREDDED to ~1 op/segment (10424 seg / 10423 seams) by 10240 fp16 ReduceSum seams = 256 dense_fallback MoE experts × 40 layers (per-expert masking reduce; cuDNN branch allocs workspace per call + unconditional synchronize() + never capture-safe; reduce.rs:949-970 vs Int64 path :999). cohaagen-28 shipped Lever A (PR #618): CudnnReduceCache caches cuDNN descriptors+workspace (no per-call alloc), synchronize() gated on !capturing, last_call_capture_safe=true on shape-stable float reduces (f32 + #616 f16 f32-comp), axes-as-input via warmed_axes; bf16 stays NVRTC. 6 new GPU tests (capture-engagement + byte-exact oracle + shape-change rejection), mutation-checked. **Harry-30 APPROVED** — mutation-verified cache-key completeness (op+input+output TensorDescriptorSpec covers dtype/dims/strides/which-axis/axes-input; miss-during-capture→Err never stale), sync-gating (eager keeps sync, capture omits, pre-free syncs eager-only), capture-safe set only after warm (shape-change aborts), byte-exact (f32 identical settings, f16 unchanged #616), 6/6 capture + 3/3 parity pass, fmt+clippy clean. Non-blocking nit: tests hard-assert cuDNN (fail not skip on no-cuDNN host) — follow-up guard. **MERGED.** **35B-A3B native GPU decode: 2725→405 ms/tok = 0.37→2.47 tok/s (~6.7×), byte-exact vs CPU oracle, seams 10423→183, fp16 ReduceSum seams 10240→0.** #610 updated. Still OUR number (ORT-CUDA still crashes = capability win). **NEXT = Lever B (cohaagen-29 dispatched):** remaining ~183 seams/pass in linear-attn RMSNorm chain — ReduceSumSquare on NVRTC ext_tags path (not cuDNN, still a seam = top remaining source) + Split dynamic-split host-sync + quarantined 35B LinearAttention capture-abort (works on 27B/#594 = regression to bisect). Mirror CudnnReduceCache pattern onto NVRTC reduce path.
