# Team Focus — now

**Current focus:** Base-decode CUDA perf vs ORT + GLM/DeepSeek support, on H200. Two constraints stand: (1) beat ORT under equal conditions on BASE decode first; (2) speculative decode is additive/opt-in only and is never the basis for an ORT-win claim.

## Recently merged (this arc)

- **#1366 — parallelize `cumsum_i64` position-id scan (byte-identical, +3.8% deep-ctx DeepSeek decode under CUDA graphs):** the post-derive_len deep-ctx (~2600) re-rank surfaced the int64 `CumSum` (`position_ids = cumsum(attention_mask)`) as the next derive_len-class anti-pattern — one lane→one thread, so a batch-1 decode (`lanes==1`) collapses to ONE thread serially scanning `width=seqlen` (O(context): 45.8µs @640 tok → **341µs @~2600 tok**, 1/step). Deckard added `cumsum_block_i64` — block-per-lane cooperative prefix scan, gated to int64 low-lane (`lanes ≤ sm_count`) calls; f32 + prefill keep the serial kernel. **Byte-identical BY CONSTRUCTION** (integer add associative); fixed grid=lanes/block=256 → capture-safe. Kernel **341.5µs→18.08µs (18.9×)**. E2E (H200 GPU5, medians-of-5, DeepSeek-V2-Lite int4): deep **CUDA-graphs ON (production) 109.5→113.6 tok/s (+3.8%)**, short flat (+1.1%), eager +1.7% (noisy). KEY: this is a **CUDA-graph-path win** — eager decode is CPU-launch-bound with idle GPU gaps that hid the 341µs kernel; under graphs the ~330µs/token saving lands on the critical path. Gates: golden 24-tok lock byte-identical (coordinator-verified GPU6 pinned, 49.94s; runs under graphs → also proves capture-safe), new `cumsum_i64_block_path_matches_serial_reference` GPU test (exclusive/reverse/multi-tile width 1000), f32 conformance PASS, dense qwen2.5-0.5b md5 identical. Files `cumsum.rs` (+93) + `indexing_gpu.rs` test (+50). Coordinator-validated pinned single-threaded + admin-merged, origin/main @ e19697fb0. **Finding: individual fp32 decode kernels are near the floor (attention_split + QMoE GEMVs roofline-NO-GO'd); the real DeepSeek lever is now Sapper's fp16-activation QMoE tensor-core gap (ORT 1.57× faster) — Deckard redirected there.**
- **#1357 — parallelize `derive_len` mask-frontier scan (byte-identical, +21.2% deep-ctx DeepSeek decode):** deep-ctx (~2600) nsys re-rank after #1350 surfaced `derive_len` as the #2 decode kernel — a single-thread `grid(1,1,1) block(1,1,1)` serial O(key_len) mask scan fired per-layer per-decode-step (146µs/call, 5.7% GPU time, growing linearly with context). Deckard replaced it with a **block-parallel min-index reduction** (256 threads, shared-mem min-combine); launch stays `grid(1,1,1)` → capture-safe. Byte-identical BY CONSTRUCTION (integer index reduction; block-min of padded indices = same frontier). Kernel **143.6µs→4.86µs (29.5×)**, 5.7%→0.1% of GPU time. E2E (H200 GPU4→coordinator-validated GPU7, PINNED, single-threaded, medians-of-5, DeepSeek-V2-Lite int4): **deep ~2600 ctx 46.55→56.43 tok/s (+21.2%)**, short ~520 ctx 58.62→60.18 (+2.7%). Win-(b) per-step hoist (removing 26/27 redundant launches) EVALUATED but DEFERRED — correct (frontier identical across all 27 layers/step) but only ~0.7% after (a), not worth threading step-scoped state + capture-safety risk. Numerics: golden 24-tok lock byte-identical (coordinator-verified GPU7 pinned, run alone to dodge #804), derive_len unit test extended with unaligned frontiers (1/255/256/257/617/2599/2600) PASS, standard_attention_gpu 23/24 (1 pre-existing dtype-homogeneity fail), dense qwen2.5-0.5b md5 identical. Single file `standard_attention.rs` (+48/−8). Coordinator-validated pinned single-threaded + admin-merged, origin/main @ 354be8fc7. **Next: deep-ctx decode re-rank (mix shifted post-derive_len) — likely `topk_block_f32` (~9.8µs, MoE router top-k) or pivot to GLM block-128 fast-path work.**
- **#1350 — adaptive split-KV sizing for attention_row decode (byte-identical short / f64-tol wide, +63% deep-ctx over monolithic + another +4.2% deep / +4.7% shallow over fixed-256):** the depth-tuned follow-up to #1340. Profile-first at ~2600-tok ctx (where attention_row ≈40% of decode) showed the split-KV win is FAR bigger at depth than #1340's shallow ~500-ctx A/B measured: fixed-256 split gives **28.40→46.37 tok/s (+63%)** deep. Critically DISPROVED the "grid ≈ SM count (132)" hypothesis via an ncu+E2E chunk sweep: the split kernel is memory-latency-bound and needs multiple resident CTAs/SM to hide KV-load latency, so the real optimum is **grid ≈ 512 = one full occupancy WAVE (~4 CTAs/SM)**, not grid≈132 (chunk 512 → only 43.95 tok/s). Shipped adaptive `num_splits` derived from fixed KV `cap` + fixed row count to target ~one wave, floored at MIN_CHUNK=128 keys/split (V2-Lite 16 rows → 32 splits/grid 512 at depth, scaling down at shallow). `ONNX_GENAI_ATTN_SPLIT_CHUNK` still pins fixed for A/B. Capture-safe (geometry from cap+rows only, never live seqlen); chunk≥128 keeps the single-split bit-exact short path. Occupancy A/B (ncu attention_split, deep): monolithic grid16/waves0.02/warps9.8%/DRAM0.8% → fixed-256 grid256/0.48/11%/34% → **adaptive grid512/0.97/24%/48%**. Numerics: golden 24-tok byte-identical (coordinator-verified GPU6 pinned), 340-tok eager==capture PASS (deep multi-split), capture/fp16/bf16 gpu-tests + 15 lib unit tests PASS, standard_attention_gpu 23/24 (1 pre-existing dtype-homogeneity fail), dense qwen2.5-0.5b md5 identical. E2E (eager, medians-of-5): deep fixed-256 46.37→**adaptive 48.32 (+4.2%; +70% vs monolithic)**, shallow 58.08→**60.82 (+4.7%; +17.7% vs monolithic)**, short capture neutral (single-split). Single file `standard_attention.rs`. Coordinator-validated pinned single-threaded + admin-merged, origin/main @ 32ee82cb5. **Next: launch_bounds/register-footprint pass on attention_split (warps-active still only 24% at grid 512 → per-SM headroom); then deep-ctx QMoE re-rank (all prior QMoE profiling was at short "Hello" ctx — kernel mix shifts as attention shrinks at depth).**
- **#1340 — split-KV / FlashDecoding for attention_row decode (f64-tol wide-ctx / byte-identical short, ~2.3× kernel / +10.2% E2E wide-ctx):** the big attention win. After #1337 widened the decode blocks (latency-hiding on the 16 active SMs), split-KV fills the **116 idle SMs**. ncu GO signal (V2-Lite eager wide-ctx decode): `attention_row` = #1 kernel at **36% of GPU decode**, grid=16, waves/SM=0.02, occupancy 9.8%, DRAM 0.79% — machine-starved, memory-latency-bound. Two-kernel FlashDecoding behind `ONNX_GENAI_ATTN_SPLITKV` (default-ON): `attention_split` spreads each row's KV reduction across `num_splits` blocks (chunk-local online softmax → partial P·V + max/sum meta), `attention_combine` does a uniform log-sum-exp merge. **Single-split sentinel** (`max=0,sum=1`) makes contexts ≤ chunk (256) a bit-exact pass-through = BYTE-IDENTICAL short path. **Capture-safe:** `num_splits`/engage derive only from fixed KV `cap`, never live seqlen → eager==capture identical launches; scratch from the capture-warmed ws pool (governed workspace layout untouched — an early plumbing attempt was deliberately reverted to keep layout unit tests green). Numerics: golden 24-tok lock byte-identical (short path, coordinator-verified PASS on cohaagen-post434 export, GPU3 pinned); wide-ctx reorders fp32 → #1150 f64-tol; capture/fp16/bf16 gpu-tests PASS; standard_attention_gpu 17/18 (the 1 fail = pre-existing dtype-homogeneity bug, coordinator-confirmed on clean main); the 340-tok long-ctx eager==capture lock is blocked by the PRE-EXISTING #804 process-wide-config freeze-guard identically on main and PR (in-process ONNX_GENAI_CUDA_GRAPH 0→1 toggle — harness defect, NOT a kernel regression). Dense qwen2.5-0.5b byte-identical, no regression. H200 A/B (GPU3, pinned, medians-of-5): decode attn kernel **147.6→~65µs (~2.3×)**, DRAM 0.79%→9.26%, **E2E wide 51.64→56.90 tok/s (+10.2%)**, short eager/capture/dense neutral. Composes cleanly with #1337. Single file `standard_attention.rs`. Coordinator-validated pinned single-threaded + admin-merged, origin/main @ 763d81f5. **Next: re-profile at ~2000+ ctx (attention_row hits 40%, split reaches ~8 blocks/row) to quantify the fuller win; consider adaptive `chunk` targeting grid ≈ SM count.**
- **#1337 — attention_row decode block-widening 128→256 (byte-identical, −14% kernel / +5.5% E2E at realistic prompt):** after both QMoE decode levers closed as NO-GO (see below), profiling surfaced `attention_row` as the largest untouched non-dense decode kernel. ncu roofline verdict: at decode (q_seq==1) it launches ONE block per (batch, q_head, query) row → collapses to **16 blocks / 6.23% occupancy / 62.7% barrier stalls** = pure memory-latency-bound with only ~4 warps/SM to hide K/V load latency (unlike the occupancy-maxed QMoE GEMVs — real structural headroom). Critically it scales linearly with context and is the #1 decode kernel at realistic prompts: **12.4% ("Hello") → 25.3% → 40.0% of GPU decode at ~500-tok prompt / 256 tokens** (QMoE GEMVs stay flat, get overtaken). Deckard widened ONLY the decode launch 128→256 threads/block (prefill keeps 128) so the few per-head blocks get enough warps to hide per-key latency; same reduction order (Q·Kᵀ, serial softmax, P·V) → BYTE-IDENTICAL. `ONNX_GENAI_ATTN_ROW_THREADS` override + width-lock unit test. Byte-identity PINNED: 128-tok md5 unchanged (V2-Lite short+long, dense qwen), V2-Lite golden decode lock PASS, standard_attention fp16/bf16/capture gpu-tests PASS (17/18; the 1 fail `..._requires_homogeneous_floating_input_dtypes` is a PRE-EXISTING dtype-claim bug on clean origin/main, coordinator-confirmed via origin/main run — unrelated). H200 A/B (PINNED, median of 5): attention_row kernel **172→148µs (−14%)** @ ~500-tok/128-tok, **E2E 81.94→86.47 tok/s (+5.5%)**; short "Hello" + dense qwen2.5-0.5b long-ctx neutral. 512≈256 (P·V only 128 channels). Single file `standard_attention.rs`. Coordinator-validated pinned single-threaded + admin-merged, origin/main @ 37bdefe8. **Captures only the latency-hiding win on the 16 active SMs; 116 SMs still idle. Next: split-KV / FlashDecoding to fill the machine.**
- **QMoE pipeline-fusion (NO-GO, profile-refuted pre-implementation):** proposed fusing `qmoe_gate_up_activate`→`qmoe_linear` (fc2) to amortize the intermediate-activation DRAM round-trip + per-launch metadata reads. ncu killed it before any kernel code: the intermediate round-trip = **20.99 KB = 0.075% of the 27.85 MB/launch DRAM traffic** (fc2-side read served from L2, never reaches DRAM; metadata = 24 B). Cost is **dominated by irreducible int4 expert-weight reads** (fc1+fc3 18.54 MB, fc2 9.31 MB) — a fused kernel still reads every selected expert's weights once, so fusion cuts nothing. Structure also forbids it: fc2 needs the full 1408-elem intermediate row → true fusion = 6 blocks/route on 132 SMs (~4.5% occ) vs today's 8448 blocks, and at ~11% HBM the kernels are latency-bound so collapsing the grid destroys #1323's occupancy-based latency hiding. Doubly counterproductive. Honest NO-GO, baseline unchanged.
- **QMoE int4 ORT-idea port (NO-GO, occupancy-spill regression):** on user directive "如果 ORT 做的比我们全面 那就拿过来", studied ORT's mature int4 decode GEMV (`moe_gemv_device.cuh`, `fpA_intB_gemv`, `matmul_4bits.cu`) to adopt decode-M=1 weight-load/dequant levers. The one portable byte-identical idea (issue all weight loads before consuming) **REGRESSED `qmoe_gate_up_activate_occ` 34.18→40.96µs (+19.8%)** — a 2nd live weight struct spills registers past the `__launch_bounds__(256,6)` cap #1323 relies on → 6→fewer blocks/SM, occupancy loss > MLP gain. ncu proof both QMoE GEMVs already occupancy-max (`qmoe_linear` at 32 reg / 8 blocks/SM = 64-warp HW limit, 85.4% occ) + memory-latency-bound (~11% DRAM). **Both QMoE decode levers now exhausted with evidence — moat kernels at the M=1 decode roofline; further QMoE gains need lower-bit weight FORMAT (model/numeric change), not kernel restructuring.** Consolidated in decisions.md via #1335.
- **#1323 — QMoE gate_up_activate occupancy + selective read-only int4 loads (byte-identical, +2.2% V2-Lite decode):** after #1317 collapsed the router TopK, `qmoe_gate_up_activate_f32` became the #1 QMoE decode kernel. Deckard shipped two byte-identical levers: (1) the occupancy `_occ` entry (`__launch_bounds__(256,6)`) flipped DEFAULT-ON (opt out `ONNX_GENAI_QMOE_OCC=0`); (2) read-only-cache (`__ldg`) int4 loads via a new `ReadOnly` template param **scoped to the gate/up path only** — blanket `__ldg` regressed the differently-shaped fc2 `qmoe_linear` reduction, so fc2 keeps the default load (a reusable finding for future int4-GEMV kernels). Byte-identity: 128-tok md5 identical, V2-Lite golden decode lock byte-identical greedy seq, `qmoe_occ_is_bit_identical` 0 differing bits (fp16/bf16/fp32 × rows∈{1,4,6,8}), qmoe_gpu 32/32 pinned. H200 A/B (GPU4 PINNED, nsys): `qmoe_gate_up_activate_f32` **42.68→34.24µs (−19.8%)**, `qmoe_linear_f32` unchanged (regression avoided); capture decode **124.98→127.77 tok/s (+2.23%)**. Single file `crates/onnx-runtime-ep-cuda/src/kernels/qmoe.rs`. Coordinator-validated + admin-merged, origin/main @ 02649910. **Next: QMoE int4 expert GEMV (`qmoe_linear` ~28.8µs) — studying ORT's fpA_intB/CUTLASS uint4 MoE GEMM for portable decode-M=1 levers.**
- **ORT upstream-contribution recon (NO-GO, verified):** on user request "贡献回 ORT", researched microsoft/onnxruntime@1e9aee07 to port our TopK/argmax wins upstream. **Both already in ORT `main`, more comprehensively** — verified against real source (gh search + raw fetch): (A) MoE router TopK = `DispatchSoftmaxTopK` in `contrib_ops/cuda/moe/qmoe_kernels.cu` (full warp/block dispatch tree: WarpBitonic ≤32, WarpMerge ≤64 [fires for V2-Lite], WarpTopK ≤256, BlockMergeSort 128-1024; scalar 1-thread path only for k>64||experts>1024); (B) vocab argmax = `arg_min_max_last_axis_cooperative_kernel` in `core/providers/cuda/reduction/reduction_functions.cu` (multi-block-per-row two-stage, `MIN_NUM_COLS_FOR_COOPERATIVE_ARG_REDUCTION=128` threshold, same NaN/tie semantics). No non-redundant PR to send; strong validation that our designs are production-grade. User: "行那就不 contribute" → contribution closed. **Follow-up directive: reverse the flow — where ORT is more comprehensive, adopt INTO us (→ QMoE int4 GEMV lever above).**
- **#1317 — QMoE router TopK block-parallel decode (byte-identical, +24.2% V2-Lite decode):** profiling DeepSeek-V2-Lite int4 QMoE eager decode (nsys, H200 PINNED) surfaced the router `TopK` as the #1 kernel — at decode (`slices==1`, `[1,1,64]`) the entire top-6-of-64 selection ran on ONE thread = 28.4% of GPU decode, 81.7µs/layer. Deckard added a block-per-slice `topk_block_*` path (dispatched only for under-saturated slice counts; prefill path untouched). Byte-identical to CPU oracle: `topk_block_decode_path_is_byte_identical_to_cpu_oracle` + `topk_deepseek_router_k6_of_64_is_deterministic` + tie-dense f32/fp16/bf16 lock; V2-Lite native-CUDA golden decode lock unchanged; 128-tok E2E stream identical eager+capture. H200 A/B (GPU3 PINNED, medians of 5): TopK kernel **81.7→9.73µs/layer (8.4×)**, 28.4%→4.5% of GPU; production capture decode **101.17→125.68 tok/s (+24.2%, 1.24×)**. QMoE expert-GEMV block (gate_up+linear+route+combine) ≈27.8% confirmed a real hotspot — the strategic-read's arithmetic-intensity leverage point, on a model **ORT CUDA can't run on GPU at all**. Deckard GO; coordinator-validated (16/16 indexing_gpu byte-identical, pinned single-threaded). origin/main @ 1660cff6. **Next target: `qmoe_gate_up_activate_f32` now the #1 kernel (~19.8%).**
- **#1306 — rescue of #976 split-block device-sampler argmax (byte-identical, 1.62× faster wide-vocab):** ports the two-stage per-row argmax reduction onto current `onnx-genai-ort/src/device_sampler.rs` — the ORT-wrapper device sampler, sibling to the native EP's #1293 device_argmax. `argmax_part_*` spreads one vocab row over `parts` blocks (strided coalesced, warp-shuffle reduce); `argmax_join*` combines per-part winners. Composes ORTHOGONALLY with main's multi-row argmax (`9e1aff00`): `argmax_into` launches `grid=(parts, rows)` then join `grid=(rows)`; narrow rows (`vocab ≤ 2·BLOCK`) → `parts=0` = unchanged single-launch — NOT superseded, additive. Combine rule exact drop-in (NaN never wins, ties→LOWEST index, all-NaN→0). Dropped original's `2965895f` PTX/loader prereq (already landed as #964); cherry-picked only kernel + dtype/batch/width tests, both applied cleanly 3-way. Byte-identical PINNED across every dtype/batch/odd-width (`split_matches_the_single_launch_path[_for_every_dtype]`, batched, odd-widths). H200 kernel A/B (GPU3, vocab=202048, end-to-end `argmax_into`, medians of 5): one-block 38.17µs → split (parts=99 auto) 23.49µs = **1.62× (~38%)**, ~20µs shared D2H+sync masks the larger pure-kernel win (original PR kernel 19.52→3.60µs). Narrow rows no regression by construction. 7 GPU split_argmax tests pass PINNED single-threaded (the perf assert flakes only under cargo parallel-thread GPU contention). Deckard GO; coordinator-validated pinned. origin/main @ 9ac981ca.
- **#1298 — device-property tiling: SM-count split-K consumes the arch scaffolding (byte-identical, RTX-only activation):** first real consumption of #1287's `ArchConfig`. `matmul_nbits.rs`'s `use_accuracy4_stage64` now sources its resident-warp estimate from `arch::decode_resident_warps_per_sm(cc)` instead of an inline `match`; adds `DecodeTilingProfile` + `one_wave_ctas()` that scales the occupancy target with the probed SM count. Hopper sm_90 = 64 warps/SM **frozen out** of the new SM-count split-K lever (reproduces today's selector byte-for-byte); Ada sm_89 / Ampere sm_86 (RTX 40/30) = 48 warps/SM **opt in** (fewer SMs → one-wave sooner → split-K fills grid). H200 byte-identity: structural (arch helper returns the old value on sm_90) + `accuracy4_resident_warps_matches_prior_inline_ladder` test proves helper==prior ladder across cc/shape sweep + `sm_90_decode_profile_is_frozen_out_of_rtx_splitk` lock; 13 arch/accuracy4 tests pass PINNED; `matmul_nbits_gpu` 19/20 pinned (the 1 fail is a pre-existing cuBLASLt-workspace test that reproduces on pristine main). H200 tok/s flat BY CONSTRUCTION (no sm_90 path diff). **Honest label: zero-H200-change, test-locked scaffolding-that-SELECTS — NOT a measured RTX win** (no RTX hw; consumer path validated only via synthetic `for_test` caps). Benchmark when RTX lands. Batty GO; coordinator-validated pinned. origin/main @ 04cfb77e.
- **#1293 — on-GPU argmax base-decode win, now DEFAULT-ON native greedy (byte-identical):** promotes the former opt-in `ONNX_GENAI_ONGPU_ARGMAX` (#1112) into the default native greedy loop (`decode_cuda_greedy`→`device_argmax`), killing per-step logits D2H + host reduction. Deckard's tie-break finding RECONCILES the feared 72.9% fp16-ULP divergence: the device kernel AND the shipping host greedy ref (`sample_greedy`/`argmax_logits_tensor`, canonical ONNX ArgMax `select_last_index=false`) BOTH already break ties to LOWEST id — the `max_by`/highest-index refs were test/bench code, not the sampler. Still delivered a `HighestIndex` kernel OPTION (default stays `LowestIndex`, threaded trait→provider→session→NVRTC). Byte-identical PINNED 128-tok greedy: qwen2.5-0.5b head64 **0.000%**, qwen2.5-14b-zp head128 **0.000%**. A/B (medians of 5, PINNED): 0.5b **+27.7%** (500→638 tok/s), 14b **+8.8%** (155.8→169.5). 7 ep-cuda device_argmax GPU tests + new fp16-ULP-tie regression pass pinned. Un-wired seam: multi-component `NativePipelineDecoder` still host-samples (deferred follow-up). Deckard GO; coordinator-validated pinned. origin/main @ ab7b8101.
- **#1287 — SM-version kernel dispatch scaffolding (RTX unblock, zero H200 change):** `ArchTier` (Legacy/Volta/Turing/Ampere/Ada/Hopper/Blackwell) + per-tier `ArchConfig` hints seeded from today's hardcoded selectors; sm_90→Hopper reproduces exact current H200 config (frozen by `sm_90_hopper_config_is_frozen`); mapping total (unseen cc→Blackwell). Added L2-cache-size probe to `CudaDeviceCapabilities`. Named insertion points `RTX-TILING`/`RTX-SPLITK`/`RTX-CPASYNC`/`RTX-L2RES` for pending RTX kernels. NOTHING in live kernel selection consumes `arch_tier`/`arch_config` yet → zero behavior change (grep-verified). Answers "rtx显卡也要优化" — gives consumer-card kernels a clean plug-in seam without touching the H200 path. Batty GO; coordinator-validated (7 arch/cap tests pinned). origin/main @ 1854153c.
- **#1139 — gate/up SwiGLU decode occupancy 62%→82% via launch_bounds (byte-identical, +2.6% 14b):** `_vec_occ` siblings of the symmetric RMS-fused `_vec` kernels add `__launch_bounds__(256,8)` → caps regs 40→32 (no spill) → 8 blocks/SM = 100% theoretical occ, raising achieved occ 62%→82% on the dominant decode kernel (38.8% of decode time). More resident warps hide the Short-Scoreboard/RMS-staging latency. Byte-identical BY CONSTRUCTION (register-allocation hint, no math/reduction-order change). Flag `ONNX_GENAI_GATEUP_OCC` **default-ON** (`=0` forces non-occ A/B), symmetric-RMS-only (asym `_zp` excluded to avoid spill). Wallace: byte-identity 0.000% on 14b & 7b, **+2.6% 14b (5/5 rounds)**, 7b flat/no-regression. Rachael 🟢 + Wallace GREEN. origin/main @ 0636a759. Biggest single-kernel lever of the campaign.
- **#1137 — gate/up SwiGLU dequant zero-point bias-fold (byte-identical, −7.4% issued instrs):** fold symmetric −8 int4 zero-point into the dequant magic-bias constants (bottom nibbles `x−1032`; top `fma x*(1/16)−72`) → 4 fewer f16x2 ops/word. Flag `ONNX_GENAI_GATEUP_VEC` **default-ON** (`=0` forces scalar A/B). Byte-identical (0-bit unit test + Wallace 0.000% divergence on 14b & 7b); perf 7b clean **+1.4%**, 14b break-even (decode memory-latency-bound → ALU cuts limited leverage on big model), zero regression. Rachael 🟢 + Wallace GREEN. origin/main @ 70cc06ad.
- **#1134 — int4 GEMV prefetch pipeline (base decode +5.6% 14b / +2.2% 7b, byte-identical):** PF=2 register shift-register keeps 2 int4 weight-loads in flight per lane on the single-warp block-32 scales-fp16 decode GEMV, cutting Long/Short-Scoreboard stalls. Flag `ONNX_GENAI_GEMV_PIPELINE` **default-ON** (byte-identical: 0-bit unit test + 0.000% divergence over 300 tok). Rachael 🟢 + Wallace independent GPU re-validation. origin/main @ 972fc62d.
- **#1112 — base-decode on-GPU argmax:** kills per-step logits D2H + host argmax. qwen2.5-14b-int4 **+8.7–9.1%**. Opt-in `ONNX_GENAI_ONGPU_ARGMAX`, default-OFF.
- **#1119 — argmax tie-break reconciled to lowest-index (ONNX/ORT canonical):** device_argmax now breaks value ties toward the LOWEST global index, matching ONNX ArgMax `select_last_index=false`, `sample_greedy`, and `argmax_logits_tensor`. Byte-identical incl. tie positions; makes the opt-in flag parity-safe for a future default-ON. device_argmax suite 5/5.
- **#1120 — rustfmt repair** of device_argmax.rs test blocks #1119 left unformatted (unblocks Fast CI).

## Native-vs-ORT base-decode standing (measured 2026-08-17, Wallace)

Fresh steady-state decode (medians of 5, 128 tok, on-GPU argmax ON), native CUDA vs ORT CUDA 1.27 on the SAME int4 model+prompt:

| Model | Native | ORT | native/ORT |
|---|---|---|---|
| Phi-4-mini (3.8B) | 312.3 | 234.2 | **1.33×** |
| qwen2.5-7b | 305.6 | 271.3 | **1.13×** |
| qwen2.5-14b-zp | 153.3 | 85.2 | **1.80×** |
| DeepSeek-V2-Lite int4 (MoE) | 55.15 (native, fp32-act post434) | 86.78 (ort-genai, fp16-act export)† | **ORT 1.57× faster — native QMoE is fp32-activation-only (no fp16 tensor-core path); cross-export caveat** |
| GLM-4-9B int4 (dense GQA) | 213.39 (capture, same-export ortfair) | 251.37 (ort-genai, same-export ortfair)‡ | **ORT 1.18× faster — NOT a moat; tractable ~18% native optimization target** |
| qwen3.5-2b (hybrid linear-attn, head_dim=256) | 170.97 (eager, post-#1273) / 100.9 (capture, pre-kernel) | 61.2 (eager, noisy) | **1.65× capture / 1.10× eager**§ |

**Native is ahead of ORT CUDA on all three dense int4 models under real deployment conditions.**
§ qwen3.5-2b (Wallace 2026-08-18, H200, `qwen3.5-2b-text-generic-cpu-1`, int4-wt/**fp32-act**, medians of 5): FLIPS the qwen2.5 story — native beats ORT in BOTH framings (eager 1.10× within noise; capture 1.65× clean, `captures replays fallbacks=0`). Root cause: ORT CUDA EP **cannot place the hybrid `LinearAttention`/`CausalConvWithState` ops on GPU** (25 Memcpy nodes, CUDA-graph blocked, strict-no-fallback errors) — native has GPU kernels + captures the whole hybrid graph. Linear-attn path is already efficient (6.6% of decode). New levers surfaced: (#1, in flight) fused **general head-size** GQA decode kernel — head_dim=256 falls out of the head≤128 fused fast path → slow `gqa_attention_reference_f32` = **31% of decode**; (#2) generic-cpu export is **fp32-activation** (~2× fp16 cost); (#3) giant vocab 248320 lm_head fp32 GEMV 11.6%.
† DeepSeek-V2-Lite (**RESOLVED 2026-08-19→2026-07-27, Sapper FAIR CUDA-vs-CUDA QMoE A/B**): the "0.17 tok/s ORT" was a **CPU-fallback artifact** (the A/B harness pulled CPU-only onnxruntime; fp32-activation post434 → 27 nodes on CPU incl. all 26 QMoE, 52× "QMoE kernel not found"). The 598×/336× ratios are **RETRACTED — never cite as an ORT-beating result.** With a proper **fp16-activation export** (`ds-e2e-artifacts/cohaagen-deepseek-v2-lite-int4-fp16act`, graph surgery on post434's int4 weights — mobius #515's activation_dtype auto-infer is upstream-merged), **ORT CUDA places all 26 QMoE nodes on `CUDAExecutionProvider` (1165 CUDA nodes, 0 Memcpy bridges)** — so "ORT can't run int4 QMoE on GPU" is **DISPROVEN**. **Honest fair A/B** (H200 GPU3, greedy, medians-of-5, no CUDA-graph; cross-export — forced by kernel dtype asymmetry: native QMoE=**fp32-activation-only**, ORT QMoE=**fp16-only**; SAME int4 weight bytes): short ctx **ORT 86.78 vs native 55.15 tok/s → ORT ≈1.57× FASTER.** On a fair CUDA-vs-CUDA QMoE decode, **native is BEHIND** — root cause: native's QMoE kernel has no fp16 tensor-core activation path (native's #1 DeepSeek M=1 decode lever). **Deep-ctx (~2600) native ABORTED** — Attention workspace sized at graph-capture (4.46 MB) isn't resized for ~2600-ctx KV (needs 42.2 MB); ORT ran it fine. This CONTRADICTS Deckard's working deep-ctx +21% derive_len run on the SAME post434 (harness-dependent) → **Sapper investigating whether it's a genuine engine bug or a harness/config gap.** Our native EP does run fp32 QMoE on GPU correctly — valuable for the fp32-activation export ORT drops to CPU by default — but under a fair fp16 A/B, native LOSES. Levers: (1) fp16-activation tensor-core QMoE kernel (queued); (2) fix/confirm the deep-ctx Attention workspace resize.
‡ GLM-4-9B (**RECONCILED 2026-08-19, Wallace fair same-export A/B**): **"ORT can't load GLM" was STALE/FALSE** — the `cuda-post434` dir merely lacked `genai_config.json`; the `-ortfair` sibling has one and onnxruntime-genai CUDA loads AND runs it. **Honest same-export number** (`glm-4-9b-int4-cuda-ortfair`, H200, greedy, CUDA-graph both, medians-of-5): **native 213.39 tok/s vs ort-genai 251.37 → ORT 1.18× faster.** This IS clean apples-to-apples — post434 and ortfair **share the identical `model.onnx.data` inode** (same int4 weights, both fp16-activation). **The earlier 97.62 / 2.56× is RETRACTED** — a run-specific artifact (weight-offload/contention) that does NOT reproduce across 2 binaries × 2 exports × 2 configs; the 211.74-vs-97.62 "discrepancy" was config/artifact, never the export. **GLM is a tractable ~18% native optimization target, NOT a moat.** #1 native GLM decode kernel = `matmul_nbits_gemv_f16_general_bs_splitk_wide` (41.2% of decode GPU time at only 15.75% DRAM peak); MatMulNBits GEMV family ~68%, GQA attention only ~8.6% (the CPU op-timer's "48% GQA" was a host-dispatch artifact). **Root cause:** GLM's int4 export is **block-128**, which disqualifies it from every fused fast-path (all gated on `block_size==32`), forcing the unfused split-K general GEMV + separate silu_mul + rmsnorm launches. **Lever (in flight, Wallace prize-bounding):** extend the block-32-only fused/non-split fast-paths to block-128. The mobius `genai_config.json` omission is **intentional** (opt-in `--runtime ort-genai` gate), NOT a bug. Prior native capture/eager 1.64× (own-vs-own) and byte-identical lock remain valid native-internal facts.

### ORT-fairness decomposition (Wallace, 2026-08-17 — the honest anatomy of the lead)

Root-caused the earlier "ORT errors through harness": auto-discovery was silently loading the **conda CPU-only ORT**; fixed by pinning `ONNX_GENAI_ORT_LIB` to the CUDA build + `ONNX_GENAI_EP_FALLBACK=1`. With that fixed:

**Graph-vs-graph is UNATTAINABLE — and that's the finding, not a gap in our test.** Stock ORT CUDA EP **cannot capture a usable CUDA graph** on these onnxruntime-genai int4 models (shape-massaging nodes forced to CPU + dynamic KV): `enable_cuda_graph=1` no-ops on 14b (80.7 vs eager 83.4), is garbage on Phi (4.4 tok/s), fails on 7b. **Native *can* capture (it owns the KV cache + does static-shape replay) — that capability is itself the source of the lead, and ORT structurally can't match it on these models.**

**Per-model ORT-graph-mode failure reasons (Wallace 2026-08-18 reconfirmation, H200 GPU2, ORT pinned to CUDA build):** graph-vs-graph attempted directly and documented — **Phi-4-mini:** hard reject, *"model has control flow nodes which can't be supported by CUDAExecutionProvider"* (236→19.8 tok/s if forced). **qwen2.5-7b:** runtime error, *"ort_value must contain a constructed tensor"* (CPU-assigned shape nodes). **qwen2.5-14b-zp:** accepted but **no-op** — eager 96.9 vs "graph" 98.7, tokens byte-identical (CPU shape-massaging fragments the graph). Eager-vs-eager reconfirmed: Phi **0.85×**, qwen7b **0.77×**, qwen14b-zp **1.19×** (native/ORT per-kernel) — matches the 2026-08-17 EQUAL column; ORT's int4 kernels are faster per-kernel on 2/3 models, native's deployment lead is architectural (capture + on-GPU argmax). Two-framing standing table durable.

Fair kernel-isolation test = eager-vs-eager (both launch-bound), medians of 5:

| Model | ORT eager | native CLAIMED (graph+argmax) | native EQUAL (eager+argmax) | native PURE (eager, host argmax) |
|---|---|---|---|---|
| qwen2.5-14b-zp | 83.4 | 167.1 (**2.00×**) | 106.5 (**1.28×**) | 67.5 (0.81×) |
| qwen2.5-7b | 273.6 | 312.3 (**1.14×**) | 207.0 (0.76×) | 206.4 (0.75×) |
| Phi-4-mini | 230.1 | 313.1 (**1.36×**) | 198.0 (0.86×) | 199.2 (0.87×) |

**Verdict — the win is ARCHITECTURAL, not per-kernel:** captured-graph launch-overhead elimination = **+51–58% on every model** (dominant lever); on-GPU argmax = huge on 14b (152k-vocab D2H avoidance, +58%), ~0 on small vocab; **intrinsic per-kernel decode: native is 0.75–0.87× of ORT eager on all three** — ORT's kernels are actually slightly faster. This **independently corroborates the batch-1 kernel NO-GOs**: kernels are not where the win is or where headroom lives.

**Strategic read:** our moat is graph-capture + device-sampler integration on dynamic-KV int4 models — a *capability* ORT can't provide, more durable than a tuning lead. The honest headline: *"1.14–2.00× faster than ORT under identical deployment conditions, because native captures a full-decode CUDA graph + on-GPU sampling that ORT's CUDA EP structurally cannot on these models; intrinsic per-kernel throughput is comparable-to-slightly-behind."* Do NOT chase more byte-identical GEMV micro-ops — mis-aimed. Leverage lives in (a) preserving/extending the graph + device-sampling envelope, (b) arithmetic intensity (M>1 QMoE / low-bit). ORT CPU fallback negligible (2 non-GEMM nodes).

**Kernel-level lever (graph-independent):** int4 **MatMulNBits GEMV family = ~77% of decode GPU time**; dominant single kernel `matmul_nbits_gemv_f16_gate_up_decomposed_swiglu_rmsnorm` = **38.8%**, running at **1.4 TB/s = ~29% of H200 4.8 TB/s peak**, top stall **Short Scoreboard (shared-mem staging)**, ~3× bandwidth headroom. Software-pipelining the dequant→compute path to raise DRAM util toward 70–80% is the highest-leverage next win.

- **gate/up cp.async weight-stream pipeline (`ONNX_GENAI_GATEUP_CPASYNC`, branch `squad/gateup-cpasync` @ eff4e0e7) — NOT merged, NO-GO:** double-buffer int4 weight loads global→shared via `cp.async` to hide DRAM latency behind compute. **Byte-identical (0 mismatching bits, cp.async is a pure value-preserving copy)** BUT a hard regression: kernel **2.3× slower** (52.96→119.9us), DRAM util *halved* (33.4%→15.4%), Long Scoreboard *rose* (12.68→13.73), E2E 14b −36%. Deeper 3-stage didn't rescue → fundamental. **Mechanism: #1139's 82% occupancy already saturates TLP latency-hiding; for a once-used int4 stream with tiny per-tile compute, the global→shared→register round-trip + commit/wait/LDS issue overhead has nothing to overlap behind. Direct LDG + high occupancy wins.** Also corrected the ncu wording: the dominant kernel's top stall is **Long Scoreboard (global weight-load latency, ~52%)**, NOT Short Scoreboard/shared-mem (LDS only 0.18 cyc) — so the winning direction is register-resident, not smem-staged. Branch preserved.

## Shelved (honest NO-GO)

- **gate/up pre-permute (`ONNX_GENAI_GATEUP_PREPERM`, branch `squad/gateup-preperm` @ 6629f0aa) — NOT merged:** `_vec_occ_preperm` stages the normalized activation once in weight-nibble-interleaved order → main loop reads with one vector load, no per-iter `prmt.b32`. **Byte-identical (Rachael 🟢: hoisted permute proven to preserve exact FMA accumulate order + RMS reduction; Wallace: 0.000% divergence on BOTH 14b & 7b over 300 tok).** BUT the perf win did not reproduce: ncu-isolated −3.4% kernel win is real, yet E2E is within noise (14b median +0.27%, 2/5 rounds regressed, widened variance; 7b flat). Decode is memory-latency-bound → ALU-side savings don't convert. Shelved as a dead default-OFF flag would be pure maintenance debt. Branch preserved. **Lesson: post-occupancy, the dominant gate/up kernel is memory-bound (Short-Scoreboard 52% of stall, DRAM ~33%, 1.4 TB/s = ~29% of peak) — further ALU cuts have limited leverage. Next wins must be memory-side.**

- **MLP down-proj occupancy (`ONNX_GENAI_DOWN_OCC`, branch `squad/down-occ` @ 393b162d) — NOT merged, NO-GO:** applied #1139 `__launch_bounds__(256,8)` to `_down_tpl`. **Byte-identical (0-bit test across c2/c4/c8 COLS widths, launch_bounds is register-cap-only).** Occ rose 66.5%→**87.3%** BUT duration FLAT (22.62→22.59us), E2E 14b base won 5/5 (−0.8%). Two kill-switches hit: (1) **spilled** at 32-reg cap (245,760 local-mem reqs — down's heavier body: 4× permute + COLS accumulates + `warp_sums[8][COLS]` shared reduction), pushing Long-Scoreboard *up* 9.62→11.49; `(256,6)` fallback useless (kernel already 39<48 regs = 75% theoretical). (2) **No conversion** — down is already ~38% DRAM, so occupancy isn't the residual bottleneck. Branch preserved. **Lesson: launch_bounds occupancy only converts on kernels that (a) fit the reg cap spill-free AND (b) are occupancy-starved, not already near-bandwidth. down fails both.**

- **attn q/o `_pipe` occupancy (`ONNX_GENAI_QO_OCC`, branch `squad/qo-occ` @ 9ba8c131) — NOT merged, NO-GO:** applied #1139 `__launch_bounds__(256,8)` to the q/o `_pipe` kernel. **Byte-identical (extended `scales_f16_pipeline_is_bit_identical_to_scalar` to genuinely exercise the `_pipe_occ` arm — 0-bit across q/o, wide-MLP, small, ragged-N × fp16/fp32 scales).** Perf NO-GO — actively regresses: duration 10.27→**13.44us (+31%)**, achieved occ 58.1%→**55.4% (down!)**, **spill 0→281,600 reqs**, E2E 14b base won 3/3 (−6.4%, 166.3 vs 155.4 tok/s). **The register-granularity trap: Hopper allocates registers in steps of 8. `_pipe` uses 40 (75% theoretical); the only step below is 32 (100%), which spills — no 36-reg option. `(256,7)` also yields 32 regs + identical spill; `(256,6)` is a no-op (permits 42, kernel uses 40).** Branch preserved.

## Batch-1 kernel vein MINED OUT (decisive campaign conclusion, evidenced 5×)

**Kernel fusion scope (Luv, profiling-only, NO-GO) — the top candidate already exists & is shelved:** QKV fusion is already built in-repo (`CudaQkvProjectionFusion`, `optimizer.rs:2103`, flag `ONNX_GENAI_CUDA_ENABLE_QKV_FUSION` default-OFF); byte-exact, cuts −104 GEMV launches/token, previously measured flat-to-worse (47.33→47.26 tok/s 30B). Luv independently reproduced **−10.8% on qwen2.5-14b** (167.7→149.4 tok/s, base wins 3/3) — worse because the fused wide-N node drops the tuned `_pipe`/`_down_c2` dispatch + adds a `Split` copy, forfeiting the #1134/#1139 wins. Input-RMSNorm→QKV fold and down-fold cut only launches/tiny-activation-traffic (not weight bytes) → predicted flat + RMS-reduction-delicate + lose per-kernel tuning. gate/up & lm_head already fused.

**Root cause (now evidenced 5× — preperm, cp.async, down_occ, qo_occ, QKV-fusion all fail for the SAME reason):**
1. Decode is **weight-DRAM-bandwidth-bound** — every GEMV streams its full disjoint int4 weights once/token; no batch-1 lever (fusion, occupancy, pipelining) reduces those bytes.
2. **CUDA graphs already amortize launch overhead** — the classic fusion win is pre-captured; only ~1.1us GPU bubbles + ~10KB activation round-trips remain (<0.3% of traffic).
3. The 3× DRAM-bandwidth headroom only converts by raising **arithmetic intensity** — i.e. amortizing each weight read across **M>1 token-columns** (batched verify / spec-decode territory, which is additive-only and shelved for fast-base) OR **lower-bit/mixed-precision quant** (cuts weight bytes directly — oracle-gated, Rachael's domain, NOT byte-identical).

**Campaign takeaway:** batch-1 byte-identical decode is at the bandwidth floor and native already leads ORT on all 3 int4 models. Further headline gains require either (a) proving the ORT lead holds under equal graph conditions (fairness follow-up), (b) net-new model support (GLM/DeepSeek — untouched mandate half), or (c) oracle-gated lower-bit quant.

## GEMV-family decode survey (Luv, 2026-08-17 — where the leverage is)

Ranked decode mix (nsys, 14b, GEMV family ≈ 86.8% of decode):
1. **gate/up SwiGLU+RMS `_vec_occ` — 42.8%**, occ 82%, Long-SB ~52% stall. #1134+#1137+#1139 done → **near floor** (preperm + cp.async both NO-GO'd here).
2. **MLP down-proj `..._scales_f16_down_c2` — 24.4%**, DRAM 39.9%, occ **66%** (regs 39 cap theoretical to 75%), Long-SB 9.4 + Barrier 2.7. Grid-fill only, **MISSING #1139 launch_bounds** → **next lever**.
3. attn q/o `..._pipe` — 14.7%, occ **59%** (regs 40), Long-SB 11.2. #1134 pipe done, missing launch_bounds → **secondary lever**.
4. lm_head `..._rmsnorm` — 4.9%, occ 91%, issue-bound → near floor, skip.

Whole family is **Long-Scoreboard (global int4 weight-load latency) bound**, DRAM 30–40% of peak → ~2.5–3× BW headroom. The lever that converts is **occupancy-based latency hiding** (#1139: +2.6%), NOT bandwidth tricks (cp.async/PF=2 both NO-GO on issue-bound kernels). 128-bit gate/up loads NOT applicable (already 128B-coalesced; per-lane widening reorders accumulate → breaks byte-identity). down_c2 already uses uint4 loads → its headroom is occupancy, not load-width.

## In flight / next levers

Batch-1 kernel vein mined out (5 NO-GOs). Three parallel fronts, all higher-value than another batch-1 micro-opt:

- **✅ LANDED (#1273, 3747a68) — general head-size fused f32 GQA decode kernel:** the fused f32 decode fast path was capped at `MAX_HEAD_DIM=128` → qwen3.5-2b's head_dim=256 fell to `gqa_attention_reference_f32` = **31% of decode**. Deckard generalized it via per-DPL `template<int DPL>` specialization (`GQA_DECODE_ENTRIES(N)` emits DPL 1..8, `dims_per_lane=ceil(head_dim/32)`), now covering head_dim {64,80,96,112,128,192,256} — each **byte-identical** vs the CPU f64 reference (max_abs ≤1.79e-7), 19 gqa tests pass. Headline **qwen3.5-2b head256 102.31→170.97 tok/s (1.67×)**, reference-kernel hotspot **31.2%→1.1%**, no head-128 (qwen2.5) regression, no register spills (small heads keep their narrow footprint). **Follow-up (named, deferred):** asymmetric "dual head size" (v_head_dim ≠ qk_head_dim — Gemma family, DeepSeek MLA) audited GO-not-needed-yet — ONNX GQA is symmetric (single `head_size` for Q/K/V), no current model needs it; exact code change to thread a separate `v_head_dim` is documented in Deckard's decision note for when an asymmetric export appears.
- **Support gaps surfaced by the qwen3.5 probe (Wallace 2026-08-18):** (a) **Phi-3.5-mini-instruct generic-cpu** native load FAILS on the `If` control-flow op (all other ops supported) — same control-flow class that blocks ORT CUDA-graph; needs native `If`/subgraph support or `ONNX_GENAI_BACKEND=ort`. (b) **qwen3.5-9b multimodal** (embedding+text+vision, `inputs_embeds` path) unmeasured — `profile_native` expects a single decoder .onnx; needs the embeds pipeline (harness limitation, not an EP gap).
- **Wallace — ORT fairness follow-up (in flight):** THE open question on the headline claim. Native leads ORT (1.33/1.13/1.80×) but native runs a captured CUDA graph while ORT runs eager. Get ORT into CUDA-graph mode (static-shape IO binding) for graph-vs-graph, or isolate the kernel-only delta via eager-vs-eager. Either outcome is honest.
- **🎯 V2-Lite CUDA-graph-capture UNLOCK (top lever, 2-STAGE — stage 1 done, stage 2 IN FLIGHT):** Wallace scoped GO → Deckard implemented the mask classifier → but a SECOND pre-existing blocker gates the real win.
  - **Stage 1 ✅ MERGED — Wallace's corrected classifier (PR #1171, main @ `bc1e97ff`):** topology-gated capacity-classifier in `executor/{geometry,build}.rs`. Deckard's original was 🔴 REJECTED (2 safety blockers); Deckard LOCKED OUT, **Wallace revised** (same capacity-policy surface he scoped), Rachael 🟢 re-approved, admin-merged. Both blockers fixed, strictly TIGHTENING: (1) `is_capacity_form_attention_mask_input` requires present-KV `inputs[4]`+`inputs[5]` (grounded `standard_attention.rs:822-840`), test `mask_builder_to_attention_without_past_kv_is_rejected`; (2) dropped `value != mask` exception so a root mask that escapes as graph output is rejected, test `mask_binding_that_is_graph_output_is_rejected`. GLM-5.2 `Add` exclusion preserved.
  - **Stage 2 ✅ MERGED — LEON'S `_d1` FIX = 1.79x WIN (PR #1181, main @ `c9c7f64c`):** additive-mask query axis `_d1` (`v_model.Unsqueeze_18` `['batch',1,'_d1','past+seq']`) was an unresolved internal symbol → prepare-only planner refused the Attention workspace. Leon's fix = EXACT recovery through the V2 mask cone (`Shape/Sub/Slice/Unsqueeze/Where/Cast`) → `_d1`=`sequence_len`, no max-seq guess. Fail-closed: unsupported chains fall back to bounded/error, never guess. Rachael 🟢 (planner recovery exact/deterministic/fail-closed, dense planning unchanged, new test asserts `[1,1,1,2048]`) + Wallace 🟢 GO (**0.000% divergence over 320 tokens; capture 101.80 vs eager 56.94 tok/s median = 1.79x, zero arm overlap** min-ON 100.57 > max-OFF 57.00). Qwen3 dense reservation unchanged (983040 B), eager untouched, opt-in `ONNX_GENAI_CUDA_GRAPH` no default flip. **THE graph-capture top lever is FULLY LANDED (classifier #1171 + `_d1` fix #1181). The DeepSeek MoE performance half of the mandate is CLOSED under equal conditions — byte-identical base decode, opt-in flag, NOT spec-decode. Durable +79% architectural moat: extends graph-capture to the MoE decode path, a capability ORT's CUDA EP structurally can't provide on dynamic-KV int4.** Follow-ups: **Leon's Engine long-context under-plan ✅ MERGED (PR #1189, main @ `b416a3e0`)** — `Engine::generate` held a prefill-era prepared Attention workspace (16904 B) across KV-bucket growth; at ~320 tok Attention needs 33288 B and `workspace_preparation_required` correctly blocks execute-time growth → mismatch in BOTH eager+capture. Fix = new `DecodeCudaState::prepare_decode_workspace_after_capacity_growth(session, grew)` that re-runs governed `prepare_with_device_bindings` ONLY when `grew==true`, at all 3 decode-step sites, before execute/capture/replay (persistent bindings, no mid-replay corruption). Rachael 🟢 (grew-gated, correct timing, dense/short-context no-op) + Leon's own A/B (V2-Lite 340 tok eager 47.32 + capture 89.69 token-identical, captures=2 replays=336 fallbacks=0; 181 session-lib + Qwen3 dense CUDA lock green). Real correctness gap for long generations on the Engine path — closed. **Wallace's Native-vs-ORT V2-Lite measurement ✅ DONE:** ORT CUDA EP structurally can't run the int4 QMoE export on GPU (26× QMoE nodes → CPU fallback, 0.17 tok/s) — native (101.68 capture) is the only engine serving it on GPU. Both DeepSeek halves of the mandate (perf + support) now closed vs ORT, not just vs our own eager.
- **✅ MERGED — Luv QMoE gate/up occupancy lever (PR #1167, main @ `651901a3`):** flag `ONNX_GENAI_QMOE_OCC` default-OFF. Ported dense #1139 gate/up occupancy win to fused gate/up SwiGLU expert GEMV. ncu: 54 reg/50% theo/43.3% ach/42.3us → (256,6) 40 reg spill-free/75% theo/63.8% ach/37.8us (−10.6% kernel). Byte-identical (`qmoe_occ_is_bit_identical` 0 bits; qmoe_gpu 32/32; E2E tokens=golden). Rachael 🟡. Note: flag selects `_occ` for any QMoE QuantLayout when enabled. Discovered+fixed pre-existing fmt drift blocking all PRs' Fast check (#1172 landed same fix independently). `qmoe_linear`/grouped-GEMM at floor — **QMoE occupancy vein mined out.** Branch deleted, worktrees cleaned.
- **✅ MERGED #1150 (main @ e075a715) — DeepSeek-V2-Lite MoE E2E native + f64-correct CUDA golden.** Combined Leon's planner fix + Luv's oracle correction into one reviewer-gated unit (Rachael 🟢 on both planner regression and oracle re-gate). Golden rebased CPU→native-CUDA (`…207,16,24,1012`), justified by f64 reference (CUDA closer to truth than CPU on every measured QMoE/dense case). New f64-bounded QMoE test + env-gated router probe `ONNX_GENAI_QMOE_ROUTE_DUMP`. Luv rebased onto current main pre-merge and confirmed the golden held through #1129's MoE-claiming change (graphs OFF/ON both pass). Branches `squad/qmoe-divergence-fix` + `squad/leon-deepseek-v2-planner` deleted. **Episode resolution: the "CUDA numerics bug" was a WRONG-ORACLE premise error, not a kernel bug** — CPU-vs-CUDA divergence is benign f32 accumulation-ORDER drift; the repo already treats f64 (not CPU) as truth and never required CPU=CUDA bit-equality (`matmul_nbits_gpu.rs:734` tol `2e-4.max(·*2e-5)`). Token-5 expert-set swap (CPU expert 61 / CUDA expert 1) is a below-fp32-resolution near-tie (logit Δ~5e-5 < reassoc drift ~4.7–6.4e-5); Rachael judged an epsilon tie-break arbitrary and accepted it. Leon's original lock-to-CUDA instinct was right; his SIN (correctly 🔴'd) was doing it SILENTLY — the merged artifact does it explicitly with f64 justification.

## GLM/DeepSeek support scope (Luv, 2026-08-17 — more mature than archive implied)

| Model | Arch | E2E native? | Decode |
|---|---|---|---|
| GLM-4-9B int4 | dense GQA partial-RoPE | ✅ | native-only (ORT can't load it); **128.82 eager / 211.74 captured tok/s = 1.64×, byte-identical over 256 tok, opt-in** [Wallace 2026-08-18, H200]. **Graph capture ✅ engages with ZERO code change** — V2-Lite capture stack (#1171+#1181+#1189) already covers GLM's dense GQA topology; `captures=3 replays=185 fallbacks=0`, independent lock `glm4_9b_decode_lock.rs`. |
| DeepSeek-R1-distill-1.5B | dense Qwen | ✅ | 690 tok/s |
| DeepSeek-V2-Lite int4 (MoE) | QMoE + std Attention | ✅ supported + f64-correct (#1150) | **native fp32-act 55.15 tok/s short** [Sapper 2026-07-27 fair CUDA-vs-CUDA, H200, medians of 5]; golden locked to native-CUDA validated vs f64. **⚠️ "ORT CANNOT run it" is DISPROVEN** — with an fp16-activation export, ORT CUDA places all 26 QMoE nodes on GPU (0 Memcpy) at **86.78 tok/s = 1.57× FASTER than native**. The old "0.17 tok/s / ONLY engine on GPU" was a CPU-fallback artifact (harness pulled CPU-only ORT + fp32-act export ORT drops to CPU). Native's QMoE kernel is **fp32-activation-only (no fp16 tensor-core path)** = the real decode lever. Deep-ctx (~2600) native currently aborts (Attention workspace not resized for large KV — Sapper investigating). Graph-capture path (#1171/#1181/#1189) + derive_len #1357 + cumsum #1366 remain valid native-internal wins. |
| GLM-5.2 MoE (CSA/IndexShare) | MoE + sparse attn | tiny fixtures only | — |

**Key structural facts:** MLA is NOT a custom op — DeepSeek-V2 exports as standard `RotaryEmbedding`+`Attention` → **no MLA-kernel gap**. QMoE router+expert-GEMV, CompressedSparseAttention (V4-Flash/GLM-5.2), IndexShare/DSA CUDA kernels **all already exist** + CPU-oracle validated. Gaps: (1) V2-Lite MoE blocked E2E in workspace planner → Leon; (2) QMoE expert-GEMV under-optimized (pre-campaign schedule) → Luv; (3) native MLA kernel — no model needs it today, low priority; (4) GLM-5.2 MoE perf — kernels exist, blocked on a real int4 artifact.

- **Deferred (oracle-gated, Rachael's domain if pursued):** lower-bit/mixed-precision quant — the only remaining lever that cuts weight-bytes-per-token, but not byte-identical.
- Foundry-local ORT benchmarking remains the fairness reference for every native-vs-ORT claim.

## Spec-decode status (shelved)

Model-draft spec-decode on qwen2.5-14b-int4 is an honest **NO-GO** for this fast-base config (best E2E net 0.974× @ W=4, byte-identical; free-draft ceiling ~1.175× < 1.25× gate). Byte-identical machinery is built and shelved on `squad/spec-*` branches for future slower-base/different-draft configs. No spec-decode ORT-win claim allowed.

**Gate-3 B\* re-probe post-Marlin (2026-08-18, Luv) = NO-GO, and it re-scoped the blocker.** The settled fund/kill gate for the ENTIRE spec-decode family (model-draft, prompt-lookup/n-gram, EAGLE-3/MTP) is the captured verify break-even `B* = C_verify(captured M=K)/C_decode(captured M=1)`; GO only near ≤~2. Measured on current main (`923dc592`, Marlin #960/#961/#962 landed) on qwen2.5-14b-zp: **B\* = 17.5/18.4/20.0× at K=2/4/8** (captured M=1 primitive 5.915 ms; M=K verify 103–118 ms; 96 capture segments). qwen2.5-7b: **B\* = 14.9/15.7/17.4×** (29 segments). Marlin did NOT collapse the cliff and B\* is WORSE than the 2026-08-14 baseline of 8.5. **KEY re-scope:** the M>1 `KernelCaptureUnsupported` seam is NO LONGER GQA/SkipSimplifiedLayerNorm (those two #957 "cheap residual fixes" are now irrelevant — they don't appear as M>1 seams). The sole remaining blocker is **`MatMulNBits` at M>1** — the captured M=K graph splits and launches `matmul_nbits_gemm_f16` eagerly (nsys: 76.9% of GPU kernel time, 12,469 eager instances at qwen14). **Marlin's capture-safe M>1 int4 GEMM is NOT being selected for the MatMulNBits M>1 path.** The actionable question (next lever): is that a fixable routing/wiring gap (route MatMulNBits M>1 → Marlin, which is capture-safe by construction) or a permanent kernel-capability wall? **ANSWERED 2026-08-18 (marlin-routing scope + Luv re-probe): WIRING GAP — and the wiring fix WORKS mechanically but spec-decode STILL NO-GO on cost.** Marlin M>1 is capture-safe (`crates/onnx-runtime-ep-cuda/src/kernels/marlin_gemm.rs:96-110`), gated behind opt-in env `ONNX_GENAI_MARLIN_M_GT_1={1|true|on}` (matmul_nbits.rs:6987-7129; hard-safe predicates bits==4, SM80+, no fused swiglu/silu/rmsnorm-prologue). Luv re-ran Gate-3 with the env flag ON (zero code change): capture fragmentation **eliminated** (qwen14 96→1 segments, qwen7 29→1, seams gone), top kernel switched to `matmul_nbits_marlin_gemm_f16_splitk`, K=8 byte-identity **PASS**. **But B\* only dropped ~15-20 → 5.19/5.19/5.79× (qwen14) and 4.64/4.71/5.23× (qwen7) @ K=2/4/8** — still above the ≥4 NO-GO gate, well above ≤2 GO. **SPEC-DECODE FAMILY NOW DEFINITIVELY MINED OUT (3 rigorous probes: model-draft 0.974×; n-gram post-Marlin B\*~15-20; n-gram Marlin-M>1 B\*~5-6).** The intern's "acceptance 2-3 sufficient" is measured-false; do NOT fund the verify-graph build. Residual verify cost is no longer fragmentation but Marlin M>1 GEMM/repack/reduce — nsys shows a per-call `matmul_nbits_marlin_repack` in the hot path (12,469 gemm instances at qwen14).

**PIVOT (2026-08-18) — the Marlin M>1 capture-safe path is a PREFILL/TTFT lever, not a spec-decode lever.** M>1 is exactly the prompt-prefill workload. Marlin M>1 is byte-identical + single-capture + off-by-default. Two threads to scope: (a) is `matmul_nbits_marlin_repack` per-call or one-time? **ANSWERED: one-time `OnceLock` cached (`marlin_gemm.rs:140-176`), no hoist needed.** (b) Is Marlin M>1 faster than portable tiled GEMM for real prefill shapes? **ANSWERED 2026-08-18 (Luv prefill A/B, real e2e TTFT via `profile_native`): NO — keep opt-in.** qwen14 TTFT Marlin/portable = 0.976/0.988/0.999× @ M=128/512/2048 (only a ~2.4% M=128 win, neutral by 2048); qwen7 = 1.005/1.013/1.001× (neutral-to-worse). Argmax matched every arm, BUT full-vocab logprob dumps (152064-way) are **NOT byte-identical** ON vs OFF (max abs Δ 0.017 qwen14, 0.168 qwen7) — fails the silent-default byte-identity bar. **VERDICT: Marlin M>1 stays opt-in (`ONNX_GENAI_MARLIN_M_GT_1` default-off is correct).** The entire Marlin-M>1 thread is now MINED OUT: not a spec-decode win (B\*~5-6), not a prefill win (neutral), not byte-identical. Evidence: `.worktrees/luv-gate3-bstar-postmarlin/target/prefill_mgt1/` + `gate3_mgt1/`. n-gram/prompt-lookup's "acceptance 2-3 sufficient" is FALSE at B\*≈15-20. Evidence: `.worktrees/luv-gate3-bstar-postmarlin/target/gate3/`.

## Standing facts

- Canonical greedy/argmax tie-break is **LOWEST index** (ONNX ArgMax `select_last_index=false`, matching ORT); all decode/sampling/argmax paths must keep the lowest token id on ties for byte-identical ORT parity.
- Native-vs-ORT comparisons must use identical weights, optimized CUDA decode configs, steady-state methodology, and oracle/token fairness. Base native decode is compared against ORT base; spec decode is additive and cannot be the ORT-win basis.
- CUDA-graph decode profiling must use `nsys --cuda-graph-trace=node` / `ncu --graph-profiling node`; eager op profiling can mis-rank launch-heavy ops. The per-op timer is blind to captured CUDA-graph replay.
- Keep shipped decode optimizations byte-identical or oracle-gated, portable/default-on only when validated, guarded by honest A/B env vars.

## Prior waves (compressed)

Earlier August work delivered native CUDA capture, bf16/int4 decode improvements, Marlin M>1 speculative-verify capture, DeepSeek/QMoE fixtures, split-K/SkipRMSNorm/128-bit-wideload GEMV wins, and testing directives. Detail lives in `.squad/decisions-archive/2026-08.md` and agent history archives.
