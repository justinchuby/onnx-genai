name: Rust security audit

on:
  schedule:
    - cron: "0 6 * * 1"
  push:
    branches:
      - main
    # Not a required check. paths-ignore only applies to push/pull_request,
    # so schedule and workflow_dispatch runs below are unaffected.
    paths-ignore:
      - "docs/**"
      - "wiki/**"
      - "site/**"
      - "**/*.md"
      - "**/*.mdx"
      - "**/*.markdown"
      - "LICENSE"
      - "NOTICE"
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - "docs/**"
      - "wiki/**"
      - "site/**"
      - "**/*.md"
      - "**/*.mdx"
      - "**/*.markdown"
      - "LICENSE"
      - "NOTICE"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      # Keep audit on Rust stable deliberately: this follows Rust's stability promise
      # without freezing security fixes.
      - name: Install Rust stable toolchain
        shell: bash
        run: |
          rustup toolchain install stable --profile minimal
          rustup default stable
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      - name: Check RustSec advisories
        # Default cargo-audit policy fails on vulnerabilities but allows
        # informational warnings such as unmaintained transitive crates.
        run: cargo audit
