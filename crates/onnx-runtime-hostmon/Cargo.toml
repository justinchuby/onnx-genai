[package]
name = "onnx-runtime-hostmon"
version.workspace = true
edition = "2024"
description = "Foreign-CPU contention on a process's confined core set, for trustworthy benchmarks"
license = "MIT"
repository = "https://github.com/justinchuby/onnx-genai"

[dependencies]
libc = "0.2"
