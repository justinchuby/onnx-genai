[package]
name = "onnx-runtime-tracer"
description = "Unified, pure-Rust tracing for the ORT 2.0 runtime: collect timed spans and export them as Chrome Trace Event Format JSON for Perfetto / chrome://tracing"
version = "0.1.0-dev.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["onnx", "tracing", "profiling", "perfetto", "chrome-trace"]
categories = ["development-tools::profiling", "science"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
