pipeline:
  models:
    decoder:
      filename: decoder.onnx.textproto
      type: decoder
  dataflow:
    - from: encoder.hidden
      to: decoder.encoder_hidden_states
  strategy:
    kind: autoregressive
    decoder: decoder
