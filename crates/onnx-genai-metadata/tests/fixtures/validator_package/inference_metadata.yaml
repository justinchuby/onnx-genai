schema_version: v1
pipeline:
  workflow:
    manifest:
      capabilities:
        - workflow_ssa
    components:
      identity:
        implementation:
          kind: onnx
          artifact: identity.onnx.textproto
        ports: {}
    steps:
      - kind: invoke
        component: identity
