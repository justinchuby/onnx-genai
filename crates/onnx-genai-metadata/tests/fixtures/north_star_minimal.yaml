schema_version: v1
pipeline:
  workflow:
    manifest:
      capabilities:
        - workflow_ssa
    components:
      noop:
        implementation:
          kind: binding
        ports: {}
    steps:
      - kind: invoke
        component: noop
