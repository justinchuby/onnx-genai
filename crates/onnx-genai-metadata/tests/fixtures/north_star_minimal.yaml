schema_version: v1
pipeline:
  workflow:
    manifest: {}
    components:
      noop:
        implementation:
          kind: binding
        ports: {}
    steps:
      - kind: invoke
        component: noop
