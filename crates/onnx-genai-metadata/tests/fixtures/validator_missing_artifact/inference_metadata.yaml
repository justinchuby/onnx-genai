pipeline:
  workflow:
    manifest: {}
    components:
      missing:
        implementation:
          kind: onnx
          artifact: missing.onnx.textproto
        ports: {}
    steps:
      - kind: invoke
        component: missing
