pipeline:
  workflow:
    manifest:
      ir_version: "1.0"
      onnx_opsets:
        ai.onnx: 13
      capabilities:
        - workflow_ssa
    components:
      missing:
        implementation:
          kind: onnx
          artifact: missing.onnx
        ports: {}
    steps:
      - kind: invoke
        component: missing
