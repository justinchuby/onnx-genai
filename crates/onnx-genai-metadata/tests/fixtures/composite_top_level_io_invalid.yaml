model:
  io:
    token_input: input_ids
pipeline:
  models:
    decoder:
      filename: decoder.onnx.textproto
      type: decoder
      io:
        token_input: input_ids
  strategy:
    kind: autoregressive
    decoder: decoder
