[package]
name = "onnx-runtime-ep-nxrt-testplugin"
description = "Test-fixture nxrt plugin cdylib for integration testing (Pris owns)"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
onnx-runtime-ep-api = { path = "../onnx-runtime-ep-api" }

[lints.clippy]
all = "warn"
