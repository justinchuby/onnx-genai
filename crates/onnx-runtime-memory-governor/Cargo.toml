[package]
name = "onnx-runtime-memory-governor"
description = "Tiered memory governance contract: one authority owns the VRAM/RAM/disk budgets and every component that holds bytes leases them, so placement, eviction and offloading can be decided in one place"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
keywords = ["onnx", "memory", "governor", "runtime", "inference"]
categories = ["memory-management", "science"]

[dependencies]
onnx-runtime-memory-api = { workspace = true }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "large_alloc_cache"
harness = false

[lints]
workspace = true
