# onnx-genai — Implementation Progress

Tracks implementation status of `docs/DESIGN.md` (§1–§40). Updated as work lands.

**Published:** `onnx-genai` v0.1.0 + 8 sub-crates on crates.io. CI (fmt/build/test/**blocking clippy**) + scheduled `cargo-audit`. Coverage ~77% line.

_Last updated: 2026-07-14_

## ORT 2.0 — from-scratch Rust ONNX runtime (`docs/ORT2.md`, big design — steady progress)

New parallel track (user co-designed the design with the team). Goal: a from-scratch Rust ONNX runtime living in this monorepo — own strided IR (symbolic shapes + device placement first-class), plugin-EP contract that preserves the ORT graph ABI (the EP ecosystem is the moat), cost-model-driven placement, async executor. Backend swap via a `backend-ort2` feature in `onnx-genai-engine`. Roadmap: `docs/ORT2-IMPL-PLAN.md`.

- **Phase 1 foundation ✅ landed (`203161c`, Roy/Lead):** 6 `onnx-runtime-*` crates scaffolded (additive; no existing crate touched). **`onnx-runtime-ir` fully implemented** (`#![forbid(unsafe_code)]`, **34 passing tests**) — the frozen public contract: `DataType` (ONNX-numbered, sub-byte packing), symbolic `Shape`/`Dim`/`SymbolConstraints`, strided `TensorLayout`/`MemoryFormat` + broadcast math, first-class `DeviceId`, arena `Graph` with edge-consistent mutation, deterministic Kahn `topological_order`, `validate`, `IrError`/`GraphError`. `loader`/`ep-api`/`ep-cpu`/`session`/`capi` are compiling skeletons.
- **Phase 1 — all 6 crates ✅ MERGED (`8c9c8fc`), ~128 tests green together:**
  - ✅ **`onnx-runtime-ir`** (Roy, `203161c`, 34 tests) — frozen contract.
  - ✅ **`onnx-runtime-ep-api`** (Track B, Batty, `65ec9f6`, 17 tests, reviewed 🟡 Holden) — `DeviceBuffer` unique-ownership (leak-over-double-free), sound Send/Sync, DLPack `byte_offset`/negative-stride/sub-byte, `Cost` `#[non_exhaustive]`.
  - ✅ **`onnx-runtime-loader`** (Track A, Deckard, `7e0e367`+`dd5297d`, 18 tests, reviewed 🟡 Gaff) — pure-Rust protox (no system protoc), graph-builder, mmap external weights; `load_model_with_weights() -> (Graph, Arc<WeightStore>)` keeps mmaps live for the executor; `norm_axis` clamp.
  - ✅ **`onnx-runtime-ep-cpu`** (Track C, Batty, `ea30279`, 39 tests, reviewed 🟡 Chew numerics + 🟡 Holden safety) — **pure-Rust** reference kernels (MatMul/Add/Relu/Reshape/Transpose/Gather/LayerNorm; oneDNN deferred to Phase-1.5 behind the `Kernel` seam), `strided::view_in_bounds` bounds gate.
  - ✅ **`onnx-runtime-session`** (Track D, Roy, `24b8129`, 8 tests, **Miri-clean**, reviewed 🧪 Chew 🟢 + 🛡️ Holden 🟡) — sequential CPU executor: `load_model_with_weights` → CPU auto-detect → per-node kernel resolution into a shape-keyed cache → topo-order `run()` materializing views + dispatching kernels, Holden's `view_in_bounds` gate on every input/output view before every dispatch. Owned host `Tensor` (single-free Drop).
  - ✅ **`onnx-runtime-capi`** (Track E, Batty, `8c9c8fc`, 12 tests, **Miri-clean**, reviewed 🛡️ Holden 🟢) — Phase-1 Tier-1 C ABI: `ort2_create_session/run/create_tensor/release_*` + accessors + owned `OrtStatus`; all FFI unsafe isolated, panic-fenced via `catch_unwind`, null-checked handles, double-release unreachable.
- **Phase-1 exit milestone ✅ ACHIEVED & MERGED (`85f379b`, Roy):** `bert_toy_optimized.onnx` (opset-12 BERT-toy, 384 nodes) runs **end-to-end** through `onnx-runtime-session` on the pure-Rust CPU EP and **matches onnxruntime 1.27.0 (CPUEP) to float32 rounding** — `prediction_scores` [1,8,99] max_abs **1.19e-7** (max_rel 3.4e-5), `seq_relationship_score` [1,2] max_abs 6.0e-9; 0 elements exceeded tol (atol=rtol=2e-3). **Zero cross-crate fixes needed** — ir/ep-api/ep-cpu/loader were all correct on the first real-model run. Enabling work (all merged): 🔧 **Batty** ep-cpu op expansion (+17 kernels, 90 tests, `e485a83`); 🏗️ **Roy** session symbolic-shape resolution (`da8eab3`); 📦 **Deckard** loader const-fold-lite shape inference (`b6f032e`). Session-local JIT fix: data-dependent output shapes (e.g. a `Slice` whose `ends` comes from a runtime `Shape→Min→Cast` chain) resolve just-in-time in the run loop (model-agnostic, dispatch on op type; Holden's `view_in_bounds` gate preserved, no new unsafe). Committed fixture + python reference generator + self-contained Rust conformance test (session 14→15 tests). Reviews (non-author): 🧪 **Chew** correctness 🟡 SHIP (Slice sizer verified char-for-char vs kernel — no size mismatch), 🛡️ **Holden** safety 🟡 SHIP (all 4 soundness invariants HELD, miri-clean). Design seam validated: loader=shape-inference, session=symbolic→concrete resolution, ep-cpu=compute.
- **Phase-2 shape inference ✅ crate landed (`onnx-runtime-shape-inference`, Roy):** general symbolic shape inference over the `onnx-runtime-ir` `Graph` — an extensible per-op registry keyed by `(domain, op_type, opset)` with range-based version dispatch (`ai.onnx` + `com.microsoft`), a canonical `DimExpr` symbolic-dim polynomial (affine/product forms + reshape `-1` symbol cancellation), first-class **shape-DATA propagation** (`Shape→Gather→Unsqueeze→Concat→Slice→Reshape` chains resolve statically), and strict/permissive merge policies. **40+ op handlers** (MatMul/Gemm, broadcast elementwise, LayerNorm/Softmax/ReduceMean+family, Reshape/Transpose/Gather/Concat/Slice/Squeeze/Unsqueeze/Expand/Where/Split/Flatten, Shape/Size/Constant/ConstantOfShape/Cast/Identity, Conv/MaxPool/AveragePool/Pad). `#![forbid(unsafe_code)]`, clippy `-D warnings` clean, **56 tests** (10 dim-expr + 42 per-op unit + 3 whole-graph + 1 doctest) including an **end-to-end `bert_toy` test where EVERY value fully resolves** and a symbolic-batch `N` propagation test through MatMul/Add/Reshape. Frozen ir contract untouched (fresh symbols minted at/above `0x8000_0000`). Wiring/migration to replace the loader `const-fold-lite` + session JIT stopgaps is a follow-up.
- **Phase-2 shape inference ✅ WIRED into the loader (Roy):** `onnx-runtime-shape-inference` now runs inside `onnx-runtime-loader` (`load_model*` calls `InferenceRegistry::default_registry().infer_graph(&mut graph, &opset_imports, Permissive)` after the graph + initializers are built); the loader `const-fold-lite` pass (`shape_inference.rs`, ~1.1k LOC) is **retired/deleted** (no back-compat shim); the session **JIT stays as a fallback-only** safety net for genuinely data-dependent extents (e.g. a `Slice` whose `ends` come from a runtime `Shape→Min→Cast` chain). One inference fix required for a bindable graph: broadcasting two distinct symbolic dims now keeps a deterministic *representative* symbol (prefer a named low-range graph symbol over an anonymous fresh one) instead of minting an unbindable fresh symbol — this re-unifies data-dependent extents onto the graph's real `batch`/`seq` dims. Conformance held: `bert_toy` still matches onnxruntime 1.27 CPUEP, max_abs **1.192e-7**; all ORT2 pure-Rust crates green, clippy clean.
- **Design: EPContext-node support now specified (`docs/ORT2.md` §55, Roy):** ORT `com.microsoft::EPContext` contrib-op — full attribute schema, consume (loader recognition + `source`-keyed EP dispatch + embed/external blob resolution + `main_context` multi-graph) and generate (`*_ctx.onnx` writer for `ep.context_enable`) paths, `ep.context_*` C-API options (§21.4), model-agnostic dispatch, mapped to the internal §4 `EpContext`. **Phase-2 scope** (foundational for CUDA/QNN/OpenVINO/TensorRT EPs; no Phase-1 EP consumes it). Docs only — no crate code.
- **Design: CUDA EP kernel stack now specified (`docs/ORT2.md` §15, Roy):** retitled "CUDA EP Kernel Strategy" — the decided, pure-Rust stack for how `onnx-runtime-ep-cuda` talks to the GPU: **`cudarc`** foundation (driver/streams/memory + cuBLAS/cuBLASLt/cuDNN/NVRTC bindings), **cuBLASLt** standard GEMM (fused Bias+Act epilogue), **CuTe (CUTLASS 3.x C++)** custom fused kernels via `extern "C"`/`nvcc`-in-`build.rs` (SM90 TMA/WGMMA + SM80 fallback), attention phased **cuDNN SDPA (2a) → FlashAttention-3 shim (2b)**, NVRTC for trivial elementwise; supplemental ThunderKittens noted. **cuTile deferred to Phase 3** (no Hopper/SM90, Python-only — dated citations); Rust-CUDA/Triton-AOT/TileLang/Mojo rejected for production. Model-agnostic hard rule restated (shape-driven, dtype-templated, arch-gated; attention params are runtime args). Crate-layout + `build.rs` sketch included. Docs only — no crate code.
- **Deferred (non-blocking for BERT):** IR-hardening pass (fp8/int4 ONNX numbering fix, FNUZ/FLOAT4E2M1, unknown DataType, unknown-rank Shape, Attribute tensor-list variants — needed before quantized-model milestone).
- **Open design questions (resolved as Phase 1 landed):** owned device-aware `Tensor` — **kept in `session`** for P1 (host-only; doc-noted to move to a shared `onnx-runtime-tensor` crate when `ep-cuda` lands); protobuf via pure-Rust protox + vendored `onnx.proto3` (no system protoc); sub-byte sizing uses `storage_bytes`; `capi` crate-type stays `lib` until entry points land. Still open: `OrtGraphView` in `ep-api::abi` (Phase-2 FFI, `todo!()`).



## Status by design section

| § | Feature | Status | Notes |
|---|---------|--------|-------|
| 1–8 | Vision, architecture, core components, data flow, concurrency, model dir, crates, deps | ✅ Done | |
| 9 | API surface | 🟡 Partial | chat/completions/models/sessions/status/metrics/audio ✅; `/v1/embeddings` (#7) ✅ wired to engine pooled-embedding API; logprobs(#8) ✅; `/v1/debug/*` (config/sessions/kv/trace) ✅; **Perfetto trace export (#13) ✅** at `/v1/debug/trace/perfetto` (static-str-only event names, gate-parity, model-agnostic; OTLP explicitly deferred) — Zhora, reviewed 🟢 Deckard, merged `8d1bf3d` |
| 11,12,15 | Testing, design decisions | ✅ Done | coverage ~77% |
| 16 | Quantized models | ✅ Done | EP select + int8 KV; fp8/int8 KV **storage now selectable** end-to-end (#15) via `--kv-cache-dtype`/`ONNX_GENAI_KV_CACHE_DTYPE` (f32/int8/fp8_e4m3fn/fp8_e5m2) → `PageTensorConfig.dtype` |
| 17 | Diffusion pipeline (image) | ❌ Missing | #16 |
| 18,19 | ORT wrapper, dep graph | ✅ Done | |
| 20 | Generalized pipeline | 🟡 Partial | AR/composite/single-pass/vision/audio ✅; iterative diffusion pending |
| 21 | Tool use / function calling | ✅ Done | Hermes-verified E2E |
| 22 | Grammar constrained decoding | ✅ Done | llguidance JSON-schema/regex/lark |
| 23 | FIM / infilling | ✅ Done | engine + `POST /v1/completions` |
| 24 | Sampling policy | ✅ Done | full sampler suite; **real RNG fixed 2026-07-12** |
| 25 | Extensibility | ✅ Done | DecodeBackend/SpeculativeProposer/Sampler traits |
| 26 | Multi-agent serving | ✅ Done | batched continuous serving (~6× throughput) |
| 27 | Multi-token speculative | ✅ Done | draft + prompt-lookup + MTP + EAGLE-3 + **Gemma4 assistant (shared-KV proposer)** — validated **token-identical on real Gemma4 E2B weights on CUDA/H200** (Milestone B, `10f82b3`). **Acceptance bug root-caused + fixed** (Leon, branch `squad/gemma4-accept`, review in flight): assistant needs `inputs_embeds=concat(target_input_embedding(last_token), last_hidden)`; engine had fed two backbone-hidden vectors → deterministic `multi_token_accepts=0`. Fix adds optional `input_embedding` metadata field + `LinearEmbedder`. **acceptance 25%→70.6%, multi_token_accepts 0→12/17**, still token-identical. Raw speedup still <1× (drafter lm_head compute cost) — separate perf follow-up |
| 28 | vLLM speculator compat | ✅ Done | config auto-discovery + EAGLE-3 proposer |
| 29 | Language diffusion | ❌ Missing | large |
| 31 | Observability | 🟡 Partial | `/metrics` + `/v1/status` + trace ids + `/v1/debug/*` ✅; **Perfetto/Chrome trace export (#13) ✅** (`/v1/debug/trace/perfetto`, JSON download, empty-honest); OTLP still deferred |
| 32 | Metrics API | ✅ Done | |
| 34 | Cluster/session router | ✅ Done | New `onnx-genai-router` crate (§34): R1 server `GET /v1/status` node-status contract (model-agnostic `--node-id`); R2 pure core — YAML `RouterConfig`, `RoutingPolicy` (affinity→prefix→least-loaded + Weighted w/ scored `affinity_weight` bonus), session/prefix affinity maps, migration accounting; R3 runnable binary — hyper/axum reverse proxy (streams SSE, strips hop-by-hop, 16 MiB caps), `/router/{status,sessions,metrics,drain,rebalance}`, concurrent `/v1/status` poller w/ health-miss demotion, Prometheus metrics, SIGINT drain. Model-agnostic (opaque node/session ids); no ORT/engine deps. Reviews: Chew 🟡→fixed (f32 contract, affinity_weight), Deckard 🟡→fixed (concurrent poller, rebalance overload guard). 73+4 tests |
| 35 | Native preprocessing | ✅ Done | `onnx-genai-preprocess`: image (bicubic/CLIP + tiling none/fixed_grid/dynamic_anyres) + audio log-mel; audio wired (#12). Multi-tile prompt token-expansion library (`expand_image_placeholders`, thumbnail-order-safe) ✅; engine count-based expansion + server tile-count seam wired (#14) ✅ |
| 36 | Backpressure/lifecycle | ✅ Done | admission cap + 429 ✅; configurable `max_queue_depth` (CLI/env) + 429 Retry-After ✅ |
| 37 | Model lifecycle mgmt | ✅ Done | Multi-model registry (#9): M1 `ModelHandle`+`ModelRegistry` refactor (Gaff 🟢); M2 multi-model config (`--models-dir`/`--models-config`, TOML/JSON) + startup load + per-request routing + deterministic default (Chew 🟡→fixed: embeddings empty-model default); M3 runtime load/unload + lazy load + LRU eviction + admin endpoints (`/v1/admin/models`, gated) with lock-safe `spawn_blocking` build + per-id load de-dup + graceful `Arc`-drain unload (Deckard 🟢) |
| 38 | Distributed KV connector | ✅ Done | Pluggable connector abstraction + local-tiered backend, end-to-end: K1 `KvCacheConnector` async trait + **prefix-dependent** chunk-hash `KvCacheKey` (rolling cumulative FNV-1a → equal key ⟹ identical prefix ⟹ safe cross-node reuse) + `NullConnector`; K2 `LocalTieredConnector` bridging existing `PrefixCache`+`PageTable` GPU/CPU tiers + fp8, priority-aware eviction, `cpu_load_ms_per_page`-scaled load estimate; K3 engine wiring — generic `EngineConfig.kv_connector` (`Null`\|`LocalTiered`, no per-model logic), `ConnectorBridge` (private current-thread Tokio runtime driving the async trait from the sync engine std::thread); **K4 real byte materialization** — `KvPayload` carries real f32 KV bytes (head-major `[num_kv_heads, num_tokens, head_dim]`, byte-symmetric with `materialize_sequence` + past-tensor `[1,kv_heads,seq,head_dim]`); engine **extracts on store** + **injects fetched KV on lookup** (`export_/import_runner_kv`), gated by fetch-vs-recompute so a hit **genuinely shortens prefill**. Gold test: connector-served request is **token-identical** to fresh `NullConnector` full recompute *and* `0<fetched_tokens<prompt_len`. `Null` default byte-for-byte inert. Reviews: Chew 🟡→fixed (K1/K2), Deckard 🟢 (K3), Chew 🟡 (K4 — byte-layout/no-false-hits/deferred-path safety all verified). **Deferred (non-blocking, gated no-ops — never faked):** fp8 store compression, non-f32 dtypes, continuing (non-empty) sessions, non-runner/paged decode path, remote/distributed backend (a future impl of the same trait). Follow-ups: multi-layer gold fixture (Pris), graceful recompute fallback on inject failure (Batty). |
| 39 | Paged/radix attention | 🟡 Upstream | Mobius block-table KV graph (Option C, std ops) = draft PR onnxruntime/mobius#395; runtime wiring pending |
| 40 | Sliding window attention | 🟡 Partial | Contiguous single-window SWA (metadata `sliding_window` + KV `apply_sliding_window` + windowed decode) ✅; **attention-sink tokens (StreamingLLM §40.4)** ✅ token-exact `[0,sink)∪[window_start,len)`; per-layer hybrid (§40.3) + discontinuous `position_ids` (§40.8) declined pending Mobius/ORT |

## Open backlog (GitHub issues)

- **#7** `/v1/embeddings` ✅ (wired to `Engine::embed_with_options` via `DriverCommand::Embed`) · **#8** logprobs ✅ (opt-in per-token + top_logprobs, chat+completions, streaming + speculative paths) · **#9** model lifecycle/multi-model · **#13** debug endpoints + **Perfetto ✅ (merged `8d1bf3d`)** · **#15** fp8 KV quant ✅ (selectable fp8/int8 host-mirror storage) · **#16** image diffusion.
- Closed: **#2** server split · **#3** decode ownership · **#4** FIM endpoint · **#5** benchmarks · **#10** EAGLE-3 proposer · **#11** audio log-mel preprocessing · **#12** audio input · **#14** vision preprocessing/tiling.

## Recently completed (this session)

Complete runtime built from scaffold + published: generation (greedy/speculative draft+prompt-lookup+MTP), samplers, FIM, grammar, tool use (Hermes-verified), chat templates, multi-session + prefix cache, paged/tiered/int8 KV, long-context O(1)/token static-cache, batched multi-agent serving, OpenAI HTTP (chat/completions/vision/streaming/sessions), observability, benchmarks (`onnx-genai-bench`), `onnx-genai-preprocess` crate, security hardening, CI + audits. **Fixed: categorical sampling had no RNG (always token 0).**

## Design-backlog push (2026-07-13, session 2)

Reviewed multi-agent batch closing several §-gaps (each committed + reviewed):
- **§13/§31 debug endpoints + §36 backpressure** (Zhora): `/v1/debug/{config,sessions,kv,trace}` + configurable `max_queue_depth` (429+Retry-After). Security-hardened after review (Rachael): endpoints **disabled by default**, opt-in `--enable-debug-endpoints`/`ONNX_GENAI_DEBUG_ENDPOINTS=1` (404 when off, no existence leak); session capability IDs **redacted** (`sess-{8hex}…`, non-replayable).
- **§40 sliding-window attention** (Leon): contiguous single-window SWA was already present; added **attention-sink tokens (StreamingLLM §40.4)** end-to-end (metadata `sink_tokens`, KV sink-aware windowing, engine token-exact `[0,sink)∪[window_start,len)`). Reviewed 🟢 (Chew): boundary math exact, discontinuous-position paths genuinely bail rather than emit wrong output. Per-layer hybrid (§40.3) + discontinuous `position_ids` (§40.8) declined pending Mobius/ORT.
- **§35 multimodal token-expansion**: preprocess `expand_image_placeholders` library (Sapper) — thumbnail token/pixel order made authoritative from tensor layout after review (Deckard). Engine count-based expansion + server tile-count seam wired (#14, Batty); multi-image over-count + `tokens_per_tile>0`/empty guards added after review (Leon). Single-image path complete; multi-image bails cleanly (needs per-image tile counts).
- **§34 cluster/session router** (Roy + Pris): new standalone `onnx-genai-router` crate (hyper/axum reverse proxy, no ORT/engine deps) — session→node affinity, system-prompt prefix affinity (FNV-1a), least-loaded + Weighted policy with scored `affinity_weight` bonus; `/router/{status,sessions,metrics,drain,rebalance}`; concurrent `/v1/status` poller with health-miss demotion; SSE streaming, 16 MiB caps, SIGINT drain. Server side exposes `GET /v1/status` node-status contract + generic `--node-id`. Reviews: Chew 🟡→fixed (f32 contract, affinity_weight read), Deckard 🟡→fixed (concurrent poller, `/v1/sessions` cap, miss-on-unknown-id, rebalance overload guard). 73+4 tests. Model-agnostic (opaque node/session ids).
- **§38 distributed KV connector** (Zhora K1/K2 + Leon K3/K4): pluggable `KvCacheConnector` async trait + **prefix-dependent** chunk-hash `KvCacheKey` (rolling cumulative FNV-1a → equal key ⟹ identical prefix) + `NullConnector` (K1); `LocalTieredConnector` bridging existing `PrefixCache`+`PageTable`+fp8 with priority-aware eviction and `cpu_load_ms_per_page`-scaled load estimate (K2, Chew 🟡→fixed); engine wiring via generic `EngineConfig.kv_connector` + `ConnectorBridge`, engine refcount path kept strictly distinct from the connector's own tiers (K3, Deckard 🟢); **K4 real byte materialization** — `KvPayload` carries real f32 KV (head-major `[kv_heads,tokens,head_dim]`), engine extracts on store + injects on lookup gated by fetch-vs-recompute so a hit genuinely shortens prefill; gold test proves connector-served output is token-identical to full recompute with `0<fetched_tokens<prompt_len` (Chew 🟡 — byte-layout symmetry, no-false-hits, and deferred-path safety all verified). **Deferred (gated no-ops, never faked):** fp8 store compression, non-f32 dtypes, continuing sessions, non-runner/paged path, remote/distributed backend.

_Follow-ups:_ SWA hardening nits (Chew): first-activation `debug_assert`, rewind-into-sink symmetry, draft `sink_tokens` under spec decode.

### Gemma4 multimodal export — scoped (2026-07-13, Sapper); NOT a small patch

> **UPDATE 2026-07-13 (session 2, active):** Now being pushed forward per user ("don't be daunted — change whatever's needed, including the mobius PR"). Focus first on **Gemma4 speculative decoding**, which maps onto our existing MTP/EAGLE-3 proposer infra (§27/§28 ✅) rather than greenfield. Mobius already has `src/mobius/models/gemma4_assistant.py`. Two scoping agents mapping the exact runtime-proposer contract + mobius emitter gap; implementation to follow in BOTH repos (runtime + mobius `--runtime onnx-genai` emitter, PR #398).

> **LANDED 2026-07-13:** Gemma4 assistant speculative decoding is a **new shared-KV proposer type** (NOT MTP/EAGLE-3): the 4-layer assistant shares slices of the target's paged KV (`shared_kv.{sliding,full}_attention.*`), has its own internal lm_head (`logits`), and threads `projected_state` (`inputs_embeds = concat(prev,cur)` of backbone-hidden vectors). Runtime slice landed (metadata `ProposalType::Gemma4Assistant` + parser, `Gemma4AssistantDecodeSession` in ort, `Gemma4AssistantProposer` + KV-slice sharing in engine, metadata-driven activation, non-runner decode path). Synthetic `onnx_ir` fixture proves speculative output is **token-identical to plain greedy** (`tests/gemma4_assistant_full.rs`). Mobius exporter landed on branch `onnxruntime/mobius:feat/gemma4-assistant-onnx-genai` (off PR #398): emits the `speculative:` block + copies tokenizer. **Remaining for real E2B/12B run:** export gated Google weights on a high-mem CUDA host + accuracy/speedup validation; refine multi-layer shared-KV binding (runtime currently binds one representative target layer per group).

> **REAL E2B RUN LANDED 2026-07-14 (Milestones A + B):** Real **Gemma4 E2B** exported via Mobius as a **single-model merged package** (target `input_ids→logits + projected_state f32[.,.,1536] + present.{0..14}`, plus `assistant/model.onnx`; Mobius `8c77d78`) and run end-to-end on **H200 via CUDA EP** (`ONNX_GENAI_EP=cuda`, verified 34 GB VRAM / no CPU fallback). Required heterogeneous **per-layer head_dim** KV cache (E2B sliding hd256 / full hd512 at layers [4,9,14]) — landed as `LayerTensorConfig` in kv + engine (commit `9db1a3c`, W2 Batty + W3 Leon, Chew 🟡). **Milestone A:** target-only greedy coherent (`"The capital of France is" → "Paris."`), ~166 tok/s (`abd0b7a` + `scripts/run_target_greedy_cuda.sh`). **Milestone B:** shared-KV **speculative decode token-for-token identical to greedy** on the real weights (commit `10f82b3`: SWA models take paged windowed path even with share-buffer dtype; dtype-agnostic proposer f16↔f32; `tests/milestone_b_real.rs` env-gated). Correctness ✅. **Open perf follow-up:** speculative is currently **0.53× (slower)** — acceptance ~25% with `multi_token_accepts=0`; the drafter never gets 2+ ahead. Diagnosed to the `projected_state` hidden-space choice (backbone post-norm `last_hidden_state`); affects speed only, not correctness. Also open: BOS-adding tokenizer in the package (Gemma degenerates without a prepended `<bos>`); §38 connector payload path still uniform-geometry (dead code for E2B, thread per-layer before enabling a connector on a mixed model).

Scoped the "export Gemma4 E2B/12B via Mobius and smoke-test through onnx-genai" ask. It is **blocked on major runtime + Mobius work**, not a metadata one-liner:

- **Runtime VLM contract today:** exactly one ONNX input `pixel_values` (Float32, rank-4 NCHW/NHWC RGB); server supplies `<component>.pixel_values` + tile count; prompt must contain exactly one placeholder (image URL parts do not auto-insert it yet — text must contain `<|image|>`); placeholder expands to `tokens_per_tile × num_tiles` before KV alloc; multi-image rejected. (`server/state.rs`, `server/image_input.rs`, `engine/pipeline.rs:301`.)
- **Concrete Gemma4 IDs:** soft-token placeholder id `258880` (`<|image|>`), `tokens_per_tile = 280` (E2B fixed; 12B advertises 280 but merged-patch count can vary). `255999` is BOI `<|image>`, **not** the repeated soft-token.
- **Why metadata-alone fails:** Gemma4 vision graphs take **rank-3 pre-patchified** tensors (`[B,N,768]` E2B / `[B,N,6912]` 12B) + require `pixel_position_ids`, dtype f16 (server forces Float32), and Gemma4 uses a **separate embedding model → `inputs_embeds`** whereas our decode loop expects token-ID inputs and runs no non-decoder component per step.
- **Mobius gap:** `--runtime onnx-genai` exists only in open PR #398 (`541b8a7`), which emits decoder-only attn/KV metadata — **no `pipeline`, preprocessing, `pipeline.vision`, or tokenizer copy**.
- **Speculative model:** Gemma4 `*-assistant` are 4-layer **MTP** proposers consuming target hidden states + shared KV — **not** ordinary causal drafts; cannot use the `draft_model` path. Needs a dedicated Gemma4-assistant proposer.
- **Recommended sequencing:** (1) multi-tensor + rank-3 vision ingestion incl. `pixel_position_ids`; (2) embedding→decoder orchestration (or a Mobius fused decoder taking `input_ids`+`image_features`); (3) extend PR #398 emitter with pipeline topology + tokenizer copy + E2B vision metadata; (4) validate `--no-weights` first; (5) full E2B export on high-mem CUDA host; (6) smoke-test single image + `<|image|>Describe this image.`; (7) 12B after variable merged-patch support (~25 GB); (8) Gemma4-assistant proposer + spec integration.

## Notable design changes / decisions to record

- Preprocessing lives in its own crate `onnx-genai-preprocess` (§35).
- Real-model exact-equality tests use `intra_op_threads=1` (ORT FP determinism).
- Paged/radix attention (§39.4 Option C): Mobius now grows block-table KV via standard ONNX ops (ScatterND + Gather + opset-24 Attention) — draft PR onnxruntime/mobius#395. Same op path supports vLLM PagedAttention AND SGLang RadixAttention (share physical pages via block_table). Runtime-side wiring to consume paged KV is the next step once the PR lands.
- Audio & vision quality gated on real Mobius model packages (Whisper / CLIP+decoder).
- **Benchmarking (§ new):** `onnx-genai-bench` cross-runtime harness (`compare.rs` / `scripts/compare_runtimes.sh`) measures TTFT + decode tok/s vs Ollama (llama.cpp) + LM Studio over the OpenAI API. Goal: beat llama.cpp + LM Studio (Metal). Runs recorded under `docs/benchmarks/`.
  - **Fix (GQA):** Mobius `--ep webgpu` emits `com.microsoft::GroupQueryAttention` — WebGPU Qwen2.5-0.5B now **24 GQA / 0 Attention, 268 WebGPU / 6 CPU, 1 H2D / 0 D2H** (transfers eliminated).
  - **Q4 correctness FIXED** (mobius PR #396): two GGUF→ONNX bugs (missing Qwen2 QKV biases + wrong NEOX reverse-permute) — garbage → coherent.
  - **fp16 GQA KV consumed** by runtime; **runtime now owns the KV cache via our own `InferenceMetadata`** (`inference_metadata.yaml`), NOT ORT-GenAI `genai_config.json` (deleted). GQA op = on-device attention compute only. Mobius emits our config via `--runtime onnx-genai` (PR #398).
  - **In progress:** Q4+GQA WebGPU model (quantized weights + on-device attention) — the fair GPU comparison vs LM Studio Metal.
  - **Q4+GQA WebGPU = 30.5 tok/s** (168 MatMulNBits + 24 GQA); quantized embedding via `GatherBlockQuantized` (272MB→76MB, mobius PR #400); Q4_K_M support (PR #399).
  - **Device-resident KV blocked by ORT 1.27 WebGPU EP** — binding a user-preallocated device tensor as an in-place GQA share-buffer SIGSEGVs on long gens; gated behind `ONNX_GENAI_DEVICE_KV=1`. Safe default (`validationMode=disabled`) ships → **~49.6 tok/s** (no regression). Plumbing ready for when ORT fixes it — and for **CUDA** (mature IoBinding + `enable_cuda_graph`), the likely path to close the gap on H200.
  - **🎯 ROOT CAUSE of the CPU gap found (accuracy_level):** MatMulNBits was emitted with NO `accuracy_level`, so ORT's MLAS ran the slow fp32 dequant+GEMM path instead of the int8 dot-product path (ARM SDOT / x86 VNNI) llama.cpp uses. Fix (mobius PR #402, `default_int4_accuracy_level=4` was dead config, now plumbed). Measured CPU decode: baseline 39.3 → **acc4 91.8 (2.33×)** → **quantized-head + acc4 = 194.7, EXCEEDS LM Studio CPU (157)**. Session was already persistent; graph-opt already ALL; orchestration ~1%. fp16/bf16 levels regress on M1.
  - **Definitive CPU (WARM, corrected 2026-07-13):** at parity-or-ahead of ALL runtimes. Warm decode: short ~212 (ours) ≈ LM Studio 156 / Foundry 212; **long ~175 (ours) LEADS** LM Studio 160 / Foundry 160; beat Ollama both. The earlier "Foundry Local leads 203/166" was a **thermal/under-warmed artifact** (this M1 Max swings 85–216 tok/s unwarmed) + pre-KV-fix. Decisive test: FL's *exact* model through OUR runtime = decode parity (the FL win doesn't reproduce). FL sets zero custom ORT session options — we already match; their fused-QKV (121 vs our 169 MatMulNBits) is decode-neutral (bandwidth-bound). Residual FL edge = ~2-4% prefill/TTFT only.
  - How CPU is achieved: `accuracy_level=4` (int8 MLAS path, 2.3×) + quantized head (#401) + quantized embedding (#400) + in-place O(1)/token GQA KV (fp32-GQA gate fix). Orchestration is ~1% (98.9% ort.session_run); session persistent, KV Arc-rotated.
  - **CUDA (H200):** `--features cuda`, device-resident KV + `enable_cuda_graph`. Runbook committed.
  - **🍎 MLX-native EP (`../onnxruntime-mlx`, renamed from onnxruntime-mps):** **Full pivot to MLX (2026-07-13).** After the hand-kernel EP reached decode 11.3→133 tok/s + prefill GEMM + a validated MLX-prefill/hand-decode hybrid, MLX was found **Pareto-dominant** (decode never slower + prefill 2.5–3× + coherent + memory-stable). Per user directive (全面改mlx / keep one efficient impl), **MLX is now the SOLE compute path** — the whole fused decoder subgraph (prefill AND decode) runs through MLX; **all hand-written `.metal` kernels + machinery were deleted**. Architecture: ONNX fused subgraph → MLX graph → single `mlx_eval` at the boundary → ORT outputs (MatMulNBits→`mlx_quantized_matmul`, GQA→`fast_scaled_dot_product_attention`+`fast_rope`, RMSNorm→`fast_rms_norm`, …). `mlx-c` is a **hard build dep** (mlx 0.32 / mlx-c 0.6). **EP name `MLXExecutionProvider`** (renamed from the interim `MetalEP`); onnx-genai binds by this exact name (`REGISTRATION_NAME` in session.rs) — only point `ONNX_GENAI_METAL_EP_LIB` at the new **`libonnxruntime_mlx_ep.dylib`**. Verified: build green, **ctest 3/3** (mlx_op_tests/mlx_leak_test/mlx_e2e), E2E coherent ("Paris"), memory flat (0.00 MiB/7 cycles), **prefill 2.2–3.5× vs CPU**, decode ~122–148 tok/s. MRR leak fix preserved. Follow-ups: bf16 end-to-end, GatherBlockQuantized asymmetric zero_points, larger-model decode edge.
  - **🎯 Full Mobius + broad ai.onnx opset-17+ op coverage (landed, 2026-07-13):** **162 of 202 ai.onnx ops** on MLX (up from 11), each claiming the **most relaxed dtype set** (bool/int/uint 8-64/fp16/bf16/fp32; float64 excepted — Metal has no double precision). **676 pytest passing / ~54 skipped** (ORT-CPU dtype gaps), ctest 3/3, coherent E2E, memory-safe (`ctx.Keep`), zero-size handled on MLX. Includes signal/FFT (DFT/STFT/windows/MelWeightMatrix — audio frontends) + vision transforms (GridSample/AffineGrid/Col2Im). Built by parallel worktree agents; zero `ep.cc` edits per op. The 40 remaining ops are all engine-limited/impossible on Metal (control-flow, recurrent, sequence/optional/string/loss types, DeformConv/RoiAlign/NMS, exact-integer QLinear*, mlx-c-blocked NonZero/Unique/Det). Also: interior-optional-input null-deref fixed, claim predicates hardened, teardown UAF fixed, `mps→mlx` fully renamed, single abi3 wheel + CI green.
   - **🚀 Shipping track (landed, 2026-07-13):** the MLX EP is now installable + CI-gated + publishable. **Python package** `onnxruntime-mlx` (nanobind + scikit-build-core, `python/`): `pip install onnxruntime-mlx` → `onnxruntime_mlx.register_execution_provider_library()` + `providers=["MLXExecutionProvider",...]`; the wheel bundles the mlx runtime (delocate), onnxruntime resolved at runtime. **CI** (GitHub Actions, macOS-arm64): builds the EP (provisions ORT 1.27.0 + brew mlx-c) + runs the pytest op suite + a wheel smoke test — **green on first run**. **publish.yml**: cibuildwheel (cp39–cp313) + PyPI **Trusted Publishing** (OIDC, no token) on release. **README** rewritten with install/usage (Python + C/C++). Also fixed a teardown use-after-free (segfault at process exit via onnx-genai): DataTransfer new/delete + MetalContext shared_ptr (EP) + `Engine._environment` dropped last (runtime) → 0/10 crashes. Perf (warm): prefill **1.85–2.77× vs CPU**; decode CPU-favored on 0.5B (weight-bandwidth-bound). Conformance vs cbourjau/onnx-tests in progress.
   - _History (pre-pivot):_ custom Apple Metal ORT plugin EP, empty→working in-session. Skeleton→kernels (366/393 on GPU)→subgraph fusion (366→27)→GQA kernel (→1 subgraph)→uint4 bandwidth loads→**prefill GEMM**→MLX-prefill hybrid. Fixed critical MRR MTLBuffer leak (was crashing machine) + leak-regression test. Modular op-architecture designed; MLX evaluation (Phase-0) → full-swap NO-GO on the 1.25× decode bar, but the user overrode on the one-efficient-impl principle. Team: Nabil/Mariette/Coco/Freysa.
  - Config standard: `schemars` JSON Schema (Draft 2020-12). Mobius PRs #397-402 open (#399 rebased/mergeable), #395 draft.
