docs(metadata): record the completed model.io removal, and finish the audit

The design note already contained the staged removal path for `model.io`; this
records that steps 2-4 are done and why step 3 was skipped — a warning that
still loaded the package would have kept the second answer alive for another
release, which is the opposite of the point.

Also finishes the audit the reversal implies:

* `schema/model_io.rs` is `schema/decoder_abi.rs`. The file no longer holds a
  serialized block; it holds the *resolved* ABI type, which the design note
  itself records being mistaken for the retired key often enough to be worth
  renaming.
* Comments across the importer, recognizer and parser that described `model.io`
  as a live source now describe what actually holds. The retired spelling
  survives in exactly two kinds of place: the rejection that explains it, and
  the migration guidance that names the conversion.
* `MODEL_METADATA.md`, `DESIGN.md` and `WORKFLOW_POLICY_COMPONENTS.md` no longer
  describe an import-only fallback that does not exist.

Verified: fmt; clippy `-D warnings` across engine, metadata, server, CLI, C API
and genai-config; H200 CUDA — canonical_execution_parity 8, gemma4_chained
8, native_workflow_parity 12, one_runtime_e2e 5, fixture floor 3, real-model
corpus green. **All five real foundry models are byte-identical to the
pre-change baseline**, which is the load-bearing evidence: retiring the
serialized block and executing every decoder as a workflow changed no token.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
