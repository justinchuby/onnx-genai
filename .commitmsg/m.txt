Merge remote-tracking branch 'origin/main' into feat/native-workflow-backend

Two conflicts, both resolved in the direction the branch already established.

`native_decode/proposer.rs` was deleted here when the shared-KV proposer surface
went (Rule 3); main gave it an `optimization=basic` session option for
quantized-MoE relayouts. The file stays deleted: the option belongs to sessions
that still exist, and `native_decode/load.rs` already carries it.

`tiny-glm52-qmoe-indexshare` was rewritten on both sides — main regenerated the
fixture for the GLM-5.2 / DeepSeek-V4 native decode work, and this branch
re-emitted every converted package so its decoder declares
`onnx-genai.autoregressive-decode`. Main's version is taken and re-emitted
through `migrate_model_io --reemit`, so the fixture is main's content stated in
the current emitter's form rather than a hand-merged hybrid of the two.

Checked afterwards that all fourteen packages declaring the runtime token policy
also declare the decode contract, so no fixture main added or changed slipped
through without the step it executes being named.

Verified: engine, server and metadata suites; fmt; clippy `-D warnings`.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
Signed-off-by: Copilot <223556219+Copilot@users.noreply.github.com>
