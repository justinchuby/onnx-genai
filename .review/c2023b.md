Pre-flight result, as promised. **The step this PR makes required passes on the merged tree.**

```
$ NXRT_REQUIRE_ORT_TESTS=1 cargo test --locked \
    $(python3 .github/scripts/workspace_test_packages.py cargo-args ort-backed) \
    -- --test-threads=1

TOTAL: 1394 passed, 0 failed, 26 ignored
```

Exit 0. Same env var, same `--test-threads=1`, same derived package set as the workflow step, on `main` merged in as of `cf23d0e7e`.

The specific thing I wanted to know: **`shape_dispatch_gate` executed** —

```
Running tests/shape_dispatch_gate.rs (target/debug/deps/shape_dispatch_gate-8b29e27828ebcffc)
```

That is the test #1982 left failing on `main` with both required checks green, and it is this PR's motivating example. It runs and passes now, so promoting this lane to required does not import a known-red test. That was the merge-blocking risk and it is answered.

**Limits, stated:**

- x86_64 Linux only. This says nothing about the Windows leg, which this PR leaves in `CLI ORT` — still the only place these run on Windows, and still advisory.
- **26 ignored.** I have not enumerated them, so I am not claiming all 1394+26 are meaningfully covered — only that nothing fails and nothing errors.
- Bounded to `taskset -c 24-31` on a shared host; this is a pass/fail run, not a measurement, and I make no claim about host quietness.
- 370s wall on a warm tree. The workflow step will be slower from cold, though as the PR notes, the two clippy steps above it already build this graph with the ORT download, so the marginal cost is codegen, linking and the run.

**Still not arming auto-merge**, and the reason is unchanged: `Rust quality` remains red on `main` from #2056, so this PR's own CI cannot yet distinguish "the new step failed" from "the inherited break failed first". Once #2115 lands I'll refresh this branch and let its own required checks speak. The local run above is evidence, not a substitute for that.
