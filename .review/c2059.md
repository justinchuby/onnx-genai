Correction to my comment above, for the record: **#2078 landed the platform gate ~20 minutes before I posted it** (`6db21953c`), so `main`'s two Windows lanes were already repaired and my "Fix in #2084" was stale when written.

I merged #2078 in and rescoped #2084 to the part it doesn't cover — the `allowed == cores` premise check, which **passes** on an unrestricted process rather than failing, and misattributes to #1780 when it does fail. Detail on #2078.
